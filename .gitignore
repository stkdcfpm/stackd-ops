# Live data exports must NEVER be committed — this repo is publicly served via GitHub Pages
Test-data/
Stackd-Backup-*.json
Stackd-Clean-*.json
*.backup.json

# OS cruft — accidentally tracked twice before (SEC-GAP-020-class hygiene miss)
.DS_Store
