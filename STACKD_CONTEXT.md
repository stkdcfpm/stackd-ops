# STACKD — Project Context

> **Single source of truth for all Claude tools — Projects, Code, and new conversations.**
> Fetch this file at the start of any new session: `https://raw.githubusercontent.com/stkdcfpm/stackd-ops/main/STACKD_CONTEXT.md`
> Updated by Claude Code on every version delivery. Updated by Claude Projects after every strategy session.

---

## Current state

| Field | Value |
|-------|-------|
| Last updated | 4 September 2026 (this update covers REQ-CLOUD-006, built and tested on branch `claude/cloud-006-invoice`, PR pending at time of writing — code-level facts confirmed by running `node tests/run.js` on that branch after merging `main`; REQ-INTEG-002-2c/v2.9.78 is confirmed merged to `main`) |
| Current version | v2.9.79 |
| Test count | 867 / 867 passing (confirmed by re-running the suite, not just read from source) |
| Build branch | main |
| Deployment | **GitHub Pages**, custom domain `app.getstackdops.com` (see `CNAME`) — live, not pending |
| CI/CD | GitHub Actions (`qa.yml`) — runs `node tests/run.js` on every push/PR to main |

**Correction from the previous version of this file:** this file was frozen at v2.9.51 / 422 tests since 10 August while the codebase moved on to v2.9.57 / 521 tests across six shipped versions (v2.9.52–v2.9.57 — per-line quote margins, Supplier price intelligence, the first Supabase-backed layer (Suppliers/Buyers Cloud Data), three new AI Assistant capabilities, RFQ supplier comparison, and a data-integrity cleanup tool). See Version history below for the full list, including v2.9.58 (Phase 1 of an Order Request/RFQ → Quote → Invoice referential integrity build), v2.9.59 (removes v2.9.58's unused `sourceQuoteId` field, confirmed unnecessary post-merge), v2.9.60 (Phase 2 of the same initiative — buyer-approval capture on Invoice), v2.9.61 (Demo Mode expanded with 7 scenarios so Phase 1/2 can be tested locally instead of against production data — interim mitigation for `SDLC-GAP-003`), v2.9.62 (REQ-INTEG-002 sub-phase 2a — a Supplier Payment ledger, the first standalone piece of a larger Payment Allocation build), v2.9.63 (REQ-INTEG-002-2a-fix — reconciling `PO.dep` display with that same ledger, since real-world testing found v2.9.62's ledger had zero visible effect anywhere until this fix), v2.9.64 (REQ-INTEG-002-2a-fix-2 — 4 real issues found in manual testing of v2.9.63, most critically a currency-mixing bug in the Accounts totals bar), v2.9.65 (REQ-CUR-002 — closes the two currency-mixing gaps logged, not fixed, alongside v2.9.64), v2.9.66 (REQ-INTEG-002 sub-phase 2b — Invoice→PO enumeration fix, making `inv.pos[]` the authoritative record after check-first found it was a dead field), v2.9.67 (REQ-SYNC-002 — batches Sheets sync into one request per direction instead of 8-10 sequential ones, closing `SYNC-GAP-002`; requires a manual Apps Script redeploy to take effect), v2.9.68 (REQ-QTE-002 — lets an operator override the three global overhead charges — Origin/Dest Charges, FPM Admin — on a per-quote basis instead of only globally in Settings), v2.9.69 (REQ-ORD-006 — Edit/Delete buttons on RFQ responses, closing a real gap the user hit correcting a supplier's revised pricing; the load-bearing detail is that both operations rotate the response's id/repoint `committedResponseId` rather than mutating in place, specifically so the existing Quote staleness banner still fires, via PR #119), and v2.9.70 (REQ-AI-GAP-011 — an envelope button on each RFQ response lets an operator paste a supplier's email and get an AI-extracted before/after diff of commercial terms to Apply or Discard; Apply reuses REQ-ORD-006's edit mechanism unmodified, inheriting its staleness-banner behavior for committed responses, via PR #120), all shipped and merged as of this update. v2.9.71 (REQ-DATA-003 — fixes a live, user-reported CSV-import bug: a multi-line quoted Notes/Address field was torn into multiple phantom Supplier records with real, non-detectable ids; rewrote `parseImportCSV()` as a single-pass character-stream parser with continuous quote-state tracking, confirmed the Google Sheets sync path was never involved, via PR #121), a docs-only backlog entry logging the live Suppliers-tab cleanup as a prerequisite for the cross-platform backend initiative (PR #122), and a comprehensive architecture/data-model reference document mapping every entity, relationship, and data-management process in the platform ahead of scoping that same initiative (`docs/architecture-data-model-v1.md`, PR #123), all shipped and merged as of this update. v2.9.72 (REQ-PO-002 — fixes a live defect surfaced by that same architecture review and assessed there as its single highest-priority finding: `qteToPoConvert()` built every Quote-converted Purchase Order using the wrong field names, at both the document level and the nested line-item level, making the resulting record functionally invisible to the rest of the app — empty editor, $0 totals, no drift detection, wrong currency, no FPM-funding credit — on the application's primary conversion path; fixed both levels of the mismatch and added a migration for already-broken historical records, wired into the same 5 call sites as the existing `backfillInvoicePOs()` migration, via PR #124), shipped and merged. v2.9.73 (REQ-CLOUD-002 — extends the Supabase-backed Cloud Data layer, previously scoped to Suppliers/Buyers only, to Line Item and Contact — Phase 1's second step of the same cross-platform backend migration; the Supplier-migration-completion precondition queries the live, currently-connected Supabase project rather than a local flag, and a pre-flight check on each migration verifies every local foreign-key reference actually resolves against that project before any row is inserted; both entities do a full exhaustive external-reference sweep and 30-day archived rollback mirroring the existing Supplier/Buyer pattern; five existing UI actions that mutate Contact fields outside the normal save path, plus one Line Item price-history site, were made Cloud-Data-aware since each would otherwise have silently discarded a real edit on the next Cloud refresh — found only in the third of four spec-gate rounds, alongside a data-loss defect in the initial refresh-wiring design that would have wiped every existing Cloud Data adopter's local Line Item/Contact data on first reload, both fixed before implementation began, via PR #125), shipped and merged. v2.9.74 (REQ-CLOUD-003 — extends the same Cloud Data layer to Order Request, Phase 2 sub-phase 1 of 3 of the deal-pipeline step of the cross-platform backend migration, with Quote/Purchase Order deferred as separate future sub-phases; Order Request Lines/RFQ Responses stay embedded as one JSONB column on the parent row rather than being promoted to child tables, with no migration precondition since nothing in that blob can be FK-constrained; eleven mutation sites share one new `persistOrdChange()` helper, an inventory that took three requirements-gate rounds to get exhaustive; fixed two confirmed-live bugs found during this REQ's own research — the Supplier/Buyer sweep never rewrote RFQ Response `supId`, and neither that sweep nor the Contact sweep pushed an already-migrated Order Request's fix back to Supabase, only to its local mirror — and corrected a stale `AI_SYSTEM_PROMPT`/user-guide Cloud Data description that had never been updated past its original Supplier/Buyer-only scope; spec-gate needed 3 rounds (the first two both FAIL — a SQL column-type bug, a refresh function never wired into app startup, and a data-loss bug in round 1; 12 pre-existing tests broken by the required async conversion, undetected until an explicit trace of the test suite itself, plus a second data-wipe risk in the CSV re-importer in round 2); implementation then self-caught two more defects via mutation testing that neither gate's test list covered, including a `forEach`-to-`for`-loop conversion that needed every internal early-exit rewritten from a function-ending `return` to a loop-continuing `continue`), shipped and merged via PR #126. v2.9.75 (REQ-CLOUD-004 — extends the same Cloud Data layer to Quote, Phase 2 sub-phase 2 of 3 of the deal-pipeline step, with Purchase Order deferred as the final future sub-phase; Quote is the first entity in this series whose own id is referenced *outward* by other entities (`Order Request.activeQuoteId`, `PurchaseOrder.quoteId`, `Invoice.linkedQuoteId`), so its migration sweeps outward via its own id-map — never via `Quote.linkedPOIds[]`, which can carry a stale entry per `PO-GAP-007` — in addition to the usual inward retrofit of the four existing sweep functions, each of which now also pushes a touched Quote back to Supabase if Quote has already migrated; a real pre-flight duplicate-number scan was needed since Quote's number, unlike Order Request's, is manually editable; requirements-gate took 3 rounds (round 1 found the outward-sweep field list incomplete with its corresponding retrofit missing entirely, plus a self-contradictory SQL type; rounds 2/3 each closed one advisory) and spec-gate 2 rounds (round 1 found that wiring the new refresh function into app startup broke a pre-existing Order Request test and, through it, a second one, an optional-field mapping that assigned `undefined` inside an object literal without actually omitting the key, and four tests that never reached the code they claimed to exercise; round 2 clean) — every fix in both spec-gate rounds was verified by applying the diffs to a scratch copy of the real codebase and re-running the suite, not just re-reading the SPEC, via PR #127), shipped and merged. v2.9.76 (REQ-CLOUD-005 — extends the same Cloud Data layer to Purchase Order, Phase 2 sub-phase 3 of 3, completing Phase 2 of the deal-pipeline step of the cross-platform backend migration; Purchase Order is the first entity in this series built from three different creation paths (`savePO()`, `autoPos()` — confirmed as the entity's actual primary/majority creation path — and `qteToPoConvert()`) rather than one save function, each gaining its own dedicated Cloud Data branch; requires a combined precondition never needed together before (Supplier-migration-completion plus a live pre-flight `supId`-resolution check, and a genuine pre-flight duplicate-`num` scan since Purchase Order's number is manually editable); Purchase Order's own id is referenced outward by an array field, `Quote.linkedPOIds[]`, requiring per-element `.map()` remapping rather than scalar rewriting; closes `PO-GAP-007` (`delPO()` now sweeps a deleted PO's id out of every Quote's `linkedPOIds[]`, unconditionally, regardless of Purchase Order's own migration state); requirements-gate took 3 rounds (round 1 found 5 blocking/5 advisory, most notably a completely missing `autoPos()` mutation site despite it being the primary creation path; round 2 corrected an over-generalized attribution; round 3 clean) and spec-gate took 3 rounds, the most severe of this series — round 1 **FAIL** with 10 blocking/5 advisory including a literal syntax error (`await` inside a non-async `savePayment()`) that would have broken the entire application, reproduced as a collapse to 17/771 passing tests; round 2 **FAIL** with 3 blocking findings after an independent reviewer, applying every diff literally rather than trusting round 1's own "all fixed" commit message, found the `pullAll()` exclusion fix, the `savePayment()` async signature diff, and a stale pre-existing test's title/assertion were each still genuinely unaddressed in the actual document; round 3 clean PASS — every round's fixes independently verified against a scratch copy with the full suite re-run, not just re-reading the SPEC's own claims; implementation then mutation-tested across every safety-critical piece (the outward array sweep, `PO-GAP-007`, both multi-PO-creation paths, both FPM-recovery pushes, the `pullAll()` exclusion), each reverted mutation reproducing exactly its predicted test failure, shipped and merged via PR #128. Phase 2 (Order Request, Quote, Purchase Order) is now fully Cloud-Data-eligible; Phase 3 (Invoice, Credit Note, Shipment, Payment ledgers) remains explicitly future, unscoped work. v2.9.77 (REQ-LI-001 — user-requested: Line Item unit cost/price now accepts up to 3 decimal places instead of 2, for high-volume/low-unit-price items where the old cap measurably distorted margin/COGS; a new, narrowly-scoped `RX.currency3` regex is used only at `lf-c`/`lf-p`, leaving `RX.currency` and the 11 other fields sharing it untouched; a check-first pass found 8 separate display paths — the Line Items Library table and picker, Invoice/PO/Quote PDF previews, a Quote line's version-history panel, and Supplier Price History — that would have silently truncated a stored 3rd decimal even after validation allowed it, each fixed with a new `fmtN()` helper or an explicit `d=3` argument; requirements-gate took 4 rounds, each of the first three finding one genuinely new gap the prior round's fix had missed (a mislabeled/incomplete display-site inventory, a 7th display site, a stale summary count), round 4 clean; spec-gate clean on the first round; build-gate found 2 non-blocking hygiene findings, both fixed in place; 28 new tests, 803/803), shipped and merged. v2.9.78 (REQ-INTEG-002 sub-phase 2c — Buyer payment tranches: check-first found buyer payments already support multiple partial payments per invoice, so this sub-phase instead closes the real parity gaps against the now-more-mature Supplier Payment ledger — a required `purpose` field and `currency`/`rateLock` fields, reusing `lockFxRate()` verbatim, with a legacy-record currency default of `inv.cur` rather than `'USD'`; new `getInvTotalPaidNative(inv)`/`getInvEffectiveDepInfo(inv)` mirror the PO-side pair, built on a rewritten, now-shared `getInvPayments(inv)`; requirements-gate ran 11 rounds, the last four each correcting a real defect in this matching rule's `invId`/`invNum` field-preference order, concluding (unlike `REQ-INTEG-002-2b`'s PO-side precedent) that a payment's `invId` is the more trustworthy field, since neither field is ever re-edited after creation and only `invNum` can be silently invalidated later by an unrelated invoice rename; six currency-blind/matching-inconsistent read sites now share one correct reconciliation path; also fixes a confirmed-live bug, `deletePayment()` never reversing `inv.status`; logs `PROC-GAP-003` (NGN/GHS share EUR's pre-existing, unlogged `toGBP()`/`fromGBP()` gap), guarded against, not fixed; spec-gate 2 rounds (round 1 CONDITIONAL PASS, 1 blocking/2 advisory, fixed in place; round 2 independent re-verification from scratch, clean PASS); implementation mutation-tested, reverting the `invNum` fallback reproduced exactly its predicted single test failure; rebased onto `v2.9.77`/`REQ-LI-001` after this branch was cut, bumping that REQ's `fmt()` call-site regression-guard count by 1 for this REQ's own new, legitimate call site; 31 new tests, **834/834**), shipped and merged. v2.9.79 (REQ-CLOUD-006 — extends the same Cloud Data layer to Invoice and Credit Note, Phase 3 sub-phase 1 of 3 of the cross-platform backend migration, with Shipment and Buyer/Supplier Payment deferred as later sub-phases; the most demanding precondition in the series (Buyer, Line Item, Quote, AND Purchase Order must all have completed their own migrations); the first self-referential FK within one migrating table (a Credit Note's `linkedInvId` pointing at another row in the same `DB.inv` batch), resolved via a two-pass insert; thirteen call sites — the most of any entity so far — share a new `persistInvChange()` helper; requirements-gate took 8 rounds, the longest in this series, three of them (rounds 5-7) each finding one more instance of the identical `invId`/`invNum`-divergence bug class in a different sweep (Purchase Order's outward link, the local Buyer Payment sweep, then the CN self-referential remap itself); spec-gate took 2 rounds (round 1 CONDITIONAL PASS/FAIL — 3 blocking, including a real `saveCN()` implementation bug where the cloud-saved record was never committed to `DB.inv` outside the local-only branch — fixed; round 2 independent re-verification from scratch, clean PASS, 835/835 against the SPEC's own 803-baseline); implemented on top of `REQ-INTEG-002-2c` (merged after the SPEC was drafted), requiring three of the SPEC's own diffs — `saveCN()`'s balance-due recompute, `savePayment()`, `deletePayment()` — to be adapted onto that REQ's already-shipped improvements (`getInvEffectiveDepInfo()`/`getInvTotalPaidNative()`, `deletePayment()`'s status-re-derivation block) rather than applied verbatim; found and fixed one genuine test-authoring bug during implementation (a mismatched invoice number between a new test's setup and its own assertion, which had been silently leaking Cloud Data state into the rest of the suite by throwing before its own cleanup ran); self-directed mutation testing on the `saveCN()` commit-ordering fix reproduced exactly its predicted single test failure; mutation-testing the two-pass remap's `linkedInvNum`-first precedence found the SPEC's own AC-4b test doesn't actually discriminate order (its blank-`linkedInvId` scenario falls through to the same fallback either way) — closed with one new build-time regression test exercising a genuinely disagreeing `linkedInvNum`/`linkedInvId` pair, confirmed to fail under the mutation and pass once reverted; closes no open gaps outright but logs `CN-GAP-001`/`INV-GAP-002` and broadens `CLOUD-GAP-003` to finally cover Purchase Order's own CSV-import bypass, which `REQ-CLOUD-005` had committed to logging but never did; 33 new tests, **867/867**), built and tested on branch `claude/cloud-006-invoice`, PR pending at time of writing, per the Last-updated note above.

---

## The business

*(Business/legal facts below are carried forward from the prior version of this file. They live outside the git repo — Companies House, UKIPO, ICO, bank/pricing records — so they cannot be verified or refuted from source code. Treat them as last-known, not code-verified.)*

**FPM International Ltd** — Brighton-based trade intermediary. Sources from Chinese manufacturers. Supplies Caribbean markets (primarily Barbados). Registered at Companies House May 2026. SIC 46190 + 62012.

**Stackd** — the product. Trade operations platform for sole traders to mid-size firms managing international trade. FPM International is the founding client and proof of concept.

**Wedge feature** — live shipment visibility without human cover. The feature that makes operators sign up. Everything else makes them stay.

**Design principle** — Automation First. Every feature evaluated against one question: does this remove manual work or just move it? Non-negotiable. **Forward-compatibility clause:** the goal is AI-augmented, not AI-run — Automation First does not mean every click must disappear in the current architecture. Where the v2.9.x stack (single-file, `localStorage`-only, no server-side proxy) genuinely can't eliminate a manual step, the requirement downgrades from "remove the click" to "don't build the data model, UX, or integration point in a way that needs rework at the v3.0.0 migration." A feature that leaves a manual step in place but shapes its data/API surface so v3.0.0's server-side architecture can automate it without a redesign satisfies the principle; one that removes the click today by bolting on something the v3.0.0 migration would have to unpick does not.

**ICP** — UK-based sole traders to micro-firms sourcing from Asia, selling to Caribbean and West Africa. Expanding to UK-Nigeria, UK-Ghana, UK-India corridors.

---

## FPM portal — data state

**Not verifiable from this repo.** The portal is `localStorage`-only (see Architecture below) — there is no server, no database, and no data file committed to git. The KPI/entity figures in the previous version of this file (Invoice Revenue $53,441, 4 invoices, 9 suppliers, etc., "verified 13 May 2026") cannot be confirmed or refuted from source control, and are now three months stale regardless. Two facts *are* code-verifiable and matter for interpreting any such figures:

- **SYNC-GAP-001 (fixed v2.9.47; residue cleanup shipped v2.9.57):** between v2.9.12 and v2.9.46, the Google Sheets "⟲ Sync" **pull** path corrupted every pulled record (fields landed under the wrong property names) and silently broke the delete buttons for those records. Any data pulled from Sheets in that window may contain blank-looking phantom records consuming reference numbers. Fixed in v2.9.47 with reverse field-mapping (`unmapRec()`) and business-key matching — but records already corrupted *before* that fix were never cleaned up, and this is exactly what the operator reported seeing in the live portal (blank/undeletable Supplier and other records) in August 2026. **REQ/SPEC-DATA-002 (v2.9.57)** added a Settings → Data "Scan for phantom records" tool: a read-only scan, a mandatory backup gate, then removal plus sequential renumbering of Supplier/Line Item/Buyer/Contact/Order Request reference numbers (Invoice/PO/Quote/Credit Note numbers are permanently excluded from renumbering, since those are real cross-entity lookup keys elsewhere and appear on documents already sent out). Also hardened `pullAll()` itself so a live recurrence of this defect class is dropped at the source with a console warning, not just historical residue.
- **SEC-GAP-020 (fully resolved 2026-07-05):** GitHub Pages serves the *entire* repository, not just `index.html`. A live business data export (`Test-data/Stackd-Clean-2026-05-17.json`) and this file's supplier-contact table were publicly exposed at `app.getstackdops.com` for some period before discovery. Both were purged, git history was rewritten (`git filter-repo`) across all branches and force-pushed, GitHub deleted the 33 affected PRs at the operator's request, and a GDPR Art. 33(5) assessment concluded it was not ICO-reportable (low risk, B2B professional contacts only, same-day remediation). **Process going forward: every file in this repo is public. Never commit live data exports, backups, or PII.**

To get current KPI/entity figures, read them from the live portal (`app.getstackdops.com`) or its export, not from this file or git.

---

## Architecture

**Stack — still localStorage as the primary store, now v2.9.57 (same shape, one new optional cloud layer for two entities):**

```
index.html           — entire application: HTML + CSS + JS in one file (~850 KB)
tests/run.js         — Node.js test runner, 521 tests, Node VM sandbox
tests/fixtures.js     — shared test fixtures
apps-script/Code.gs  — Google Apps Script webhook handler (Sheets sync backend)
cloudflare-worker/worker.js — CORS proxy in front of the Apps Script endpoint
                       (validates the request path against the Apps Script
                       deployment pattern; no auth/secrets of its own — the
                       sync token still travels in the POST body)
vendor/supabase-js-v2.min.js — vendored Supabase JS client (same-origin static
                       file, no CDN, no auto-update) — Cloud Data feature only
supabase/migrations/0001_suppliers_buyers.sql — the one Supabase table pair
                       (Suppliers, Buyers) built so far — see Cloud Data below
CLAUDE.md            — Claude Code session context (technical)
STACKD_CONTEXT.md    — cross-tool master context (this file)
docs/known-gaps.md   — known gaps log (much more detailed than this file's summary)
docs/version-history.md — full version changelog
docs/requirements-tracker.md — every REQ/SPEC feature's gate history (req-gate →
                       spec-gate → build-gate), the FM-1 exception register, and
                       the full backlog/unscoped list (including the G-01…G-10
                       reporting-compliance items and v3.0.x-deferred items)
docs/dr-procedure.md — disaster recovery / backup procedure
docs/data-model.md, docs/agent-architecture.md, docs/workflow-bpmn.md,
docs/reporting-pipeline-runbook.md, docs/councils/ — supporting docs
```

**PR preview environment (REQ-SDLC-003, closes `SDLC-GAP-003`):** a second, dedicated public repository, `stkdcfpm/stackd-ops-preview` (no custom domain of its own), gives PR reviewers a running copy of the app before merge — `stkdcfpm/stackd-ops`'s two new GitHub Actions workflows, `preview-deploy.yml`/`preview-cleanup.yml`, copy each open PR's `index.html` to `stkdcfpm.github.io/stackd-ops-preview/PR-N/` and tear it down on close. This exists as a genuinely separate repository, not a subfolder of `stackd-ops` itself, because GitHub Pages redirects a repo's own default `github.io` URL to whatever custom domain that repo has configured — `stackd-ops` already has `app.getstackdops.com`, so any same-repo preview would silently resolve onto production and share its `localStorage` (origin-scoped, never path-scoped) with the real live data. Every preview in the second repo still shares one `localStorage` bucket with every other open preview — mitigated by defaulting to Demo Mode, not eliminated. Setup requires four manual, one-time steps documented in `docs/REQ-SDLC-003-v1.md`/`docs/SPEC-SDLC-003-v2.md` §3/§5 (create the preview repo and grant this session access — Claude Code's GitHub integration cannot create repositories itself — plus a scoped deploy token, adding it as a repository secret, and enabling Pages on the new repo); until they're all done, the two new workflows simply fail harmlessly with no effect on `main`, `qa.yml`, or the production site.

**Cloud Data (v2.9.54, REQ/SPEC-CLOUD-001) — the first crack in "pure localStorage":** Settings → Cloud Data lets an operator connect Suppliers and Buyers specifically to a shared Supabase project, so more than one browser/device sees the same Supplier/Buyer records. This is genuinely useful prior art for any v3.0 multi-tenant redesign, but it is explicitly **not** multi-tenant: it is one shared Postgres table pair behind Row Level Security that grants any authenticated user full read/write access — there is no tenant/org column, no per-customer isolation, and no role distinction. Migrating existing local Suppliers/Buyers to it is a one-time, explicit, backup-gated action. **Since this pilot, seven more entities have joined independently** (Line Item/Contact via `REQ-CLOUD-002`, Order Request via `REQ-CLOUD-003`, Quote via `REQ-CLOUD-004`, Purchase Order via `REQ-CLOUD-005`, Invoice/Credit Note via `REQ-CLOUD-006`) — only Shipment and the Payment ledgers (Buyer Payment, Supplier Payment) remain local-only, both currently in progress (`REQ-CLOUD-007`/`REQ-CLOUD-008`); the event log stays local-only by design regardless. See `docs/version-history.md` for each sub-phase's own details. A real v3.0 multi-tenant schema would still need to design tenant isolation from scratch across all of them — this pilot answers "can this app talk to Supabase at all" (yes) but not "how do we partition data between paying customers."

**State layer:**

```js
const K = { s, l, i, p, pm, sh, qt, ss, as, au, ai, co, ev, bu, ord }  // localStorage keys
let DB = { sup, li, inv, po, payments, sh, qt, con, events, buy, ord }  // all entity arrays
let EI = { s, l, i, cn, p, sh, qt, co }   // currently-editing ID (null = new)
let cIL = [], cPL = [], cQL = [], cCNL = []
const QR_DEFAULTS = { fxGBPUSD, fxGBPRMB, fxGBPBBD, lclPerCBM, fcl20GP,
                      fcl40HQ, originCharges, destCharges, dgSurcharge,
                      insRate, fpmAdmin }
var QR = { ...QR_DEFAULTS, ...ld('st_qr') }   // includes QR.displayCurrency (v2.9.46)
```

**Entities (10, unchanged since the last version of this file — no new top-level `DB` entity has shipped since v2.9.51; new capability in this period extended existing entities/fields instead, e.g. `rfqResponses[]` on Order Request lines):**

| Key | Store key | Description | Synced to Sheets? |
|---|---|---|---|
| sup | st_s | Suppliers | Yes |
| li | st_l | Line items (product catalogue) | Yes |
| inv | st_i | Invoices | Yes |
| po | st_p | Purchase orders | Yes |
| payments | st_pm | Payment ledger entries | Yes |
| sh | st_sh | Shipments | Yes |
| qt | st_qt | Quotes (v2.9.4) | Yes |
| co | st_co | Contacts (v2.9.27) | Yes |
| events | st_ev | Activity event log (v2.9.28) | No — local-only, FM-1 exception |
| buy | st_buy | Buyers (v2.9.37) | **No** — BUY-GAP-001, deferred to v3.x under FM-1 freeze |
| ord | st_ord | Order Requests (v2.9.44) | No — FM-1 category-3 exception (local-only, no sync mapping) |

**Hard architectural rule (FM-1 mitigation) — unchanged and still in force:** No new features requiring a **new Sheets sync mapping** (`FIELD_MAPS` entry, new Apps Script tab, new `syncEnt` key) on the localStorage stack. v3.0.0 Supabase migration is the exit path. The three FM-1 exceptions from the prior version of this file (UI/AI pre-fill features, new fields on existing entities, new local-only `K`/`DB` entities with no sync mapping) are still the operative carve-outs, and both Buyers and Order Requests were built under them.

**Key field name note:** unchanged — invoice number is `num` not `invNum` in records; `x.num||x.invNum` fallback still used for backward compatibility.

---

## Known gaps (selected — full log is `docs/known-gaps.md`, ~500 lines, far more detailed than this table)

| ID | Area | Summary | Status |
|----|------|---------|--------|
| TRIAL-001 / Demo Mode | Trial conversion | No demo shipment mode | **✓ Fixed v2.9.31** — `loadDemoData()` seeds a full cross-entity scenario, excluded from KPI aggregates, `[DEMO]` badges |
| MTD-001 | Tax compliance | No MTD-compatible VAT export | **✓ Fixed v2.9.32** — full HMRC VAT 100 9-box export + transaction detail CSV. Two residual sub-gaps remain open: **MTD-GAP-001** (no input VAT tracking, Boxes 4/7 always £0.00) and **MTD-GAP-002** (FX rate is export-time not invoice-date) — both accepted as operator responsibility until v3.x. A formal council-gated attempt to close MTD-GAP-001 (tracked as **REQ-RPT-001 G-07**) reached requirements-gate CONDITIONAL PASS in v2.9.33 but was explicitly deferred 2026-08-22: building input-VAT tracking against the single-file, no-server, no-real-audit-trail v2.9.x stack was judged higher-risk than closing it just ahead of the v3.0 migration, where server-side validation makes it safer to get right |
| SYNC-GAP-001 | Data integrity | `pullAll()` corrupted every pulled record's field names and silently broke delete | **✓ Fixed v2.9.47.** Residue from before the fix (phantom blank records already in operators' data) went unaddressed until **✓ v2.9.57** shipped a Settings → Data cleanup tool (REQ/SPEC-DATA-002) — read-only scan, backup-gated removal, safe renumbering, FK-integrity self-check |
| SEC-GAP-020 | Security/GDPR | Entire repo (incl. live PII) publicly served via GitHub Pages | **✓ Fully resolved 2026-07-05** — history purged, PII redacted, GDPR-assessed non-reportable |
| PO-GAP-001 | Purchase orders | Quote→PO conversion misattributed every line except the first supplier's on multi-supplier quotes | **✓ Fixed v2.9.44** (one PO per distinct supplier now). **PO-GAP-002** (open, accepted): historical POs before v2.9.44 may carry incorrect attribution, not retroactively fixable |
| QTE-GAP-001 | Quote status | Convert to PO restricted to Accepted status | ✓ Fixed v2.9.25 (carried over, unchanged) |
| SEC-GAP-001 | Security | Code.gs secrets hardcoded in source | ✓ Fixed v2.9.15/23 (Script Properties) |
| AI-GAP-006/008/009 | AI Assistant | Three of nine creatable entities lacked AI pre-fill; `create_po` couldn't resolve an unsaved supplier by name; `AI_SYSTEM_PROMPT`'s PO status vocabulary didn't match the live dropdown | **✓ All resolved v2.9.55** — Invoice/Line Item/Credit Note AI creation shipped, new `get_suppliers`/`get_buyers` PII-minimized read tools added, prompt vocabulary corrected |
| BUY-GAP-001 | Buyers | Buyers entity not synced to Sheets | Open — deferred to v3.x under FM-1 |
| CLOUD-GAP-001 | Cloud Data / Suppliers | Legacy CSV/Sheets Supplier importers bypass Cloud Data — writes silently discarded on next reload once Cloud Data is configured | Open, logged v2.9.54 |
| DATA-GAP-003 | Data integrity | Friendly ref numbers (`num`) can diverge across unsynced devices | Open, accepted as permanent localStorage-architecture trade-off |
| AI-GAP-007 | AI Assistant | Action-block emission is non-deterministic | Mitigation (`temperature:0.2`) shipped v2.9.42, confirmed effective on retest — not a full fix |
| LIB-GAP-001 | Library sync | `syncEnt('li')` not called when `invoiceRefs` mutates | Backlog, unchanged |
| SEC-GAP-003 | Security | Anthropic API key in localStorage | Accepted, inherent no-server constraint |
| SEC-GAP-004 | Security | Invoice locking is client-side UX only — not tamper-proof | Accepted by design, v3.0.0 for a real control |
| SEC-GAP-011 | Data integrity | `pullAll()` still has no timestamp-based conflict resolution — Sheets wins | Open, accepted at current operator scale |
| SDLC-GAP-003 | Staging | No same-origin PR preview environment | **Fixed — REQ-SDLC-003** (a second, dedicated preview repository, since a same-repo preview would have shared production's origin/data) |
| EVT-GAP-001 | Event log | No warning when 2,000-event cap is hit | Backlog, unchanged |
| REQ-RPT-001 G-08/G-09/G-10 | Compliance / Operational / Data Integrity | Intrastat report; supplier performance tracking; HS-code duty recalculation on existing invoices | Deferred to v3.0.x, unscoped |
| ORD-GAP-003 | Order Requests / Quotes | RFQ commit ↔ Quote drift — no back-reference, no staleness warning | **✓ Fixed v2.9.58** (REQ/SPEC-INTEG-001 Phase 1) — Quote lines now carry a source back-reference; a warning banner appears if the source RFQ commitment changes afterward |
| PO-GAP-003 | Invoices / Purchase Orders | Invoice ↔ auto-generated PO drift — edits after first save never re-sync, no warning | **✓ Fixed v2.9.58** (REQ/SPEC-INTEG-001 Phase 1) — `autoPos()`-generated POs now capture a price snapshot; a warning banner appears if the source Invoice changes afterward (price, qty, or a newly-added line). One accepted residual gap: a brand-new-supplier line with no PO at all is not flagged |

---

## Version history (v2.9.32 → v2.9.77 — everything shipped since this file was last updated)

| Version | Key changes |
|---------|------------|
| v2.9.77 | New: **Line Item unit cost/price — 3-decimal-place precision** (REQ/SPEC-LI-001) — user-requested, for high-volume/low-unit-price Line Items losing real precision under the pre-existing 2-decimal `RX.currency` cap (e.g. `$0.0125` had to be rounded to `$0.01`/`$0.02`, distorting margin/COGS at volume). `lf-c`/`lf-p` now accept up to 3 decimals via a new, narrowly-scoped `RX.currency3` regex — `RX.currency` itself and the 11 other fields sharing it (Invoice deposit/5 charges, PO deposit/FPM-funded/other-charges, buyer/supplier payment amounts) are byte-for-byte unchanged. A check-first pass found 8 separate display paths that would have silently truncated a stored 3rd decimal even after it could be entered: the Line Items Library table, its price-history sub-table, the "Import from Library" picker, the Invoice/PO/Quote PDF previews, a Quote line's own version-history panel, and the Supplier Price History table — fixed with a new `fmtN()` display helper (5 sites sharing `fmt()`, which keeps its unrelated whole-currency-unit rounding for its other 87 call sites) or an explicit `d=3` argument (5 sites using `fn()`, one of which — the Quote PDF's `fn(l.cost,0)` — was found to already be silently defaulting to 2 decimals rather than the 0 its own literal argument implied, due to `fn()`'s own `d=d||2` falsy-zero fallback). Requirements-gate: 4 rounds — round 1 found 3 blocking (a mislabeled/incomplete display-site inventory; an internal field-count error that had silently dropped one field, `pf-oth`, from the test plan); rounds 2/3 each found one further gap the prior round's fix had missed (a genuinely new 7th display site; a stale summary count); round 4 clean PASS. Spec-gate: clean PASS on the first round, every diff independently verified byte-for-byte against the live file. Implementation mutation-tested across every guard change, including a deliberate loosening of `RX.currency` itself, each reproducing exactly its predicted test failure. Build-gate: CONDITIONAL PASS, 2 non-blocking hygiene findings (a test-isolation gap in one new test, a promised-but-missing regression test), both fixed in place. 28 new tests. **803/803.** |
| v2.9.76 | New: **Cloud Data (Supabase) extended to Purchase Order** (REQ/SPEC-CLOUD-005) — Phase 2 sub-phase 3 of 3, completing Phase 2 of the cross-platform backend migration's deal-pipeline step (`docs/architecture-data-model-v1.md` §8). Purchase Order is the first entity in this series built from three different creation paths — `savePO()`, `autoPos()` (the entity's actual primary/majority path, confirmed against real usage), and `qteToPoConvert()` — rather than one save function, each gaining its own dedicated Cloud Data branch, with `autoPos()`/`qteToPoConvert()` sharing a "one call creates multiple POs, grouped by distinct Supplier" shape. Migration requires a combined precondition never needed together before: Supplier-migration-completion plus a live pre-flight `supId`-resolution check (Line-Item-style), and a genuine pre-flight duplicate-`num` scan (Quote-style, since Purchase Order's number is manually editable). Purchase Order's own id is referenced outward by an array field, `Quote.linkedPOIds[]`, requiring per-element `.map()` remapping rather than scalar rewriting — `Invoice.pos[]`/`DB.supPayments[].poId` get the same treatment but stay local-only fixes. Closes `PO-GAP-007`: `delPO()` now sweeps a deleted PO's id out of every Quote's `linkedPOIds[]`, unconditionally, regardless of Purchase Order's own migration state. Two previously-unlogged FPM-funded-deposit auto-recovery sites (inside `saveInv()` and `savePayment()`, the latter converted from a plain synchronous function to `async`) now route through a new shared `persistPOChange()` helper. Requirements-gate: 3 rounds (round 1 found 5 blocking/5 advisory, most notably that `autoPos()` was completely missing from the mutation-site inventory despite being the entity's actual primary creation path; round 2 corrected an over-generalized attribution; round 3 clean). Spec-gate: 3 rounds, the most severe of this series — round 1 **FAIL**, 10 blocking/5 advisory, most critically a literal syntax error (`await` inside a non-async `savePayment()`) that would have broken the entire single-`<script>`-block application, reproduced by the reviewer as a collapse to 17/771 passing; round 2 **FAIL**, 3 blocking — an independent reviewer applying every diff literally, not trusting round 1's own "all fixed" commit message, found the `pullAll()` exclusion fix, the `savePayment()` async signature diff, and a stale pre-existing test's title/assertion were each still genuinely unaddressed in the actual document; round 3 clean PASS, two trivial citation drifts fixed in place. Every round's fixes independently verified against a scratch copy of the real codebase with the full suite re-run, not just re-reading the SPEC's own claims. Implementation then mutation-tested across every safety-critical piece — the outward array sweep, `PO-GAP-007`, both multi-PO-creation paths, both FPM-recovery pushes, the `pullAll()` exclusion fix — each reverted mutation reproducing exactly its predicted test failure. Phase 2 (Order Request, Quote, Purchase Order) is now fully Cloud-Data-eligible; Phase 3 (Invoice, Credit Note, Shipment, Payment ledgers) remains explicitly future, unscoped work. 23 new tests. **775/775.** |
| v2.9.75 | New: **Cloud Data (Supabase) extended to Quote** (REQ/SPEC-CLOUD-004) — Phase 2 sub-phase 2 of 3 of the cross-platform backend migration's deal-pipeline step (`docs/architecture-data-model-v1.md` §8); Purchase Order (`REQ-CLOUD-005`) remains the final deferred sub-phase. A new Settings → Cloud Data card lets an operator migrate Quotes independently of Supplier/Contact/Order Request — Quote Lines (including nested price-history and source-traceability fields) stay embedded as one JSONB column, with no migration precondition and a real pre-flight duplicate-number scan (Quote's number, unlike Order Request's, is manually editable with no case-normalization anywhere). Quote is the first entity in this series whose own id is referenced *outward* by other entities — `Order Request.activeQuoteId`, `PurchaseOrder.quoteId`, `Invoice.linkedQuoteId` — so `migrateQteToSupabase()` sweeps outward via its own id-map (never via `Quote.linkedPOIds[]`, which can carry a stale entry per `PO-GAP-007`) in addition to the usual inward retrofit: the four existing sweep functions (`migrateSuppliersBuyersToSupabase()`/`migrateLineItemsToSupabase()`/`migrateContactsToSupabase()`/`migrateOrdToSupabase()`) each gained a "push the touched Quote back to Supabase if Quote has migrated" retrofit, and `migrateQteToSupabase()` itself gained the mirror-image retrofit for Order Request — five cross-phase push sites in total, the first time this series has needed both directions. Requirements-gate: 3 rounds (round 1 found the outward-sweep field list incomplete with the corresponding retrofit missing entirely, plus a self-contradictory SQL type for `dt`; rounds 2 and 3 each closed one remaining advisory). Spec-gate: 2 rounds — round 1 found that wiring the new refresh function unconditionally into `initCloudDataLayer()` broke a pre-existing Order Request test and, through it, a second one; an optional-field mapping (`originCharges`/`destCharges`/`fpmAdmin`) that assigned `undefined` inside an object literal without actually omitting the key; four new/updated tests that never reached the code they claimed to exercise (unsatisfied validation, an unset `confirm()` stub, a missing mock row); and a documentation self-contradiction about where the new tests should live — every fix verified by applying the diffs to a scratch copy of the real codebase and re-running the suite (not just re-reading the SPEC), twice, independently reverting the two riskiest fixes each round to confirm they were genuinely load-bearing; round 2 clean, PASS. 18 new tests. **752/752.** |
| v2.9.74 | New: **Cloud Data (Supabase) extended to Order Request** (REQ/SPEC-CLOUD-003) — Phase 2 sub-phase 1 of 3 of the cross-platform backend migration's deal-pipeline step (`docs/architecture-data-model-v1.md` §8); Quote (`REQ-CLOUD-004`) and Purchase Order (`REQ-CLOUD-005`) deferred as separate future sub-phases. A new Settings → Cloud Data card lets an operator migrate Order Requests independently of Supplier/Contact — Lines and nested RFQ Responses stay embedded as one JSONB column on the parent row, never promoted to child tables or independently remapped, since nothing outside the parent ever needs to resolve a nested id to a new value; only the parent's own id is swept, into `Quote.lines[].sourceOrdId`. `contact_id`/`active_quote_id` are plain nullable `text` columns (not `uuid` — an unmigrated Contact or a not-yet-Cloud-eligible Quote always carries a non-RFC-4122 local id) with no FK constraint and no migration precondition. Eleven mutation sites now share one `persistOrdChange()` helper instead of a bare `sv()`/`saveAll()` — the exhaustive inventory took three requirements-gate rounds, the last two each catching a real missed site (`saveQte()`'s Quote-conversion mutation, both of `delCon()`'s cascade branches, `executeDataCleanup()`'s renumbering step). Fixed two confirmed-live bugs found during this REQ's own research: the Supplier/Buyer sweep never rewrote RFQ Response `supId`, and neither that sweep nor the Contact sweep pushed an already-migrated Order Request's fix back to Supabase, only to its local mirror. Also corrected a stale `AI_SYSTEM_PROMPT`/user-guide Cloud Data description that had never been updated past its original Supplier/Buyer-only scope despite Line Item/Contact already shipping in REQ-CLOUD-002. Requirements-gate: 3 rounds (round 1 found the mutation-site inventory itself wrong, round 2 caught the missed data-cleanup site, round 3 clean). Spec-gate: 3 rounds, first two both FAIL — round 1: a SQL column-type bug that would reject every non-Cloud-migrated Contact/Quote reference, a new refresh function never wired into app startup, and a `saveOrd()` data-loss bug discarding caller-supplied lines/status/linked-Quote data; round 2: the required async conversion broke 12 tests in the pre-existing suite in ways neither gate had checked for, plus a second data-wipe risk in the CSV re-importer; round 3 clean. Implementation then self-caught two more defects via mutation testing that neither gate's own test list covered: a `forEach`-to-`for`-loop conversion needing every internal early-exit rewritten from a function-ending `return` to a loop-continuing `continue`, and two round-1/round-2 spec-gate fixes that had shipped with no actual regression test proving them. Also logged, not fixed: `PO-GAP-007` (`delPO()` never cleans a deleted PO's id out of `Quote.linkedPOIds[]`, deferred to `REQ-CLOUD-005`). 22 new tests. **734/734.** |
| v2.9.73 | New: **Cloud Data (Supabase) extended to Line Item and Contact** (REQ/SPEC-CLOUD-002) — Phase 1's second step of the cross-platform backend migration (`docs/architecture-data-model-v1.md` §8), following REQ-CLOUD-001's already-shipped Supplier/Buyer mechanism exactly. New Settings → Cloud Data cards let an operator independently migrate Line Items and Contacts once Suppliers have already migrated — required even for a Contact with no Supplier link, since one allowed to migrate first could later be linked to a still-local Supplier with no way for Supplier's own migration sweep to ever correct it. The precondition check queries the live, currently-connected Supabase project (not a local flag) so it holds correctly cross-device, and a further pre-flight check on each migration verifies every local foreign-key reference actually resolves against that live project before any row is inserted. Both entities' migrations do a full exhaustive external-reference sweep and 30-day archived, disconnect-safe rollback, mirroring the Supplier/Buyer pattern. Five existing UI actions that mutate Contact fields outside the normal save path (Supplier-Contact linking/unlinking, a Supplier deletion's Contact cascade, a Quote's Contact-status conversion/reversion) plus one Line Item price-history auto-recording site were also made Cloud-Data-aware — each would otherwise have silently discarded a real edit on the next Cloud refresh once its entity had migrated. Requirements-gate: 2 rounds, both CONDITIONAL PASS. Spec-gate: 4 rounds, all CONDITIONAL PASS — findings included a data-loss defect in the initial refresh-wiring design that would have wiped every existing Cloud Data adopter's local Line Item/Contact data on first reload, a Sheets-sync exclusion gap, a cross-project stale-marker gap, and the five-plus-one bypass sites above, all resolved with concrete diffs and matching tests before implementation began. Implemented, then mutation-tested across every safety-critical check — each reverted mutation reproduced exactly its predicted single test failure. Also logged, not fixed: `CLOUD-GAP-001`/`002`/`003` (a pre-existing full-backup connection-wipe bug, the Sheets push direction never excluding Cloud-sourced entities, and the CSV importer bypassing Cloud Data the same way the fixed UI sites did). 25 new tests. **712/712.** |
| v2.9.72 | Fix: **Quote-to-Purchase-Order conversion built records with the wrong field names** (REQ/SPEC-PO-002, closes `PO-GAP-005`) — surfaced during the v2.9.71 architecture review (`docs/architecture-data-model-v1.md` §6.1) as its single highest-priority finding, not an operator report. `qteToPoConvert()` built each generated Purchase Order using a different field-naming scheme than every other Purchase Order code path, at both the document level (`lines`/`dt`/`currency`/`fpm`/`rec` instead of `lineItems`/`date`/`cur`/`fpmFunded`/`fpmRecovered`) and the line-item level (`liId`/`up`/a stray per-line `cur` instead of `lid`/`cost`/no per-line currency, plus a missing `sku`). The resulting PO was created successfully but functionally invisible everywhere downstream — empty editor, $0 totals, no drift detection, wrong currency, no FPM-funding credit — on the app's primary, steered conversion path; the existing test asserted against the buggy shape, so the suite stayed green while the feature didn't work. Fixed both levels of the mismatch, and added `migrateQtePoShape()`, wired into the same 5 call sites as the existing `backfillInvoicePOs()` migration, to correct any already-broken historical PO on next load. Requirements-gate: 2 rounds, both CONDITIONAL PASS (round 1 caught the missing line-item-level fix and a wrong migration-wiring precedent, both resolved before round 2 passed). Spec-gate: CONDITIONAL PASS, advisories only — the reviewer independently applied the diff to a scratch copy and ran the real suite plus both required mutation tests, reaching 687/687. Also logged, not fixed: `PO-GAP-006`, a narrow pre-existing CSV-reimport edge case found along the way. 6 new tests. Shipped and merged via PR #124. **687/687.** |
| v2.9.71 | Fix: **CSV import corruption on multi-line quoted fields** (REQ/SPEC-DATA-003, closes `IMPORT-GAP-001`) — user reported their Suppliers tab had become "majorly corrupted" after a CSV re-import; several rows were fragments of one other supplier's multi-line Notes/address content, each with a real, sequential `SUP-####` number, invisible to the existing phantom-record scanner (which only detects a missing `id`, not a valid-id record with garbage content). Root cause: `parseImportCSV()` split the file into rows on every literal newline before any CSV quote-state tracking existed, tearing a quoted multi-line cell (exactly what Excel/Sheets export for a wrapped Notes/Address field) into multiple phantom rows. Also directly answers the user's second question — confirmed by reading the entire Apps Script sync path that Google Sheets sync is not involved; this is specifically a CSV-import-time defect. Rewrote `parseImportCSV()` as a single-pass character-stream parser with continuous quote-state tracking, plus added RFC 4180 escaped-quote support. Requirements-gate PASS (2 advisories fixed); spec-gate PASS (every AC, including escaped-quote and unterminated-quote safety, independently verified by actually running the proposed code against adversarial inputs, plus the full test suite run against the patched file; 2 advisories fixed); build-gate **PASS**, no findings — the "exactly 5 tests fail on revert" mutation-test claim verified precisely. Shared by all six CSV import types. 10 new tests, closing a pre-existing zero-coverage gap for Supplier CSV import. No retroactive cleanup — the operator had already manually removed the corrupted records and taken a backup. **681/681.** |
| v2.9.70 | New: **AI-assisted RFQ response update from a pasted supplier email** (REQ/SPEC-AI-GAP-011) — user asked, in the same conversation that produced REQ-ORD-006, whether an AI capability existed to submit a supplier's email content and have it update an RFQ response automatically; there wasn't one anywhere in the codebase. Each response row's "Compare RFQs" panel gains an envelope (✉) button, functional only with a Claude API key configured: pastes a supplier's email, one scoped AI call extracts any of cost/currency/MOQ/lead time/payment terms/notes the email explicitly states a new value for, shown as an old→new diff for Apply or Discard — never applied automatically. New `rfqParseUpdateFromEmail()` mirrors the existing single-shot semantic-check pattern (`ordCheckLineGapsSemantic()`/`conCheckEnquirySemantic()`), with an object-shape guard (`typeof [] === 'object'` would fool a naive `Array.isArray` check alone) and a whitelist filter. `rfqApplyEmailParse()` deliberately calls REQ-ORD-006's `editRfqResponse()`/`saveRfqResponse()` unmodified rather than reimplementing persistence, inheriting the id-rotation/staleness-banner mechanism automatically. Requirements-gate CONDITIONAL PASS (5 advisories, all fixed); spec-gate CONDITIONAL PASS with **1 real blocking finding**: the pending-proposal tracking state was flat module vars with no per-line scoping, but multiple Order Request lines' comparison panels can be open simultaneously — a stale Apply button from an abandoned line could silently act on a different line's proposal. Fixed by adding a `cRfqEmailParseLineId` tracking var plus two independent guards in `rfqApplyEmailParse()`. Build-gate CONDITIONAL PASS (a stray formatting change and incomplete version-ship housekeeping, both fixed here; zero findings against the feature logic itself). The cross-line fix was mutation-tested twice by two different actors: removing the primary guard doesn't corrupt data (a second guard catches it) but does silently destroy the *other* line's still-legitimate pending proposal, proving both guards are genuinely necessary. 17 new tests. **671/671.** |
| v2.9.69 | New: **Edit and delete RFQ responses** (REQ/SPEC-ORD-006) — user asked how to update a recorded RFQ response after a supplier sends revised pricing, and whether an AI capability could parse the update from a pasted email (the second question scoped separately as `REQ-AI-GAP-011`, held pending this one merging). Found by direct code read that `openRfqResponse()`/`saveRfqResponse()` only supported adding a response — no edit, no delete, anywhere. New `editRfqResponse()`/`delRfqResponse()`; `saveRfqResponse()` branches on a new `cRfqEditId` state var to replace an entry in place instead of pushing. Load-bearing design decision: editing or deleting a *committed* response deliberately rotates its `id` (edit, via a fresh `uid()`) or nulls `committedResponseId` (delete), rather than mutating the response object in place while keeping its old id — because the existing, unmodified `renderQteSourceDriftWarn()` (REQ/SPEC-INTEG-001 Phase 1) detects staleness purely via an id comparison (`ordLine.committedResponseId !== quoteLine.sourceRfqResponseId`); an in-place mutation would leave that comparison silently matching and the staleness banner would never fire for a Quote whose source pricing had, in fact, just changed. This reasoning was verified three separate times: hand-traced at requirements-gate, hand-traced with actual code extraction at spec-gate, and confirmed by mutation testing at build-gate (temporarily reverting the id-rotation reproduced exactly the missing-banner regression it exists to prevent). Requirements-gate CONDITIONAL PASS (2 advisories — a missing AC for the delete-confirm() message content, a fixture citation misattributed to the wrong prior REQ — both fixed); spec-gate PASS (3 advisories, including a real test-isolation trap: `cRfqEditId` is module-level UI state `resetDB()` doesn't reset); build-gate CONDITIONAL PASS (1 real finding — `AI_SYSTEM_PROMPT`'s existing RFQ-comparison description still claimed "no response is ever deleted," now false — fixed, along with a `docs/user-guide.md` update, in this same delivery). 13 new tests. **654/654.** |
| v2.9.68 | New: **Per-quote overhead charge overrides** (REQ/SPEC-QTE-002) — user asked why a quote's Origin/Dest Charges and FPM Admin figures couldn't be adjusted or removed; found by direct code read to be global Rate Engine defaults with no per-quote override, always read live from Settings. Three new optional fields on the Quote form let each of the three be overridden individually per quote (blank = inherit the current global default; any typed value, including `0`, overrides just that quote and stops tracking future global-default changes for that field). New `qteEffectiveOverhead()` helper mirrors the existing per-line markup override pattern exactly. Every quote saved before this version has no override set, so every existing quote's total is byte-for-byte unaffected. Also fixes a bug this change would otherwise have introduced: the PDF preview's itemized breakdown rows now read the same resolved figures the total is computed from (`cQte()`'s new `overheadBreakdown`), instead of always reading the raw global Settings value regardless of any override. Requirements-gate CONDITIONAL PASS (inverted Sheets-sync-survival claim, two citation drifts, one missing AC — all fixed in place); spec-gate CONDITIONAL PASS (one real blocking finding — a signature change that would have broken two existing tests — fixed before implementation); build-gate **PASS**, no findings, with two mutation tests independently reproduced by the reviewer. 11 new tests. **641/641.** |
| v2.9.67 | Fix: **Sheets sync performance** (REQ/SPEC-SYNC-002, closes `SYNC-GAP-002`) — user reported sync took a long time. Two compounding root causes: `syncAll()`/`pullAll()`/`pushAll()` each made 8-10 sequential Sheets HTTP requests per direction, awaited one at a time; and each of those was independently expensive, since every Apps Script handler reopened the spreadsheet from scratch. Fix (user-chosen, of two options presented): two new Apps Script actions, `bulk_upsert_all`/`pull_all`, open the spreadsheet once per request and loop over entities in-process, each wrapped in its own try/catch so one entity's failure can't discard the others' results. Client-side functions build one payload per direction and fall back transparently to the exact previous sequential behavior if Apps Script hasn't been redeployed yet — no breakage either way, just no speed win until the operator redeploys `Code.gs` manually. Acknowledged, not mitigated in code: this narrows the margin against Apps Script's 6-minute execution-time quota, judged low-risk at current data volumes, logged as a monitored risk with a concrete revisit trigger. Build-gate independent review caught a real regression before merge — a pre-existing `var pulled` inside `pullAll()`'s simple-entities loop shadowed the new `pulled()` helper function, silently breaking most simple entities on every real pull — fixed, with a mutation-tested regression test added. 10 new tests. **630/630.** |
| v2.9.66 | Fix: **Invoice→PO enumeration** (REQ-INTEG-002, sub-phase 2b of the Payment Allocation build: 2a Supplier ledger → **2b Invoice→PO enumeration** → 2c Buyer payment tranches → 2d full allocation link). Check-first found `inv.pos[]`, an Invoice field written on every save, never read anywhere — the real "which POs belong to this invoice" answer was an ad-hoc filter (`po.invId===inv.id||po.invNum===inv.num`) independently retyped at 3 call sites (`renderAccts()`'s per-invoice view, and two separate FPM-deposit auto-recovery blocks in `saveInv()`/`savePayment()`). User-chosen design: make `inv.pos[]` authoritative — new `getInvoicePOs(inv)` helper now reads it at all 3 sites; new `backfillInvoicePOs()` migration keeps it correct, wired into app boot, JSON restore, CSV import, Sheets record import, and `pullAll()`'s bulk Sheets pull (three escalating independent review rounds each found one more live PO-linking path beyond the originally-assumed sole path, `autoPos()`); `delPO()` now removes a deleted PO's id from any invoice that referenced it, closing a silent staleness gap nothing had ever noticed since nothing read the field. The final review round caught a genuine algorithm bug before implementation: the backfill's original per-invoice matching could assign one PO to two invoices at once when its `invId`/`invNum` disagreed (exactly what a Sheets re-link produces) — fixed by resolving each PO to a single home invoice, preferring `invNum` (the field a real re-link actually changes) over `invId`. Logged, not fixed: `PO-GAP-004`, a narrow pre-existing `savePO()` edge case. Went through 4 REQ/SPEC revisions across requirements-gate and spec-gate — every finding was a missed real-world code path or a genuine algorithm defect, none a citation error. 11 new tests, plus 3 pre-existing tests fixed. **620/620.** |
| v2.9.65 | Fix: **Close `ACCT-GAP-001`/`AI-GAP-010`** (REQ-CUR-002) — the two currency-mixing gaps logged, not fixed, alongside v2.9.64's totals-bar fix, same defect class, user-prioritized as the next fix once that shipped. `renderAccts()`'s per-invoice (Sup. Dep. Paid, FPM Funded, Sup. Bal. Due, Total to Chase) and per-supplier (Total COGS, Dep. Paid, Bal. Due to Sup.) sums now convert each linked PO's figure via `toDisp()` before summing, instead of raw-summing across POs that may span multiple currencies — check-first found the original `ACCT-GAP-001` entry under-scoped (`fpmFunded`/`totalToChase`/`totalCOGS` weren't named), fixed together regardless. Invoice-native columns (Invoice Total, Buyer Dep. Rec., Buyer Bal. Due) unchanged. `_aiExecTool('get_kpis')` gets the identical fix for `invoiceRevenue`/`netProfit`/`outstanding`/`poBalanceDue`, plus a new `currency` field in its JSON response so the AI assistant always has an explicit unit. Dep. Coverage % and `avgMargin` needed no code change — proven algebraically invariant to the display-currency choice, since both are ratios of two sums over the same record set, both converted identically. Full requirements-gate → spec-gate → build-gate cycle; spec-gate caught a pre-existing test that would have broken once `get_kpis` started converting currencies, fixed by pinning its display currency to match its own PO's currency before implementation. 5 new tests. **609/609.** |
| v2.9.64 | Fix: 4 real issues found in manual (Cowork-driven) testing of v2.9.63's `PO.dep` reconciliation fix (REQ-INTEG-002-2a-fix-2). **Critical:** `renderAccts()`'s Accounts totals bar summed raw amounts across POs/Invoices of different currencies with zero conversion, mislabeling the garbage total as a single `$` figure — fixed via the same `toDisp()`-before-summing pattern already correct in `rDash()`'s KPI tiles; also caught the identical flaw in the buyer-side total (untested originally — all seeded invoices shared one currency) and the FPM Exposure tile in the same block. Full requirements-gate → spec-gate cycle (spec-gate's test plan needed 2 blocking corrections: an unworkable raw-float assertion against `fmt()`'s rounded output, and a regression-guard test whose premise was false under the app's actual default display currency, GBP not USD). **Also fixed:** the PO list not re-rendering after a Supplier Payment is recorded/deleted (`rPO()`/`rDash()` were missing, unlike the buyer-side precedent); raw unrounded floats in the Edit PO deposit field (display-only fix); a barely-visible read-only state for that field once ledger records exist (caught and fixed a self-inflicted bug along the way — `vClrAll()` was silently wiping the new styling). Demo-data fix: `PO-DINV-0005-1` now seeds a real $13,225 legacy deposit instead of $0, making its intended backward-compat-fallback scenario actually testable. Logged (not fixed) two related pre-existing gaps: `ACCT-GAP-001`, `AI-GAP-010`. 10 new tests. **604/604.** |
| v2.9.63 | Fix: **Reconcile `PO.dep` display with the Supplier Payment ledger** (REQ-INTEG-002-2a-fix, a fix-forward against v2.9.62's Sub-phase 2a). Real-world testing found recording a payment through the new ledger had zero visible effect anywhere — 10 separate read sites across `editPO()`, `rPO()`, `prevPODoc()`, `rDash()` (KPIs and the PO Commitments chart), `renderAccts()` (per-invoice, per-supplier, totals bar), and `_aiExecTool()` all read the raw, disconnected `PO.dep` field directly. New `getPOEffectiveDep(po)` is now the single point of truth for all 10: once a PO has linked Supplier Payment records, its Deposit Paid figure is reconciled — same-currency payments summed as exact raw amounts, cross-currency payments converted via that record's own historically-locked FX rate (`lockFxRate()` now snapshots the full static rate table at save time, not just the one rate it applied, so this second conversion leg is genuinely historical too, not derived from today's live rate). A PO with no ledger records, or a currency this mechanism can't yet safely reconcile (EUR — logged as `PROC-GAP-002`, a pre-existing gap in the shared FX mechanism, not something this fix introduces), keeps showing its manually-entered figure unchanged, with an explanatory note. The "Supplier Deposit Paid" field becomes read-only once ledger records exist, avoiding two ways to change the same number. Went through 2 independent review rounds each for requirements-gate and spec-gate; round-1s each caught a real blocking defect (a missed 10th read site, and a EUR-currency PO that would have shown a silently wrong, mislabeled figure on its own PDF — worse than today's un-reconciled-but-honest behavior; and a read-only/note state that could leak from an edited PO into a freshly-opened new one, confirmed real by tracing the actual "new PO" trigger function). 19 new tests. **594/594.** |
| v2.9.62 | New: **Supplier Payment ledger, sub-phase 2a of a larger Payment Allocation build** (REQ/SPEC-INTEG-002: 2a Supplier ledger → 2b Invoice→PO enumeration fix → 2c Buyer payment tranches → 2d full allocation link). Every payment FPM makes to a supplier can now be recorded individually against its PO — date, amount, native currency, purpose (Deposit/Balance/Other), method, reference, notes — via a new "$" action on the Purchase Orders tab, replacing `PO.dep`/`PO.fpmFunded`'s single-overwrite fields with no history. Each payment locks in a GBP-equivalent snapshot (`lockFxRate()`) at save time via the existing `toGBP()` mechanism, so historical totals stay accurate even after FX rates change later. New `getPOTotalPaid()` sums a PO's payments correctly across multiple native currencies by summing locked GBP-equivalents, not raw amounts. Standalone ledger — does not read/write the existing PO fields, no link to buyer-side payments yet (later sub-phases). Went through 2 independent review rounds each for requirements-gate and spec-gate; round-1s each caught a real blocking defect (a false claim that buyer-side Payments is unsynced, which would have wrongly justified an inert field-map entry for the new entity — corrected to follow the genuine no-sync precedent, Order Requests/Buyers; and a missing backup/restore wiring that would have made the new ledger's only backup, the local JSON export, silently drop every record on restore). 12 new tests. **575/575.** |
| v2.9.61 | Expanded Demo Mode (`loadDemoData()`) with 7 new scenarios so REQ-INTEG-001 Phase 1/2 can actually be exercised locally instead of against production data (interim mitigation for `SDLC-GAP-003`, no staging environment) — not a REQ/SPEC-gated feature, test/demo-data tooling only. Added: an Order Request (DORD-0001) with 2 RFQ responses from different suppliers for the comparison panel; a Quote (DQ-0002) converted from it whose source RFQ response was re-committed afterward, reproducing the Phase 1 Quote staleness banner; an unapproved Pro-forma invoice (DINV-0002); an approved, non-stale invoice (DINV-0003) for the clean "Progress to Invoicing" path; an invoice (DINV-0004) that was approved then had a line edited, showing Phase 2's automatic approval-clearing already having fired (both events present in the log); and an invoice/PO pair (DINV-0005/DPO-0002) reproducing the Phase 1 PO staleness banner. One scenario from the original brief — an illegal Order-Request-stage auto-advance triggered by "Progress to Invoicing" — was not built: check-first confirmed Progress to Invoicing never touches Order Request stage in the shipped Phase 2 design (a confirmed decision from that build, not an oversight), so there is no such transition to exercise. Also fixed a real pre-existing gap surfaced while doing this: `loadDemoData()`/`clearDemoData()` never included Order Requests or the Line Item catalogue at all — both are now covered by the idempotency check and cleanup. 6 new tests. **563/563.** |
| v2.9.60 | New: **Buyer-approval capture on Invoice, Phase 2 of 4** (REQ/SPEC-INTEG-001 Phase 2) — while an invoice is at Pro-forma status, "Mark Buyer Approved" records `buyerApprovedAt`/`buyerApprovedBy`/`approvalMethod` (closed-set: Email, WhatsApp, WeChat, Phone / Verbal, Other)/`approvalNote`, confirming the buyer's out-of-band approval. Editing any line item (price, quantity, or an added/removed line) after approval automatically clears all four fields, forcing re-approval — header-only edits never clear it. "Progress to Invoicing," available once approved, never changes status; it only logs the action and optionally records a manual, non-binding Quote link (`linkedQuoteId`/`linkedQuoteNum`) for audit trail — there is no automatic Quote→Invoice path in the codebase, so this is a deliberate manual selection. Both new actions bypass `saveInv()`'s form-rebuild (direct record mutation) so unrelated in-progress edits in the main modal can't interfere. Also extended the `pos`-style field-preservation pattern to both CSV-import code paths, which otherwise would have silently dropped an already-recorded approval on re-import. Went through 2 independent review rounds each for requirements-gate and spec-gate; both round-1s caught real blocking defects (a false "reuses an existing mechanism" claim for the line-comparison logic; a real, demonstrated approval-loss path via CSV re-import), both fixed and confirmed in round 2. One further build-time bug (a rid-matching fallback that never actually matched) found via test-writing and fixed before merge. 19 new tests. **557/557.** |
| v2.9.59 | Removed: Invoice's schema-only `sourceQuoteId` field, added in v2.9.58 as forward-compatibility for a possible future Quote-aware Invoice-creation path. Confirmed with the business post-merge: direct Quote→Invoice traceability is not needed — the real workflow is always Quote → PO → Invoice, and that chain is already fully covered by two pre-existing mechanisms, `qteToPoConvert()` (`quoteId`/`quoteNum` on PO) and `autoPos()` (`invId`/`invNum` on PO). Everything else from Phase 1 (Quote-line source back-references, both staleness-warning banners) is unaffected. 1 test removed. **538/538.** |
| v2.9.58 | New: **Order Request/RFQ → Quote → Invoice referential integrity, Phase 1 of 4** (REQ/SPEC-INTEG-001) — traceability and read-only staleness detection only, closing `ORD-GAP-003` and `PO-GAP-003`. A Quote line created from a committed RFQ response now carries a back-reference to the exact Order Request line/response it came from; a warning banner appears if a *different* response is later committed. An `autoPos()`-generated PO now captures the invoice's unit price at generation time; a warning banner appears if the invoice's price, quantity, or line items (including a newly-added line for the same supplier) change afterward. Neither banner auto-fixes anything — detection and a visible warning only. Invoice also gains a schema-only `sourceQuoteId` field with no automatic population (no Quote→Invoice conversion path exists in the codebase to populate it from). Went through 2 independent review rounds each for requirements-gate and spec-gate; both round-1s caught a real blocking defect (the original PO check could only catch 2 of 3 named drift types; the price-comparison mechanism would have permanently false-flagged every pre-existing PO), both fixed and confirmed in round 2. 18 new tests. **539/539.** Build-gate independent review PASS, no findings. |
| v2.9.57 | New: **Data Integrity Cleanup** (REQ/SPEC-DATA-002) — Settings → Data "Scan for phantom records": read-only scan → mandatory backup gate → removal of blank/corrupted records (residue of SYNC-GAP-001, predating the v2.9.47 fix) → sequential renumbering of Supplier/Line Item/Buyer/Contact/Order Request numbers only (Invoice/PO/Quote/Credit Note numbers permanently excluded — real lookup keys elsewhere) → post-cleanup FK-integrity self-check. Also hardens `pullAll()` so a live recurrence is dropped at the source, not just cleaned up after the fact. 15 new tests. **521/521.** |
| v2.9.56 | New: **RFQ supplier comparison & commit** (REQ/SPEC-QTE-001 Part B) — each Order Request line gets a "Compare RFQs" panel recording multiple suppliers' quoted responses, ranked by landed cost converted to a common currency; exactly one response can be committed per line, and committing feeds a Quote hand-off with correct currency conversion. Fix: closes the last open item from REQ-V3-GAP-006 (Contact link/unlink event logging). 14 new tests. 506/506. |
| v2.9.55 | New: **AI Assistant** gains Invoice/Line Item/Credit Note creation plus PII-minimized Supplier/Buyer read tools (REQ/SPEC-AI-GAP-002) — closes AI-GAP-006/008/009. New: **AI-assisted enquiry intake check** (REQ/SPEC-CON-004) — flags ambiguous Contact enquiry text via a single-shot Anthropic call, PII-scoped payload. 23 new tests. 492/492. |
| v2.9.54 | New: **Cloud Data** — first Supabase-backed layer, Suppliers/Buyers only (REQ/SPEC-CLOUD-001). One shared Postgres table pair behind RLS, not multi-tenant (see Architecture above). Backup-gated one-time migration with automatic reference remapping across Quotes/POs/Line Items/Contacts/Invoices; 30-day local rollback archive. Went through 4 independent requirements-gate and 4 independent spec-gate rounds before build. 19 new tests. 473/473. |
| v2.9.53 | New: **Supplier price intelligence** (REQ/SPEC-SUP-001) — Price History panel aggregating every recorded price point for a Supplier across Line Items/Quotes/POs, with staleness flagging. 10 new tests. 454/454. |
| v2.9.52 | New: **Per-line quote margin override** (REQ/SPEC-QTE-001 Part A) — each Quote line can override the quote-level margin; overhead charges no longer marked up (deliberate behavior change, recomputes existing quotes' Grand Total). 12 new tests. 434/434. |
| v2.9.51 | New: Contacts CSV upload (Import Data tab, 6th step) — reuses existing Sheets-pull dedup/mapping logic. 10 new tests. **422/422.** |
| v2.9.50 | New: Order Request per-line gap check — structural (instant) + AI-assisted semantic check, manual trigger, nothing persisted. 11 new tests. 412/412. |
| v2.9.49 | Fix: invoice quick-add COGS warning fired on legitimate zero-margin pass-through lines. 6 new tests. 401/401. |
| v2.9.48 | New: Order Request CSV import; Quote approval audit trail (Approved By/Note). 11 new tests. 395/395. |
| v2.9.47 | Fix: **SYNC-GAP-001** — `pullAll()` field-mapping/identity loss, closed with `unmapRec()`/business-key matching. 24 new tests. 384/384. |
| v2.9.46 | New: Global display-currency toggle (GBP/USD/RMB/BBD) on Dashboard/Buyers. 9 new tests. 372/372. |
| v2.9.45 | New: Order Request line items, append-only `lineUpdates[]` provenance log. 13 new tests. 363/363. |
| v2.9.44 | Fix: **PO-GAP-001** — one PO per supplier on multi-supplier Quote conversion. New: Order Management (`DB.ord`, full Orders tab + stage state machine). 27 new tests. 349/349. |
| v2.9.43 | New: Friendly reference numbers (SUP-/LI-/BUY-/CON-####). New: Excel/Power Query reporting pipeline (docs only). 19 new tests. 322/322. |
| v2.9.42 | Fix: AI action-block reliability — `temperature:0.2` added after live UAT found 3/6 create-flows silently falling back to describing manual steps. 303/303. |
| v2.9.41 | New: AI-assisted Supplier and Buyer creation via `handleAIAction()`. 303/303. |
| v2.9.40 | Fix: Pro-forma invoice preview/PDF rendered as a plain Invoice. 301/301. |
| v2.9.39 | **Security: SEC-GAP-020 PII purge** — live dataset and contact PII removed from the public repo; `.gitignore` hardened. 298/298. |
| v2.9.38 | Security: logo-URI sanitisation, sync token stripped from backup export, sync-token header attempt reverted (Apps Script can't read custom headers). 298/298. |
| v2.9.37 | New: Buyers entity (`DB.buy`), ad-hoc buyer seed, invoice Buyer field converted to closed-set dropdown. 298/298. |
| v2.9.36 | New: Full entity event log coverage (Invoice/PO/Payment/Supplier); invoice edit delta/audit trail. 289/289. |
| v2.9.35 | New: Aging Report (DSO, overdue buckets) and P&L Report, both with CSV export. 280/280. |
| v2.9.34 | New: AI date filters on `get_invoices`/`get_payments`; quick-add COGS warning (amber banner). 270/270. |
| v2.9.33 | New: AI live data tools — `get_invoices`/`get_payments`/`get_kpis`/`get_pos` via Anthropic tool use. 263/263. |
| v2.9.32 | New: **MTD-compatible VAT Return export** (9-box HMRC VAT 100 + transaction detail CSV). 11 new tests. 263/263. *(This file was last updated at this version — everything above is new since.)* |

*Full history back to v2.8.1 is in `docs/version-history.md`.*

---

## Build queue

**Completed since this file was last accurate:** all seventeen versions v2.9.52–v2.9.68 shipped and merged to `main` via PR. v2.9.52–v2.9.58, v2.9.60, v2.9.62, v2.9.63, v2.9.64, v2.9.65, v2.9.66, v2.9.67, and v2.9.68 each went through the full requirements-gate → spec-gate → build → independent build-gate review pipeline documented in `docs/requirements-tracker.md` (v2.9.63's requirements-gate and spec-gate, and v2.9.64's spec-gate, each needed one revision round after an initial FAIL/CONDITIONAL PASS before reaching PASS; v2.9.65's spec-gate caught one blocking issue, fixed in the spec before implementation, no second round needed; v2.9.66 needed 4 REQ/SPEC revisions — three independent review rounds each found a genuine missed real-world PO-linking code path, and a fourth found a real algorithm bug in the fix's own migration, all fixed before implementation; v2.9.67's spec-gate PASS had 4 advisory-only findings fixed in place, but its first build-gate round returned FAIL on a real regression — a name-shadowing bug in `pullAll()` — fixed, with a second confirmatory build-gate round returning PASS, evidenced by mutation testing: the reviewer reintroduced the exact bug by hand, confirmed the new regression test failed with precisely the expected symptom, then reverted and reconfirmed 630/630 clean); v2.9.68's requirements-gate CONDITIONAL PASS had 3 advisory findings fixed in place (no blocking), its spec-gate CONDITIONAL PASS had 1 real blocking finding fixed before implementation (a signature change that would have broken two existing tests), and its build-gate returned PASS with no findings, the reviewer independently reproducing both of the commit's mutation-test claims; v2.9.59 was a small, low-risk post-merge cleanup (removing one confirmed-dead schema field) verified by check-first and a full test-suite run rather than a full gate cycle. v2.9.61 is test/demo-data tooling, not a REQ/SPEC-gated feature — standard build-gate review only (independent review PASS), no `docs/requirements-tracker.md` entry, per its own brief.

**Stale unmerged branches — still present on the remote, now confirmed superseded, worth deleting in a cleanup pass:**

| Branch | Status |
|--------|--------|
| `claude/con-003-id-backfill` | Superseded — Contact id backfill (`backfillConIds()`) shipped on `main` under REQ/SPEC-CON-003 |
| `claude/doc-001-user-guide` | Superseded — `docs/user-guide.md` has since been fully rewritten and shipped on `main` directly |
| `claude/inv-cogs-warning-fix` | Superseded — the invoice quick-add COGS warning fix shipped as v2.9.49 |
| `claude/ord-005-gap-detection` | Superseded — Order Request per-line gap detection shipped as v2.9.50 |

**Unmerged branches not verified this pass** (`claude/ord-followup-fixes`, `claude/sync-race-condition-fixes-*`, `claude/triage-tool-page`) — still exist on the remote, content not checked against what's since shipped on `main`. A full branch cleanup/triage session (delete confirmed-superseded branches, check the rest for unique unshipped work) remains outstanding.

**Backlog carried forward from `docs/known-gaps.md` / `docs/requirements-tracker.md` (business-priority items, still open), roughly in priority order:**

| Item | Notes |
|------|-------|
| SUP-DATA-001 (not a gap — live-data cleanup, explicit prerequisite) | **User-flagged prerequisite for the cross-platform/multi-device shared-backend initiative about to be scoped** — do the live Suppliers-tab cleanup *before or as part of* that migration, not after: migrating already-duplicated/fragmented Supplier records into a shared backend would propagate the corruption to every device instead of fixing it once. Root cause already fixed in code (`IMPORT-GAP-001`/`REQ-DATA-003`, v2.9.71) — this item is the one-time manual cleanup of records that predate the fix, still sitting in the operator's live browser-local data. Exact list identified and cross-validated against the operator's source spreadsheet (every fragment traced to the specific multi-line Notes field it came from, with zero unexplained remainders): 15 records to delete — `SUP-0023/24/25/26/27/28` (fragments of Anhui HYY/Bokni/Zhengzhou Rongchang/Xingtai Xingcha/Zhongshan Chuhui/Changzhou Weighing's multi-line Notes), `SUP-0032` (a duplicate of `SUP-0012`/Joylife, created because a re-import's name had a trailing-space/character mismatch that failed the exact-match lookup), `SUP-0033/34/35/36/37` (fragments of the same Joylife re-import's Address/Mobile/Email/Whatsapp/Web lines), and `SUP-0040/41/42` (fragments of Lianyungang Yuede's Address/Tel lines). A ready-to-run browser-console preview + delete script (using the app's real `delSup()` — reference-checked, audit-logged, Cloud-Data-aware, not raw array surgery) was handed to the operator but not yet run as of this entry — they were at the end of a long day and deferred it, explicitly asking to be reminded rather than losing track of it. **Recommended full sequence when picked back up:** (1) run the delete script for the 15 records above; (2) re-import the operator's clean source spreadsheet via Settings → Import Data → Suppliers (now safe with the `parseImportCSV()` fix) to restore the correct, complete multi-line Notes on `SUP-0013` through `SUP-0020` and `SUP-0039`, which currently have their Notes truncated to just the first line by the same historical bug — watch for the Joylife name-mismatch creating a second duplicate on this re-import, and resolve manually if it does. Separately surfaced by the same investigation, not yet acted on: the operator is on `localStorage`-only data with no shared backend, so Safari and Chrome (and different devices) already show materially different data — this is the actual motivating problem behind the cross-platform initiative about to be scoped, not a separate concern. |
| SYNC-GAP-003 | Whether the batched sync fix (v2.9.67) is actually live in production is unconfirmed — a post-redeploy Network-tab check showed a pattern consistent with the old per-entity fallback still running (possibly because the Apps Script deployment's live version was never re-pointed at the redeployed code — a known Apps Script gotcha), but a second, meant-to-be-fresh capture came back identical to the first and couldn't confirm either way. Moved to backlog rather than actively pursued, per instruction — no regression risk either way, since the fallback design means sync works correctly under both hypotheses, just at different speeds. See `docs/known-gaps.md` for exactly what a fresh Network-tab capture would need to show to resolve it. |
| REQ-INTEG-002 (Sub-phase 2d) | Not a gap — the still-unscoped, still-unbriefed final step of the Payment Allocation build (2a Supplier ledger → 2b Invoice→PO enumeration fix → 2c Buyer payment tranches → **2d full allocation link**). 2a shipped v2.9.62, stabilized over three fix-forward rounds (v2.9.63, v2.9.64, v2.9.65). 2b shipped v2.9.66 — `inv.pos[]` is now the authoritative Invoice→PO link. **2c shipped v2.9.78** (`REQ/SPEC-INTEG-002-2c`, 11 requirements-gate rounds — a `purpose`/`currency`/`rateLock` parity build against the Supplier Payment ledger, plus a hardened `getInvPayments(inv)` matching rule). 2d has no REQ file and no known scope/timeline as of this update. |
| Cross-platform backend migration — Phase 3 | Phase 2 of the deal pipeline (Order Request → Quote → Purchase Order, `REQ-CLOUD-003/004/005`) completed v2.9.76 — every deal-pipeline and master-data entity was Cloud-Data-eligible as of that version. Phase 3 (Invoice, Credit Note, Shipment, and the Payment ledgers) is now underway, split into three sub-phases mirroring Phase 2's own precedent: sub-phase 1 (Invoice, Credit Note) **shipped v2.9.79, `REQ/SPEC-CLOUD-006`**; sub-phase 2 (Shipment, `REQ-CLOUD-007`) and sub-phase 3 (Buyer Payment/Supplier Payment, `REQ-CLOUD-008`) are both currently in progress. |
| CUR-GAP-001 / CUR-GAP-002 | Same currency-mixing defect class as the now-fixed `ACCT-GAP-001`/`AI-GAP-010` (v2.9.65), still open in the Aging Report (`renderAgingReport()`) and Buyer Statement (`renderStatement()`/`openStatement()`). Logged v2.9.46, deliberately not folded into v2.9.65's fix to keep it scoped to what the user actually prioritized. |
| MTD-GAP-001/002 (REQ-RPT-001 G-07) | Input VAT tracking, invoice-date FX — a real, recurring VAT-reclaim under-claim. Requirements-gate reached CONDITIONAL PASS in v2.9.33 but explicitly deferred to v3.0.x on 2026-08-22 rather than building it against the pre-Supabase stack — the most consequential item in "deferred," not "someday." |
| ORD-GAP-004 | Order Request stage doesn't auto-advance on "Progress to Invoicing" — logged as backlog after REQ-INTEG-001 Phase 2 fact-finding confirmed this was a drafting scope-narrowing decision, not a bug; a previously-infeasible path (Invoice → Quote → `sourceOrdId` → Order Request) may now be viable since Phase 1 shipped that back-reference, but feasibility has never been investigated. |
| REQ-RPT-001 G-08/09/10 | Intrastat report, supplier performance tracking, HS-code duty recalculation — all deferred to v3.0.x, unscoped |
| PROC-GAP-002 | EUR has no FX conversion path anywhere in `toGBP()`/`fromGBP()` — only matters if EUR transactions actually happen. Logged v2.9.64 (found while building the `PO.dep` reconciliation fix, which explicitly guards against it rather than fixing it). |
| REQ-SDLC-003 setup (not a gap) | The PR preview environment shipped code-complete, but needs 4 one-time manual steps before it actually works: create `stkdcfpm/stackd-ops-preview` (public, auto-init) and grant this session access, a scoped deploy PAT, that PAT as a `stkdcfpm/stackd-ops` secret (`PREVIEW_DEPLOY_TOKEN`), and enabling GitHub Pages on the new repo. See `docs/REQ-SDLC-003-v1.md`/`docs/SPEC-SDLC-003-v2.md` §3/§5. Until done, the two new workflows fail harmlessly with zero effect on `main`/`qa.yml`/production. |
| PO-GAP-004 | `savePO()` can silently clear a PO's `invId` if its linked invoice's `num` is later renamed (a `pf-inv` field re-derivation quirk). Low likelihood — invoice numbers aren't expected to be edited routinely. Logged v2.9.66 (found during the Invoice→PO enumeration fix's check-first). |
| BUY-GAP-001 | Buyers → Sheets sync — deferred to v3.x (FM-1) |
| DASH-GAP-001 | Dashboard charts are hand-rolled divs, no interactivity — Chart.js vendoring recommended if picked up |
| CLOUD-GAP-001 | Legacy CSV/Sheets Supplier importers bypass Cloud Data once configured |
| AI-GAP-001 (broad) | Agentic multi-step order flow — deferred to v3.0.x, requires a server-side proxy |

**v3.0.0 (still the stated target, no code-verifiable date):** Supabase backend, multi-tenancy, MFA, RBAC, server-side API proxy, referral mechanics. Prerequisite: freelance data architect engagement. See `docs/v3-architect-handoff.md` for the consolidated technical/business handoff packet assembled for that engagement.

---

## Active risks

*(Updated against what's actually shipped; unresolved items unchanged from the prior version of this file.)*

| ID | Risk | Status | Notes |
|----|------|--------|--------|
| R-001 | localStorage GDPR exposure | ACTIVE | v3.0.0 resolves. SEC-GAP-020 (public-repo PII exposure, a related but distinct risk) is now closed. |
| R-003 | Trial conversion too low | **Mitigation shipped** | Demo shipment mode shipped v2.9.31 — whether it actually moved the conversion number is a business-side question this repo can't answer |
| R-004 | MTD compliance gap | **Mitigation shipped** | VAT Return export shipped v2.9.32; MTD-GAP-001/002 remain as documented residual gaps, not full closure |
| R-007 | Supplier intelligence empty database | Unverifiable from repo | Data lives in operator's localStorage |
| R-002 | Alibaba/platform competition | MONITOR | Business-side, unchanged |
| R-005 | Corridor concentration risk | MONITOR | Business-side, unchanged |
| R-006 | AI moat erosion | MONITOR | Business-side, unchanged |

---

## Legal and IP

*(Unverifiable from this repo — carried forward unchanged from the prior version of this file. Confirm current status directly with Companies House / UKIPO / ICO rather than trusting this table.)*

| Item | Status (last known) | Notes |
|------|--------|-------|
| FPM International Ltd | Registered May 2026 | Companies House. SIC 46190 + 62012 |
| STACKD trade mark | Filed May 2026 | UKIPO Classes 35 + 42. £220. 4-6 months to grant |
| EUIPO trade mark | Backlog | Apply after UK grant |
| ICO registration | Backlog | Required before first external client |
| T&Cs + Privacy Policy | Backlog | Before v3.0.0 launch |
| getstackdops.com / app.getstackdops.com | Live | GitHub Pages, confirmed via `CNAME` in repo |

---

## Pricing

*(Business-side, unverifiable from this repo — carried forward unchanged.)*

| Tier | Price | Users | Key threshold |
|------|-------|-------|---------------|
| Starter | £49/mo | 1 | Up to 5 active shipments |
| Growth | £149/mo | Up to 3 | Unlimited shipments + API tracking |
| Scale | £399/mo | Up to 10 | Supplier portal + MTD |
| Enterprise | Custom | Unlimited | Never shown on pricing page |

Enterprise base: £1,200/month + £25/user beyond 10 + integration fees.

---

## Google Sheets tracker IDs

| Tracker | Sheet ID |
|---------|----------|
| Requirements Tracker | stored in Apps Script → Script Properties as `REQUIREMENTS_TRACKER_ID` |
| Project Tracker | stored in Apps Script → Script Properties as `PROJECT_TRACKER_ID` |

Apps Script write bridge live: actions `update_requirements_tracker` and `update_project_tracker`. Sync now routes through a Cloudflare Worker CORS proxy in front of Apps Script (`cloudflare-worker/worker.js`) — confirmed present in repo, not documented in the prior version of this file.

---

## Key contacts

**PII REDACTED (2026-07-04, reaffirmed under SEC-GAP-020's 2026-07-05 full resolution).** Supplier contact names, emails, and phone numbers were removed from this file because this repository is publicly served via GitHub Pages. Contact details live in the Stackd Ops portal (Suppliers entity) — the portal's localStorage is the canonical store. Operational notes retained below without personal identifiers:

| Company | Products | Notes |
|---------|----------|-------|
| Shandong Jinbao | PVC Foam Board | Fire cert required each shipment |
| Anhui HYY | Onion Mesh Bags | — |
| Shanghai Bokni | Centrifugal Juicers | Verify 220V |
| Zhengzhou Rongchang | Sugar Cane Juicer | — |
| Xingtai Xingcha | Pallet Jacks | — |
| Fuzhou Bote | Freezers/Chillers/Cold Storage | 30% dep/70% BL. 45 day lead. CE cert |
| Zhongshan Chuhui | Solar LED Floodlights | — |
| Changzhou Intelligent Weighing | Platform Scales | — |
| Amazon Business (Reolink) | Security Cameras | amazon.com/business. US tax in COGS. Zero margin |

---

## How to use this file

**For Claude Code:** Read at start of every session. Update at end of every version delivery — bump version, test count, update build queue, add to version history, update known gaps. **This file drifted three months out of date last time (frozen at v2.9.32 while code moved to v2.9.51) — follow the "On version delivery" checklist in `CLAUDE.md` every single release, no exceptions.**

**For Claude Projects:** Fetch at start of new conversation thread.

**For new Claude conversations:** Paste this URL into your first message: `https://raw.githubusercontent.com/stkdcfpm/stackd-ops/main/STACKD_CONTEXT.md`

**Update protocol:**
1. After every version delivery — Claude Code updates version, tests, build queue, known gaps, and the Backlog carried forward table (add new gaps found this delivery, remove/mark shipped ones, add the next explicit step of any in-flight multi-phase initiative). See the "On version delivery" checklist in `CLAUDE.md`.
2. After every strategy session — update sprint status, risk register, decisions.
3. After every China trip / business-development meeting — update contacts, agent pipeline, trip outcomes.
4. Monthly — update regulatory calendar status, competitive scan notes.
5. **When re-verifying this file, distinguish code-verifiable facts (version, tests, entities, gaps — check against `main` directly) from business-side facts (legal, pricing, contacts) that this repo cannot confirm or deny.**

---

*STACKD · Source · Supply · Ship · FPM International Ltd · getstackdops.com*
*Living document — last updated 30 August 2026*
