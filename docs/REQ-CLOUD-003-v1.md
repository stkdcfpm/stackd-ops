# REQ-CLOUD-003 — Order Request Cloud Data migration (Phase 2, sub-phase 1 of 3)

**Status:** v1 — requirements-gate round 1: CONDITIONAL PASS (3 blocking, 3 advisories), fixed. Round 2: CONDITIONAL PASS (1 new blocking, 1 new advisory), fixed. Round 3: PASS, no new findings. See §8. Ready for SPEC.

---

## 0. Scoping decision: Phase 2 split into three sequential sub-REQs, not one

`docs/architecture-data-model-v1.md` §8's sequencing step 4 groups Order Request, Quote, and Purchase Order into "the same delivery." Having now researched the real current code in depth (see §1), that combined scope is materially larger than either Phase 0 (`REQ-CLOUD-001`, Supplier+Buyer) or Phase 1 (`REQ-CLOUD-002`, Line Item+Contact) — both of which needed multiple spec-gate rounds on a single entity pair. Order Request alone has a nested three-level structure (`ord.lines[].rfqResponses[]`), roughly eleven distinct mutation call sites bypassing any single choke point, and a confirmed pre-existing cross-entity bug in the already-shipped Supplier migration sweep. Combining all three into one REQ/SPEC/PR risks an unreviewable mega-change and a build-gate pass that can't meaningfully verify it end to end.

**Decision: split Phase 2 into three sequential sub-REQs, following the same incremental pattern `REQ-CLOUD-001`→`REQ-CLOUD-002` already established** — each gated, implemented, and merged before the next starts:

1. **`REQ-CLOUD-003` (this document) — Order Request**, first because it has zero existing Sheets-sync footprint (confirmed below) — migrating it to Supabase is a strict win with no coexistence question to resolve, unlike Quote/PO.
2. **`REQ-CLOUD-004` (future) — Quote**, including closing the Quote-specific logging/sync gaps `docs/architecture-data-model-v1.md` §8 calls out.
3. **`REQ-CLOUD-005` (future) — Purchase Order**, including closing `docs/architecture-data-model-v1.md` §6.4 (`autoPos()`/`qteToPoConvert()` never call `logEv()`) and a newly-found `delPO()` gap (§1.6 below).

This still satisfies §8's intent — all three entities get migrated, and the logging/sync gaps get fixed — just as three reviewable increments rather than one. Each sub-REQ's own migration-completion marker (`st_ord_cloud_migration_ts` here) becomes a precondition input for the ones after it wherever a real cross-entity reference requires it (see §1.5).

---

## 1. Business context

### 1.1 Why Order Request first, and why it's simpler than it looks

Order Request (`DB.ord`) has **zero Sheets-sync footprint today** — verified absent from `FIELD_MAPS` (`index.html:4278-4288`), from `syncAll()`/`pushAll()`'s entity arrays (`index.html:4341`, `4570`), and from `pullAll()` (neither `_allPullKeys` nor any hand-written block references `ord`). This was a deliberate `REQ-ORD-001` decision ("FM-1 category-3 precedent"), not an oversight. `SEC-GAP-011` (`pullAll()`'s no-conflict-resolution risk, the architecture doc's own stated driver for this whole initiative) **does not even apply to Order Request today** — there is currently zero cross-device sharing of Order Request data of any kind; every operator's Order Requests live only in their own browser's localStorage. Migrating it to Supabase is therefore a pure, unambiguous improvement: there is no existing sync behavior to preserve, retire, or reconcile against, unlike Quote/PO which already have (imperfect) Sheets sync this migration would need to coexist with or retire.

### 1.2 The nested-array question, resolved

`docs/architecture-data-model-v1.md` §8 flags this as genuinely new territory: "an id-remap sweep for [nested entities] needs to walk three levels deep, and needs a decision on whether nested entities get their own backend rows/ids at all or stay embedded JSON on the parent." Resolved by direct inspection of every place an Order Request Line id or RFQ Response id is referenced *from outside* the Order Request itself (`Quote.lines[].sourceOrdLineId`, `Quote.lines[].sourceRfqResponseId`) — these are the only two candidate reasons a nested child would need a stable, independently-resolvable id post-migration.

**Decision: `order_requests.lines` is a single `jsonb` column. Nested Order Request Line and RFQ Response ids are never remapped — they keep their existing `uid()` string values unchanged when the parent Order Request migrates.** This works because nothing outside the Order Request's own JSONB blob needs to resolve a nested child id to a *new* value: `Quote.lines[].sourceOrdLineId`/`sourceRfqResponseId` continue pointing at the same string they always did, which is still findable inside the (now-cloud-hosted) parent's `lines` JSONB after migration. Only `Quote.lines[].sourceOrdId` — a reference to the **parent** Order Request's own top-level id — needs rewriting, because that id *does* change (from a local `uid()` string to a Postgres-assigned UUID) when the Order Request row is created in Supabase.

This mirrors the precedent already set by `0002_line_items_contacts.sql`, which embeds `price_history`/`enquiries` as `jsonb` rather than promoting them to child tables — the same reasoning applies here, just one level deeper.

A consequence worth stating plainly: because the entire `lines` array (including every RFQ Response nested inside every line) is one JSONB value, Postgres cannot enforce a foreign-key constraint on any `supId`/`contactId` value buried inside it. **This means Order Request's own migration needs no Supplier- or Contact-migration-completion precondition** — a real, useful simplification relative to Line Item/Contact/PO, none of which had this option since their FK columns are real relational columns. The trade-off (documented, not hidden) is that a stale `supId`/`contactId` inside an RFQ Response is not caught by any database constraint — data-quality here depends entirely on the sweep-retrofit in §2.4/§2.5 being correct, not on schema enforcement.

### 1.3 A confirmed, currently-live pre-existing bug this REQ must fix

Direct read of `migrateSuppliersBuyersToSupabase()` (`index.html:5616-5660`, shipped and unchanged since `REQ-CLOUD-001`) confirms its external-reference sweep (`index.html:5644-5648`) rewrites `Quote.lines[].supId`, `PO.supId`, `LineItem.supId`, `Contact.supplierId`, and `Invoice.buyerId` — but **never touches `DB.ord[].lines[].rfqResponses[].supId`**. This is not a hypothetical: any operator who has ever run "Migrate Suppliers/Buyers to Cloud" while holding any RFQ Response data has had every one of those RFQ Responses' `supId` silently go stale — pointing at a local Supplier id that no longer resolves to anything, since the real Supplier row moved to Supabase under a new UUID. `renderRfqComparison()` (`index.html:3336-3378`) and `rfqLandedGBP()` (`index.html:3311-3314`) both depend on resolving `resp.supId` against `DB.sup` — a broken reference there silently degrades supplier comparison for every affected RFQ Response, with no error surfaced anywhere. This REQ closes it (§2.4/§2.5), scoped to `.supId` only.

**Round-1 requirements-gate correction — the `.contactId` half is not a bug.** The equivalent check on `migrateContactsToSupabase()` (`index.html:5719-5775`) shows its sweep (`5760-5765`) already correctly walks `DB.ord.forEach(...) → (o.lines||[]).forEach(...) → (l.rfqResponses||[]).forEach(...)` and rewrites `r.contactId` via `conIdMap`, before persisting. There is no `.contactId` gap to close — `migrateSuppliersBuyersToSupabase()` could not perform such a fix even if asked to, since it never builds a Contact-id map (only `supIdMap`/`buyIdMap`) and Contact's own migration is itself gated on Supplier's completion (`index.html:5722`), so no Contact-id map could exist yet at the point Supplier's migration runs. The original draft's REQ-CLOUD-003e/AC-4 incorrectly asked for both halves on the Supplier-side function; corrected in §2.5 to scope the retrofit to `.supId` only, with `migrateContactsToSupabase()`'s already-correct `.contactId` sweep left as-is except for the "push if already migrated" fix below, applied symmetrically.

### 1.4 The mutation-call-site problem, and why a shared helper is the right fix here (not per-site patches)

`REQ-CLOUD-002`'s round-3 spec-gate finding established the pattern: any code that mutates a Cloud-Data-eligible entity's fields outside its own `save*()`/`del*()` function will silently discard the edit on the next Cloud Data refresh once that entity has migrated, unless it is also made `_sb`-aware. For Contact, that pattern showed up at five call sites. For Order Request it is worse, and — per this REQ's own requirements-gate round 1 — the first-draft inventory below was itself incomplete and one entry factually wrong, exactly the kind of gap `REQ-CLOUD-002`'s round-3 review was written to catch; corrected here rather than left for spec-gate to rediscover.

**Round-1 requirements-gate correction:** the original draft claimed nine functions "all currently end with a bare `sv(K.ord, DB.ord)`." Re-verified against the real code: `ordAdminOverride()` (`index.html:2928-2939`) actually ends with `saveAll()` (a 12-key sweep, `index.html:3718`, which happens to include `sv(K.ord,DB.ord)` as one of twelve calls) — not a bare `sv()` as claimed, though the net effect on `DB.ord` is the same. Worse, `ordAddAction()` (`index.html:3666-3673`) **does not touch `DB.ord` at all** — it pushes to `_ordFormActions`, a module-level form-staging array, calling neither `sv()` nor `saveAll()`; the actual persistence happens later via `saveOrdFromForm()` → `saveOrd()` (already on the list), which reads `_ordFormActions` at save time (`index.html:3688`). It is dropped from the list below as a non-site. Two real, everyday-workflow sites were also missing entirely:

- **`saveQte()`** (`index.html:11571-11577`) — on Order-Request→Quote conversion, sets `convOrd.activeQuoteId = qt.id` and conditionally `convOrd.stage = 'Quoted'`, then calls `saveAll()`. This is exactly the everyday central deal-pipeline transition this REQ's own AC-1 says must migrate correctly (`activeQuoteId`, `stage`) — omitting it would mean converting a Request to a Quote silently fails to persist to Supabase the moment Order Request has migrated.
- **`delCon()`** (`index.html:11830-11862`) — both its `_sb` branch (`11837-11843`) and its local branch (`11851-11858`) null out `o.contactId` and every nested `rfqResponses[].contactId` for a deleted Contact, persisting via a bare `sv(K.ord, DB.ord)` in both branches. `REQ-CLOUD-002` itself established the precedent that a Contact-deletion cascade reaching into another entity's array must be brought into that entity's own Cloud Data scope when that entity migrates (`docs/SPEC-CLOUD-002-v1.md` §2.11 pulled `saveQte()`/`delQte()` into Contact's own scope for the same reason, in reverse) — the same standard requires `delCon()`'s Order-Request cascade be in scope here.

**Corrected list — eleven sites, routed through one shared choke point instead of eleven near-identical `_sb`-branch copies:** `saveOrd()` (`index.html:3029`), `delOrd()` (`3059`), `ordAdminOverride()` (`2928`), `ordLogLineUpdate()`/`ordConfirmLineUpdate()` (`2957`, `2975`), `ordAddLine()` (`3578`), `saveRfqResponse()` (`3256`), `delRfqResponse()` (`3291`), `ordCommitRfqResponse()` (`3325`), `saveQte()`'s `convOrd` mutation (`11571-11577`), `delCon()`'s Order-Request cascade (`11837-11843` and `11851-11858`, both branches), and **`executeDataCleanup()`'s renumbering step** (`index.html:9118-9155`, specifically `renumberEntitySequentially(DB.ord, 'ORD')` at `9133`, which mutates `rec.num` on every Order Request, phantom or not, on every "Clean Up Now" click — persisted via the same `saveAll()` bypass as `ordAdminOverride()`).

**Round-2 requirements-gate correction — this eleventh site was missed by round 1's own "corrected, now-exhaustive" list**, found by a fresh independent search rather than a re-read of round 1's fix. `executeDataCleanup()`'s other `DB.ord` mutation in the same function (its phantom-record filter, `index.html:9122`) is deliberately **not** added to this list: a phantom record is defined as one with no `id` at all (`isPhantomRecord()`, `index.html:2844-2849`), and a Postgres-hosted row always has a real `id` assigned by `gen_random_uuid()` at insert time — so once Order Request has migrated, no cloud-sourced record can ever be phantom, making the filter step a structural no-op for cloud-hosted data by construction, not something that needs a `persistOrdChange()` push. Only the renumbering step touches real, migrated records' fields and needs the fix.

**Decision: introduce one shared helper, `persistOrdChange(ord)`,** that every one of these eleven sites calls instead of its current `sv(K.ord, DB.ord)`/`saveAll()` call. It performs the entire `_sb`-branch logic once: push a whole-row update of the given Order Request to Supabase and refresh when Cloud Data is configured and Order Request has migrated, otherwise fall back to the existing `sv(K.ord, DB.ord)` behavior unchanged. Every call site's business logic (validation, id rotation, cascading field updates) stays exactly as it is today — only the final persistence line changes. Replacing `ordAdminOverride()`'s `saveAll()` with `persistOrdChange(ord)` is a deliberate, disclosed behavior change: it stops incidentally re-persisting the other eleven keys `saveAll()` touches, which was never load-bearing for that function (it doesn't modify anything outside `DB.ord`) and was always a harmless-but-unnecessary full save.

**Advisory, noted but not required:** `clearDemoData()`/`loadDemoData()` (`index.html:4925-4940`, `4856ff`) and `backfillOrderRequests()`/`backfillRefNums()` (`index.html:3001`, `3017`, `2832-2833`) also mutate `DB.ord` via `saveAll()` or a direct `sv()` outside the eleven sites above. These are demo/backfill utilities, not everyday operator actions, and are not required to route through `persistOrdChange()` for this REQ — noted for completeness, not left silently undiscovered.

**Inherited, not novel, consistency trade-off:** `persistOrdChange()`'s whole-row-update shape carries the same cross-tab/cross-device last-write-wins risk `saveSup()`/`saveCon()`'s identical whole-row-update design already accepts today — not a new risk this REQ introduces. Within a single browser tab, JavaScript's single-threaded execution means no two of the eleven sites can interleave mid-mutation; no UI flow was found calling two of them back-to-back on the same in-memory `ord` object before a save lands.

**A real, disclosed intermediate-state risk from splitting Phase 2 (§0):** the `saveQte()` site above is Quote-side code mutating an Order-Request field — brought into this REQ's scope specifically because deferring Quote's own Cloud Data migration to `REQ-CLOUD-004` does not defer Quote-side code from touching already-migrated Order Request data today. Any other Quote/PO-side code found at spec-gate to mutate `DB.ord` fields must be brought into this REQ's scope the same way, not deferred to `REQ-CLOUD-004`/`005` — those REQs migrate Quote/PO themselves, not fix Order-Request-directed side effects that already exist.

### 1.5 Established migration-function pattern (for spec-gate's reference, not a novel decision here)

Every `migrate*ToSupabase()` function shipped so far (`index.html:5616-5775`) follows: precondition (`_sb` configured, auth, any required prior-migration-completion check) → mandatory blocking backup attestation (`showBlockingBackupModal()`) → pre-flight conflict/orphan scan verified against the live, currently-connected project (not a local flag, per `REQ-CLOUD-002`'s round-2 fix) → sequential insert-and-map loop → exhaustive external-reference sweep (including deliberately-dead fields, checked but never silently skipped) → archive-before-remap (`localStorage.setItem('st_&lt;x&gt;_pre_migration', ...)` before any local id is rewritten) → self-id remap → refresh-and-reveal. `refresh&lt;X&gt;FromSupabase()` refuses to replace real local data unless this device's own completion marker is set or there is nothing local to lose, and sets its own marker on any successful refresh (including a fresh second device's first-ever load) — closing the gap `REQ-CLOUD-002`'s round 3 found. `restore&lt;X&gt;MigrationArchive()` disconnects Cloud Data and clears its own marker on restore (`REQ-CLOUD-002`'s round-2 fix, applied proactively to `restoreOrdMigrationArchive()` from the start here).

### 1.6 A second, newly-found pre-existing bug — explicitly deferred to `REQ-CLOUD-005`, not fixed here

Direct read of `delPO()` (`index.html:7665-7679`) confirms it cleans `Invoice.pos[]` (added by `REQ-INTEG-002` Phase 2b) but never cleans `Quote.linkedPOIds[]` — a Quote whose only PO is deleted keeps its "PO RAISED" badge (`index.html:11247`) and its `qteToPoConvert()` re-conversion guard (`index.html:11881`) permanently blocked, with no way to recover short of manual data surgery. This is real and currently live, but it is a Quote↔PO relationship concern, not an Order Request one — logged here for continuity and explicitly assigned to `REQ-CLOUD-005` (Purchase Order), alongside `docs/architecture-data-model-v1.md` §6.4 (`autoPos()`/`qteToPoConvert()` never call `logEv()`), since both require touching PO's own migration/CRUD code this REQ does not.

---

## 2. Requirements

**REQ-CLOUD-003a — New `order_requests` Supabase table.** Schema (§2.1 below) with `lines` as a single `jsonb` column carrying the full nested Order Request Line + RFQ Response structure unchanged (§1.2). `contact_id` is a plain nullable `uuid` column — deliberately **not** `REFERENCES contacts(id)`-constrained at the database level, even though `Order Request.contactId` is required client-side at save time (`vErr('of-contact', ...)`, `index.html:3032`) — see REQ-CLOUD-003b for why a real FK constraint here would be actively wrong, not just unnecessary. `active_quote_id` is likewise a plain nullable `uuid`/`text` column with **no** FK constraint (Quote is not yet Cloud-Data-eligible; forcing a constraint here would either block Order Request's migration on Quote's, contradicting §0's sequencing, or require a nullable-but-unenforced column anyway — simpler to just not constrain it, for the same reason as `contact_id`).

**REQ-CLOUD-003b — No Supplier- or Contact-migration-completion precondition for Order Request's own migration.** Per §1.2's reasoning: `supId`/`contactId` values nested inside RFQ Responses live inside an opaque `jsonb` blob no Postgres constraint can reach, so there is nothing to validate against before Order Request can migrate. `order_requests.contact_id` (the *top-level* Order Request's own required Contact FK) is a plain nullable `uuid` column, deliberately **not** `REFERENCES contacts(id)`-constrained at the database level — a real FK constraint here would reject any Order Request whose Contact hasn't itself already migrated (since `contacts` would have no matching row), directly contradicting AC-2's requirement that Order Request migrate successfully on an install where Contact has never been Cloud-migrated at all. This is the same reasoning Quote's `source_contact_id` will use in `REQ-CLOUD-004`.

**REQ-CLOUD-003c — `persistOrdChange(ord)` shared persistence helper**, used by all eleven mutation sites listed in §1.4 (`saveOrd`, `delOrd`, `ordAdminOverride`, `ordLogLineUpdate`/`ordConfirmLineUpdate`, `ordAddLine`, `saveRfqResponse`, `delRfqResponse`, `ordCommitRfqResponse`, `saveQte`'s `convOrd` mutation, `delCon`'s Order-Request cascade in both its `_sb` and local branches) in place of each site's current `sv(K.ord, DB.ord)`/`saveAll()` call. Each site's own business logic (validation, state-machine checks, id rotation on RFQ-response edit) is unchanged; only the final persistence step changes.

**REQ-CLOUD-003d — Exhaustive external-reference sweep on migration.** Only `Quote.lines[].sourceOrdId` is rewritten (to the new Supabase-assigned Order Request id); `Quote.lines[].sourceOrdLineId`/`sourceRfqResponseId` are explicitly checked-but-confirmed-unchanged in the SPEC and its tests, per §1.2's reasoning, matching this codebase's established "checked anyway, never silently skipped" convention for fields that don't need rewriting.

**REQ-CLOUD-003e — Retrofit `migrateSuppliersBuyersToSupabase()`'s sweep** (`index.html:5644-5648`) to additionally walk `DB.ord[].lines[].rfqResponses[].supId`, closing §1.3's confirmed bug (`.supId` only — the `.contactId` half is already handled correctly by `migrateContactsToSupabase()`, per §1.3's round-1 correction), **and** to push the rewritten `lines` value to the cloud-hosted `order_requests` row (via `persistOrdChange()` or equivalent direct Supabase call) rather than only fixing the local `DB.ord` array, whenever Order Request has itself already completed its own migration (`st_ord_cloud_migration_ts` set) — otherwise the fix is silently lost the next time Order Request refreshes from Supabase. Apply the identical "push if Order Request has already migrated" fix to `migrateContactsToSupabase()`'s existing, already-correct `.contactId` sweep (`index.html:5760-5765`) too — it has the same local-only-fix exposure today, just not the missing-field bug. This closes the general cross-phase risk `REQ-CLOUD-002`'s own sweeps would otherwise reproduce here in reverse (an earlier-migrated entity's rewrite of a later-migrated entity's field, applied only locally).

**REQ-CLOUD-003f — Archive-not-delete rollback**, mirroring the established pattern: `st_ord_pre_migration`/`st_ord_cloud_migration_ts`, `restoreOrdMigrationArchive()` (disconnects Cloud Data and clears its own marker on restore, from the start — not added retroactively as `REQ-CLOUD-002` had to), extended `cleanupExpiredMigrationArchive()`, extended `rCfg()`'s restore-button visibility.

**REQ-CLOUD-003g — Mandatory blocking backup gate**, reusing `showBlockingBackupModal()` unchanged.

**REQ-CLOUD-003h — Pre-flight conflict scan: documented no-op.** `ORD-####` numbers are generated by `nextRefNum(DB.ord, 'ORD')` (`index.html:3050`), which scans the current max and increments — no duplicate-producing path exists today outside a genuine concurrent-tab race (exactly the class of risk `SEC-GAP-011`/this whole initiative exists to reduce over time, not something a single migration's pre-flight scan can meaningfully guard against). No pre-flight scan is added; this is stated explicitly rather than left as a silent gap, matching `REQ-CLOUD-002`'s established convention for genuine no-ops (REQ-CLOUD-002g).

**REQ-CLOUD-003i — `delOrd()` gets a symmetric `_sb` branch despite being confirmed dead code today** (`docs/architecture-data-model-v1.md` §6.3 — defined, unit-tested, but no UI element calls it). Included for consistency with every other entity's save/delete pair, and because a future REQ wiring up a delete button should not also have to remember to add Cloud Data awareness at that point.

**REQ-CLOUD-003j — No changes to `pullAll()`/`syncAll()`/`pushAll()`/`FIELD_MAPS` for Order Request.** It has no existing footprint in any of them (§1.1) — there is nothing to exclude.

---

## 3. Explicitly out of scope

- **Quote and Purchase Order Cloud Data migration** — `REQ-CLOUD-004`/`REQ-CLOUD-005` (§0).
- **`docs/architecture-data-model-v1.md` §6.4** (`autoPos()`/`qteToPoConvert()` never call `logEv()`) — assigned to `REQ-CLOUD-005` (§1.6), since both functions are PO-creation code this REQ does not touch.
- **The newly-found `delPO()` → `Quote.linkedPOIds[]` cleanup gap** — assigned to `REQ-CLOUD-005` (§1.6).
- **`ORD-GAP-001`/`002`/`004`** — pre-existing, unrelated to Cloud Data migration mechanics.
- **Making `delOrd()` reachable from the UI** — it gets a Cloud Data branch (REQ-CLOUD-003i) but stays otherwise dead code; wiring up a delete button for Order Requests is a separate product decision.
- **`processImport('ord')`'s embedded Contact-creation sub-path** (`index.html:8639-8712`, the Contact-creation branch at `~8657-8662` specifically) bypassing Cloud Data via a raw `DB.con.push()` that never calls `saveCon()` — this is the same class of gap already logged as `CLOUD-GAP-003`; not fixed here, matching `REQ-CLOUD-002`'s own precedent of leaving CSV-import bypass for a future standalone REQ. **Round-2 requirements-gate correction:** the Order-Request-creation part of this same CSV branch already calls `saveOrd(ordObj)` per submission (`index.html:8703`) — once `persistOrdChange()` lands inside `saveOrd()` (REQ-CLOUD-003c), this path correctly becomes Cloud-Data-aware automatically, with no separate fix needed. Only the embedded Contact-creation sub-path remains a genuine bypass.
- **`Order Request Line`'s CSV-import shape inconsistency** (`docs/architecture-data-model-v1.md` §6.11 — CSV-imported lines don't initialize `rfqResponses`/`committedResponseId`) — pre-existing, unrelated to Cloud Data mechanics, already tolerated by defensive `||[]`/`||null` guards everywhere it's read.
- **`REQ-ORD-004`** (triage → Order Request CSV export) — confirmed unshipped/untracked by direct research; unrelated to this REQ regardless.

---

## 4. Acceptance criteria

- **AC-1:** With Cloud Data configured, migrating Order Requests inserts every local Order Request into `order_requests`, mapping every real `DB.ord` field (`num`, `contactId`→`contact_id`, `stage`, `description`, `actions[]`→jsonb, `activeQuoteId`→`active_quote_id` — carried forward as its current local-or-cloud value, not resolved or rewritten, since Quote is out of scope here — `outcome`→jsonb, `lines[]`→`lines` jsonb **unchanged, including every nested RFQ Response and every nested id**, `createdAt`→`created_at`, `importBatchId`→`import_batch_id`). `id` is correctly omitted (Postgres assigns a fresh UUID, matching every prior migration's convention). `_demo`/`_backfilled` (internal bookkeeping markers read only by `clearDemoData()`/`backfillOrderRequests()`, `index.html:3014`/`4935`) are explicitly **not** migrated to any Supabase column and carry forward unchanged in the local record, matching `invoiceRefs[]`'s precedent in `REQ-CLOUD-002` — stated explicitly rather than left to be discovered as an omission.
- **AC-2:** No Supplier- or Contact-migration-completion precondition blocks Order Request's migration (§1.2, §2b); an install where neither Supplier nor Contact has ever been Cloud-migrated can still migrate Order Requests successfully.
- **AC-3:** After migration, `Quote.lines[].sourceOrdId` is rewritten to the new Supabase-assigned Order Request id everywhere it matches; `sourceOrdLineId`/`sourceRfqResponseId` are confirmed unchanged by an explicit test, not merely absent from the sweep by omission.
- **AC-4:** `migrateSuppliersBuyersToSupabase()`'s sweep now also rewrites `DB.ord[].lines[].rfqResponses[].supId` wherever it matches a migrated Supplier — verified both when Order Request has not yet migrated (local-only fix) and when it has (the fix is pushed to the cloud-hosted `order_requests` row, not just the local array). `migrateContactsToSupabase()`'s existing, already-correct `.contactId` sweep gets the identical already-migrated-push fix, verified the same way. No `.contactId` rewrite logic is added to `migrateSuppliersBuyersToSupabase()` — it has no Contact-id map to rewrite with, and none is needed since `migrateContactsToSupabase()` already handles it correctly.
- **AC-5:** All eleven mutation sites listed in §1.4/REQ-CLOUD-003c persist via `persistOrdChange()`; each one's existing local-only behavior is byte-for-byte unchanged when Cloud Data is not configured or Order Request has not yet migrated, and each pushes correctly to Supabase once it has. This explicitly includes `saveQte()`'s Order-Request-side mutation and both of `delCon()`'s branches, not just the eight `ord*`/`rfq*`-named functions.
- **AC-6:** `showBlockingBackupModal()`'s existing manual-attestation gate blocks any Supabase write until checked; declining aborts with no partial state, matching every prior migration.
- **AC-7:** Pre-migration `DB.ord` is archived (not deleted) for 30 days, restorable via `restoreOrdMigrationArchive()`, which disconnects Cloud Data and clears `st_ord_cloud_migration_ts` on restore.
- **AC-8:** Zero regressions to Supplier/Buyer, Line Item, or Contact's existing Cloud Data behavior, or any of the 712 existing tests.
- **AC-9:** `refreshOrdFromSupabase()` refuses to overwrite real local `DB.ord` data on a device that has never itself run the Order Request migration and sets its own completion marker on any successful refresh (including a fresh second device's first load) — both fixes `REQ-CLOUD-002` needed two separate spec-gate rounds to reach, applied here from the start.

---

## 5. Testing approach

Follows the established pattern: unit tests for the (absence of a) precondition gate (AC-2, both zero-Supplier and zero-Contact cases), the migration function's field-mapping and nested-structure-preservation correctness (AC-1, including a multi-line Order Request with multiple RFQ Responses per line and at least one committed response), the external-reference sweep (AC-3, including a Quote line with all three `source*` fields set, confirming exactly one is rewritten), the Supplier-sweep retrofit and its Contact-sweep counterpart (AC-4, both the not-yet-migrated and already-migrated Order Request cases for each — this is the test that would have caught §1.3's `.supId` bug had it existed at `REQ-CLOUD-001` time), each of the eleven `persistOrdChange()` call sites — including `saveQte()`'s and both of `delCon()`'s branches, not just the eight `ord*`/`rfq*`-named ones — in both its `_sb`-configured-and-migrated and local-only forms (AC-5 — by far the largest test surface in this REQ, mirroring `REQ-CLOUD-002`'s round-3 finding that this exact class of site is where real bugs hide), the backup gate (AC-6), archive/rollback (AC-7), and the refresh-guard/self-marking pair (AC-9, reusing the exact test pattern `REQ-CLOUD-002`'s `tests/run.js` already established for Line Item/Contact).

---

## 6. Gate process

Per `CLAUDE.md`'s standing checklist: requirements-gate → SPEC → spec-gate → implementation → self-performed mutation testing → build-gate → PR → CI green → merge. Given `REQ-CLOUD-002` needed 4 spec-gate rounds for a materially smaller scope (two flat entities vs. one entity with a nested structure and eleven mutation sites), expect at least as many here, and budget for the SPEC needing to independently re-verify the mutation-site inventory (§1.4) is now actually exhaustive — this REQ's own first draft missed two of them (§1.4's round-1 correction), so a fresh, systematic re-grep at spec-gate, not a re-read of this REQ's list, is the right level of scrutiny.

---

## 7. Tracker / known-gaps updates required on completion

- `docs/known-gaps.md`: no `CLOUD-GAP-003` scope extension needed — `processImport('ord')`'s Order-Request-creation path already routes through `saveOrd()`, which becomes Cloud-Data-aware automatically once REQ-CLOUD-003c ships (§3, round-2 correction); only its embedded Contact-creation sub-path is a genuine, already-covered `CLOUD-GAP-003` instance.
- `docs/requirements-tracker.md`: add `REQ-CLOUD-003` with full gate history.
- `STACKD_CONTEXT.md`/`CLAUDE.md`: standard version-ship updates.
- `docs/architecture-data-model-v1.md`: update §2 entity inventory (Order Request/Order Request Line/RFQ Response rows) and §4.2's Order Request narrative to reflect Cloud Data now in scope; update §8's sequencing to record Phase 2 is proceeding as three sequential sub-phases (this document's §0) rather than one combined delivery, and mark sub-phase 1 (Order Request) complete once shipped.

---

## 8. Review-resolution log

**Round 1: CONDITIONAL PASS — 3 blocking findings, 3 advisories, all fixed in place.** The overall design (JSONB embedding for nested Lines/RFQ Responses, the three-way phase split, `persistOrdChange()` as a shared choke point, reuse of the established 9-step migration pattern) was independently confirmed sound. Every finding concerned the REQ's own empirical claims about the real code, not its architecture:

- **B1 — blocking.** §1.4's mutation-site inventory was inaccurate (`ordAdminOverride()` ends with `saveAll()`, not a bare `sv()`) and incomplete on both ends: `ordAddAction()` doesn't touch `DB.ord` at all and was wrongly listed; `saveQte()`'s `convOrd` mutation and both of `delCon()`'s Order-Request-cascade branches were missing entirely, despite both being real, everyday-workflow bypasses of exactly the kind `REQ-CLOUD-002`'s own round-3 finding exists to catch. **Fixed:** §1.4 rewritten with the corrected, ten-site list; §2c, AC-5, §5, and §6 updated to match.
- **B2 — blocking.** REQ-CLOUD-003e/AC-4 asked `migrateSuppliersBuyersToSupabase()`'s sweep to also rewrite RFQ Response `.contactId` — but `migrateContactsToSupabase()` already does this correctly today, and `migrateSuppliersBuyersToSupabase()` has no Contact-id map to do it with (Contact's own migration is gated on Supplier's completion, so no such map could exist at the point Supplier's migration runs). **Fixed:** §1.3 and REQ-CLOUD-003e/AC-4 retargeted to `.supId` only, with the already-migrated-push fix applied symmetrically to `migrateContactsToSupabase()`'s existing `.contactId` sweep instead of asking the wrong function to duplicate it.
- **B3 — blocking.** REQ-CLOUD-003a called `order_requests.contact_id` "a real FK to `contacts(id)`," while REQ-CLOUD-003b called the same column "nullable-without-hard-FK-enforcement" — two different schemas with different runtime behavior, and only one of them (no FK constraint at all) makes AC-2 achievable. **Fixed:** REQ-CLOUD-003a corrected to state the column is a plain nullable `uuid`, not FK-constrained, matching REQ-CLOUD-003b and AC-2.
- **A1 — advisory.** §0/§1 didn't acknowledge that splitting Phase 2 creates a concrete intermediate-state risk (Quote-side code, like `saveQte()`, mutating Order-Request fields) rather than just an abstract one. **Fixed:** §1.4 now states this explicitly, including the rule that any further such site found at spec-gate belongs in this REQ's scope, not deferred to `REQ-CLOUD-004`/`005`.
- **A2 — advisory.** §6's framing of `persistOrdChange()`'s whole-row-update design as an open "could two sites race" question was reframed — no two of the ten sites can interleave within a single tab (JS is single-threaded, no UI flow calls two back-to-back before a save lands), and the real trade-off (cross-tab/cross-device last-write-wins) is inherited unchanged from `saveSup()`/`saveCon()`'s identical design, not novel here. **Fixed:** §1.4 now states this as an inherited, not novel, trade-off.
- **A3 — advisory.** AC-1's field list didn't address `_demo`/`_backfilled` internal bookkeeping markers. **Fixed:** AC-1 now explicitly excludes them, matching `invoiceRefs[]`'s precedent from `REQ-CLOUD-002`.

Confirmed round-1-ready: the mutation-site inventory is now independently re-derivable from the real code (not just re-stated), the retrofit requirements target functions that can actually perform the fix asked of them, and the schema description is internally consistent with its own acceptance criteria.

**Round 2: CONDITIONAL PASS — 1 new blocking finding, 1 new advisory, both fixed in place.** Independently re-verified all three round-1 fixes by re-reading the real code a second time, plus running a fresh, independent search for `DB.ord` mutation sites rather than re-checking round 1's list — exactly the discipline round 1's own B1 finding demanded of round 2. B1/B2/B3's fixes all confirmed correct: every one of the ten listed sites checks out at its cited line and shape; `migrateContactsToSupabase()`'s `.contactId` sweep is confirmed genuinely already correct; `order_requests.contact_id`'s description is now internally consistent. One new site was found that round 1's "corrected, now-exhaustive" list still missed, and one new drafting inaccuracy:

- **New blocking.** `executeDataCleanup()` (`index.html:9118-9155`) — the operator-facing Data Integrity Cleanup feature — mutates `DB.ord` via `renumberEntitySequentially(DB.ord, 'ORD')` (`9133`), touching **every** Order Request's `num`, not just corrupted ones, on every "Clean Up Now" click, persisted via the same `saveAll()` bypass `ordAdminOverride()` used before round 1's fix. This is a real, always-available operator feature, not a demo/backfill utility — unlike the advisory-listed exceptions, it cannot be left unrouted. **Fixed:** added as an eleventh `persistOrdChange()` site (§1.4/§2c/AC-5/§5 updated to "eleven" throughout). `executeDataCleanup()`'s *other* `DB.ord` mutation (its phantom-record filter, `9122`) is deliberately left out: a phantom record is defined as having no `id` at all, and a Postgres-hosted row always has a real `gen_random_uuid()`-assigned `id`, so no cloud-sourced record can ever be phantom — the filter step is a structural no-op for migrated data by construction, stated explicitly rather than silently omitted.
- **New advisory.** §3/§7's characterization of `processImport('ord')` as bypassing Cloud Data was inaccurate: only its embedded Contact-creation sub-path (a raw `DB.con.push()`) genuinely bypasses anything — the Order-Request-creation part already calls `saveOrd(ordObj)` per submission, which becomes Cloud-Data-aware automatically once REQ-CLOUD-003c ships. **Fixed:** §3 and §7 corrected to scope the still-open `CLOUD-GAP-003` instance to the Contact-creation sub-path only, and to state plainly that no further known-gaps update is needed for the Order-Request-creation path.

Confirmed round-2-ready: a second independent, fresh search for `DB.ord` mutation sites (not a re-check of the REQ's own list) found nothing further; every remaining citation and cross-reference in the document was re-verified against the real code a second time.

**Round 3: PASS, no new findings.** A third independent, fresh search — checking `handleAIAction()`'s full dispatch table, `doImport()`'s restore path (confirmed correctly out of scope, already covered by the pre-existing `CLOUD-GAP-001`), `ordConvertToQuote()`, the full RFQ email-parse flow, `processImportRecords()`, and every one of the 14 `sv(K.ord, DB.ord)` call sites in the file individually — found nothing further. Every one of the eleven cited mutation sites and every supporting citation was re-verified against the real code a third time; no internal inconsistency found across §0-§8. Confirmed SPEC-ready.
