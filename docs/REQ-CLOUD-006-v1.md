# REQ-CLOUD-006 (Phase 3, sub-phase 1 of 3) — Extend Cloud Data to Invoice and Credit Note

**Status:** v1 — drafted. Requirements-gate round 1: CONDITIONAL PASS (3 blocking, 4 advisory), fixed. Round 2: CONDITIONAL PASS (1 blocking — round 1's own CSV-import fix still overstated a nonexistent precedent; 1 advisory — a mutation-site count off-by-one; 1 candidate finding investigated and correctly rejected), fixed. Round 3: raised a candidate finding on the same `pullAll()` citation round 2 had already raised and this document had already rejected — re-verified a third time, directly against a worktree pinned to this REQ's own commit rather than an ambient shared directory, and confirmed the citation was correct all along; both round 2's and round 3's contrary claims are now understood to share one root cause (see §8). That same investigation surfaced a real, separate, systemic problem — the document's original check-first pass had been run against a stale, pre-`REQ-LI-001` copy of `index.html` — and triggered a full citation-accuracy pass that found and corrected 44 other citation instances that genuinely were wrong. Round 4: independently spot-checked 35 citations (0 further errors — the round-3 fix holds), then a deeper content/design pass found 1 new blocking design gap (goodwill-credit `DB.payments[]` entries are self-referencing and needed explicit coverage in the local sweep) and 1 advisory (a CN's `linkedInvId` can resolve to another CN, mechanically handled but unacknowledged) — both fixed. Round 5: 1 blocking — the outward sweep into Purchase Order (REQ-CLOUD-006f) left its `invId`/`invNum` matching algorithm unspecified, a real risk given this codebase's own documented, open `PO-GAP-004` divergence — fixed by fully specifying the same `invNum`-first, `invId`-fallback precedence `backfillInvoicePOs()` already established for this exact problem. Round 6: 1 blocking — the identical unaddressed ambiguity, one array over: REQ-CLOUD-006g's `DB.payments[].invId` sweep was a bare `invId`-key lookup, which a live Sheets-sync mechanism can produce a payment with a correct `invNum` but no `invId` for — fixed with the same `invNum`-first precedence as REQ-CLOUD-006f. Round 7: 1 blocking — a third instance of the identical bug class, this time in the CN self-referential remap itself (REQ-CLOUD-006b): `FIELD_MAPS.cn` never maps `linkedInvId` to Sheets, so a Sheets-sync-originated CN can have a correct `linkedInvNum` but blank `linkedInvId` — fixed with the same `linkedInvNum`-first precedence. Round 8: **PASS**, clean, no findings — hunted for a 4th instance in the reverse direction (Invoice's own outward refs: `buyerId`/`lineItems`/`linkedQuoteId`/`pos[]`) and confirmed none is exploitable, for three distinct reasons; then a consolidation pass spot-checked 30+ citations with zero errors. Requirements-gate complete; proceeding to SPEC. See §8.
**Scope:** Phase 3 sub-phase 1 of 3 of the cross-platform backend migration's fulfillment/financial step (`docs/architecture-data-model-v1.md` §8, point 5), following REQ/SPEC-CLOUD-001 through 005's already-proven mechanism. Sub-phase 2 (Shipment) and sub-phase 3 (Buyer Payment, Supplier Payment) are separate, later REQs — deliberately sequenced last since they are the exact ledgers `REQ-INTEG-002` (2c/2d) is actively reshaping; see §0.
**Build baseline:** `main` @ `c4242e2`, 803/803 tests passing (includes `REQ-LI-001`, shipped since this REQ's check-first pass began; unrelated to Invoice/Cloud Data, no re-verification needed against it).

---

## 0. Scoping

Phase 2 (Order Request → Quote → Purchase Order) completed in three sub-phases, one entity per REQ, in referential-dependency order. Phase 3 covers Invoice, Credit Note, Shipment, Buyer Payment, and Supplier Payment (`docs/architecture-data-model-v1.md` §8, point 5) and is split the same way. **Invoice and Credit Note go first** because they share one local array (`DB.inv`, Credit Note is a `type` flag on the same records, not a separate array) and are the least entangled with other in-flight work. **Shipment goes second** (sub-phase 2, not started here). **Buyer Payment and Supplier Payment go last** (sub-phase 3, not started here) — deliberately, because `REQ-INTEG-002` sub-phases 2c (in requirements-gate review as of this REQ's drafting) and 2d (unscoped) are actively changing the Buyer Payment ledger's own shape and matching logic; migrating that ledger to Cloud Data before its shape settles would mean re-touching the migration once 2c/2d land. This REQ does not touch `DB.payments`/`DB.supPayments` structurally — it only rewrites `DB.payments[].invId` values as a side effect of Invoice's own id changing (§2, REQ-CLOUD-006g), which is required regardless of whatever matching-rule `REQ-INTEG-002-2c` ships in `getInvPayments()`, since correctly updating a stored id value is orthogonal to how a lookup function chooses to prefer `invId` vs `invNum`.

A companion review of `docs/v3-architect-handoff.md` confirms this migration is not blocked by, and does not need to anticipate, the separate "v3.0.0" multi-tenant rebuild scoping effort: that document explicitly expects today's single-tenant Cloud Data schema (including whatever this REQ adds) to need "a full redesign for tenant isolation" later, and `STACKD_CONTEXT.md`'s own Automation-First design principle already accepts that tradeoff for the 7 entities migrated so far — this REQ follows the same accepted pattern, not a new risk.

---

## 1. Business context (facts established during check-first — see §8 round 3 for a correction to this section's own citation accuracy)

**Correction (requirements-gate round 3):** the check-first pass below was inadvertently run by reading `index.html` from a shared working directory that, at the time, was checked out on an unrelated in-flight branch one merge behind `main` (missing `REQ-LI-001`'s several small insertions) — not against this REQ's own stated baseline, `main`@`c4242e2`. Every line-number citation in §1 and §2 below has since been independently re-verified and, where wrong, corrected against the real `c4242e2` state in a dedicated citation-accuracy pass (§8 round 3). The underlying factual claims (what a function does, what fields exist, what's local-only vs. Cloud-eligible) were never affected by this — only the numeric line references were off, by 1-4 lines depending on position in the file.

### 1.1 One correction to `docs/architecture-data-model-v1.md`

§4.2/§6.6 claims live single-record Sheets sync "targets the wrong entity" for Credit Notes (`saveCN()` allegedly pushes under `'inv'` instead of `'cn'`). **This is not reproducible on current code.** `syncEnt(entity, rec)` (`index.html:4372-4377`) normalizes internally: `var ent = (entity === 'inv' && (rec.type === 'credit_note' || rec.type === 'goodwill_credit')) ? 'cn' : entity;` — so both `saveInv()`'s call (`syncEnt('inv',inv)`, `7162`) and `saveCN()`'s call (`syncEnt('inv', cn)`, `10051`) already resolve correctly regardless of the literal string passed. `git blame` shows this normalization predates the architecture doc's own stated verification date. `delInv()` (`8243`) also already routes delete correctly (`delEnt(_isCnDel ? 'cn' : 'inv', ...)`). The architecture doc's other two §6.6 claims — zero `logEv()` on CN create/edit, and `delInv()` mislabeling a CN deletion as `"Invoice ... deleted"` — remain independently confirmed live (§1.6).

### 1.2 Two independent creation paths for Credit Notes, with materially different record shapes

Credit Notes can be created two ways, and they produce genuinely different shapes:
- **`saveInv()`'s CN sub-mode** (invoice number typed as `CN####...` inside the ordinary Invoice modal): format-checked via `vInv()`/`RX.invNum` (`8969`), sets `buyerId` (a real FK, unconditionally, `7008`), and shares every other Invoice field.
- **The dedicated `saveCN()`/`ov-cn` modal** (`9999-10052`): its own object literal (`10006-10023`) has **no `buyerId` field at all** — only a free-text `buyer` string — and hardcodes `lineItems:[]`, `taxRate:0`, `lf:0`, `ins:0`, `dep:0`, omitting `pol/pod/coo/incoterm/wt/cbm/pk` and every `calc_*` field entirely. Its own number validation (`vCN()`, `9985-9997`) enforces **no format regex at all** — only non-empty — unlike `vInv()`'s `RX.invNum` check. `vCN()`/`saveCN()` each run their own independent duplicate-`num` scan (`10002-10003`) against the same shared `DB.inv` array.

Neither asymmetry (the format-check gap, the shape divergence) blocks this migration — both paths already write into the one array this REQ migrates as a whole — but the migration's pre-flight duplicate-number scan (§2, REQ-CLOUD-006c) must scan the *whole* `DB.inv` array regardless of `type`, matching what `vInv()`'s/`saveCN()`'s own checks already do, and the fixed asymmetry itself is logged, not fixed, as `CN-GAP-001` (§3).

### 1.3 Every `DB.inv` mutation site (23 found; the most of any entity in this series)

| Site | Lines | Local-only today? | Needs Cloud Data branch |
|---|---|---|---|
| `saveInv()` | `7002-7190` | create/update; also pushes a **self-referencing** goodwill-credit `DB.payments[]` entry when `inv.type==='goodwill_credit'` (`7075-7078`, `invId: inv.id` — the credit's own `DB.inv` id, not a different invoice's) | ✓ own create-or-update branch; goodwill push covered by REQ-CLOUD-006g's `DB.payments[].invId` sweep (corrected, requirements-gate round 4 — see §1.6, §2g) |
| `saveInvApprove()` | `7220-7239` | update, no `syncEnt()` today | ✓ via `persistInvChange()` |
| `saveInvProgress()` | `7262-7276` | update, no `syncEnt()` today | ✓ via `persistInvChange()` |
| `autoPos(inv)` | `7278-7320` | appends `pos[]`, no `syncEnt()` for the invoice itself | ✓ via `persistInvChange()` |
| `delInv(id)` | `8230-8244` | delete | ✓ own soft-delete branch |
| `saveCN()` | `9999-10052` | create/update, also mutates a *different* invoice's `calc_balanceDue` in place (`10036-10048`); also pushes a **self-referencing** goodwill-credit `DB.payments[]` entry when `type==='goodwill_credit'` (`10024-10028`, `invId: cn.id` — the credit's own `DB.inv` id) | ✓ own create-or-update branch, plus `persistInvChange()` for the side-mutated invoice; goodwill push covered by REQ-CLOUD-006g (see §1.6, §2g) |
| `savePayment()` | `13043-13061` | mutates `dep`/`status`/`updAt` | ✓ via `persistInvChange()` |
| `deletePayment(id)` | `13095-13114`ish | mutates `dep` (never re-derives `status` — pre-existing, unrelated to this REQ; see `REQ-INTEG-002-2c` §1.3) | ✓ via `persistInvChange()` |
| `processImport()` CSV, `entity==='inv'` | `9250-9298` | create/update | out of scope — deferred, to be logged as a new `CLOUD-GAP-003` instance at ship time (corrected, §2 REQ-CLOUD-006l — no PO precedent for CSV Cloud Data support, or even for PO's own instance actually being logged, exists) |
| `processImportRecords()`, `entity==='inv'` | `9571-9636` | create/update, independently-coded duplicate of the above | out of scope — same, second new `CLOUD-GAP-003` instance |
| `pullAll()` Invoice block | `4524-4543` | unconditional merge, **no Cloud Data exclusion gate exists today** | ✓ new gate (§2, REQ-CLOUD-006i) |
| `pullAll()` Credit Note block | `4545-4565` | unconditional merge, same gap | ✓ same gate |
| `migrateSuppliersBuyersToSupabase()` | `5919-5929` | sweeps `inv.buyerId`, local-only | ✓ retrofit: also push touched Invoice if migrated |
| `migrateLineItemsToSupabase()` | `6005-6017` | sweeps `inv.lineItems[].lid`, local-only | ✓ retrofit |
| `migrateQteToSupabase()` | `6229-6230` | sweeps `inv.linkedQuoteId`, local-only | ✓ retrofit |
| `migratePOToSupabase()` | `6336-6339` | sweeps `inv.pos[]`, local-only | ✓ retrofit |
| `delPO(id)` | `8380-8387` | splices deleted PO id out of `Invoice.pos[]`, local-only | ✓ retrofit (push touched Invoice if migrated) |
| `backfillInvoicePOs()` | `2944-2963` | rebuilds `inv.pos[]` on every `initApp()` via bare `saveAll()` | ✓ retrofit: push each *changed* invoice if migrated |
| `repairCalcFields()` | `13529-13539` | strips stray `cnAmount`, runs on every `initApp()` | out of scope — cosmetic repair, no FK/id concern; local-only fix acceptable (mirrors how `executeDataCleanup()` needs no retrofit for any already-migrated entity, §1.8) |
| `runFPMMigration()` | `13496-13525` | one-time-only, guarded, hardcodes 4 legacy invoice numbers | out of scope — fires once ever, already fully executed on any real install |
| `executeDataCleanup()` | `9855-9909` | phantom-record filter | out of scope — a real Cloud-hosted row always has a real id, can never be "phantom" (same reasoning already accepted for every prior entity) |
| `advMergeBuyers()` | `11769-11795` | bulk-reassigns `inv.buyer`/`buyerId` across every matching invoice, **no per-record `syncEnt()` today** | ✓ retrofit: push every touched invoice via `persistInvChange(inv, true)` in a loop, one trailing refresh |
| Demo-data seeding | `4950-5047` | local-only by design | out of scope, matches `_demo` precedent |

### 1.4 Outward references (Invoice/CN → other entities) and their pre-flight implications

| Reference | Field | Already Cloud-eligible? |
|---|---|---|
| Buyer | `buyerId` (Invoice only; CN via `saveCN()` never sets it — §1.2) | ✓ (`REQ-CLOUD-001`) |
| Line Item | `lineItems[].lid` (Invoice only; can be `''` for a manual line) | ✓ (`REQ-CLOUD-002`) |
| Quote | `linkedQuoteId` | ✓ (`REQ-CLOUD-004`) |
| Purchase Order | `pos[]` (array) | ✓ (`REQ-CLOUD-005`) |
| Invoice (self) | CN's `linkedInvId` — **the first self-referential FK in this series**, one migrating record pointing at another row in the same migrating table | n/a — resolved within this migration's own two-pass insert, `linkedInvNum`-first (corrected, requirements-gate round 7 — blocking; `FIELD_MAPS.cn` never maps `linkedInvId` to Sheets, the same omission that caused the `PO.invId`/`payments.invId` divergence bugs rounds 5/6 found — see §1.6), §2 REQ-CLOUD-006b |

Every outward reference except the self-referential one already has a real Supabase table to check against — this REQ's precondition (§2, REQ-CLOUD-006a) is therefore the most demanding in the series: **four** prerequisite entity migrations, not one (PO needed only Supplier's).

### 1.5 Inward references (other entities/arrays → Invoice/CN) and remap implications

| Holder | Field | Remap needed on Invoice id change? |
|---|---|---|
| Purchase Order | `invId`/`invNum` | **Yes** — the mirror image of PO's own outward sweep into `Invoice.pos[]`. Requires the full "push touched PO via `persistPOChange()` if PO has migrated" cross-phase treatment, since PO already has a live Supabase table. |
| Credit Note (self) | `linkedInvId`/`linkedInvNum` | Yes, resolved within this migration's own two-pass insert, `linkedInvNum`-first (§1.4, corrected round 7). |
| `DB.payments[]` (Buyer Payment ledger — not Cloud-eligible, Phase 3 sub-phase 3, unscoped) | `invId` | **Yes, but local-only, and resolved `invNum`-first, not by a bare `invId` key lookup** (corrected, requirements-gate round 6 — blocking, correcting round 4's own "plain key-lookup" framing): a Sheets-sync-originated payment (`FIELD_MAPS.payments`, `4389`, never maps `invId`; `pullAll()`'s id-keyed merge, `4609-4635`/`4425-4433`) can carry a correct `invNum` but a blank `invId` — the same divergence risk round 5 found and fixed for `PO.invId`/`invNum`, confirmed live for payments by three existing `invId===...||invNum===...` fallback sites (`8100`, `8167`, `10044`). Resolved with the identical `invNum`-first, `invId`-fallback precedence as the now-fixed REQ-CLOUD-006f, using the same pre-migration `DB.inv` snapshot/`invIdMap`. Also covers the self-referencing goodwill-credit `DB.payments[]` entries (`saveInv()`/`saveCN()`, `7075-7078`/`10024-10028`, round 4) automatically, since those always carry a correct `invId` and `invNum` together at creation. |
| Shipment | `linkedInvs[]` | **No** — confirmed by direct reading (`11508`, `11524`, `11461`, `11664`) to be a comma-split, free-text, join-only field; nothing ever dereferences an element against `DB.inv`. No remap needed regardless of migration. |

### 1.6 Invoice-specific findings with no precedent among the 7 already-migrated entities

- **Two structurally different creation paths for one logical `type`** (§1.2) — no prior entity had two save functions producing materially different record shapes.
- **The first self-referential FK within a migrating table** (CN→Invoice) — no prior entity migration remapped a reference from one migrating record to another in the same batch.
- **The largest existing local-sweep-function retrofit set** — 4 inward sweeps (`migrateSuppliersBuyersToSupabase`/`migrateLineItemsToSupabase`/`migrateQteToSupabase`/`migratePOToSupabase`) plus `delPO()`'s splice, all already touching `DB.inv` as local-only fixes today, all needing the "push back if migrated" retrofit.
- **`advMergeBuyers()`** — a bulk operator tool with no per-record `syncEnt()` today; no prior entity's migration needed to retrofit a bulk cross-cutting tool like this.
- **`LOCKED_STATUSES`/`canTransitionStatus()`/`unlockInv()`** (`2850-2858`, `11714-11740`): `unlockInv()` is session-only/in-memory (`_unlockedInvIds`, cleared on reload), never mutates `DB.inv` directly. No Cloud-Data-specific interaction found, matching Order Request's own guarded-state-machine precedent (`ORD_TRANSITIONS`, already Cloud-migrated with no special handling needed) — re-confirmed, not assumed, at spec time (§4, AC-9).
- **The AI-assistant dispatch path needs no Cloud Data handling** (confirmed, requirements-gate round 1, mirroring the same "confirm, don't assume" bar applied to `unlockInv()` above — REQ-CLOUD-003's own round-1 finding was burned by exactly this class of gap, wrongly including `ordAddAction()` in its mutation inventory, so this series treats the AI path as worth stating explicitly, not silently assumed safe). `_aiExecTool()` (`10707-10819`) implements only read-only tools (`get_invoices`/`get_payments`/`get_kpis`/`get_suppliers`/`get_buyers`/`get_pos`) — no write tool touches `DB.inv`. `create_invoice`/`create_credit_note` (`10587-10625`) only pre-fill the `ov-inv`/`ov-cn` modal fields via `openInv()`/`openNewCN()`; the actual write still goes through `saveInv()`/`saveCN()` (§1.3, sites #1/#7) — already covered by REQ-CLOUD-006d.
- **Zero `logEv()` on CN create/edit**, and **`delInv()` mislabels a CN deletion as `"Invoice ... deleted"`** (`8242`, unconditional) — both confirmed live. Neither threatens migration correctness; both logged, not fixed, as `INV-GAP-002` (§3).
- **`delInv()` leaves dangling references** on delete: `PO.invId/invNum`, other CNs' `linkedInvId`, and `DB.payments[].invId` are never cleaned up (`Shipment.linkedInvs[]` needs no cleanup per §1.5, since nothing dereferences it). This is a pre-existing data-integrity gap, not created by this REQ — logged as `INV-GAP-002` alongside the logging gaps above, not fixed here (unlike `PO-GAP-007`, which `REQ-CLOUD-005` fixed opportunistically as a single-array-element case, this touches three separate reference kinds each needing its own cleanup design — large enough to warrant its own future REQ rather than folding into a migration REQ).
- **An in-flight, unshipped, adjacent REQ** (`REQ-INTEG-002-2c`) is actively redefining `getInvPayments()`'s matching semantics. This REQ does not call or modify that function — see §0's sequencing note.
- **A third instance of the `invId`/`invNum`-divergence bug class rounds 5 and 6 found for `PO.invId` and `payments.invId`, this time in the CN self-referential remap itself** (found, requirements-gate round 7 — blocking). `FIELD_MAPS.cn` (`4387`) maps `linkedInvNum` to Sheets but never `linkedInvId` — the identical omission pattern behind both prior bugs. `pullAll()`'s Credit Note block (`4545-4565`) matches by business key (`num`); for a CN genuinely new to the local app (e.g. added directly in the Sheet), `mergePulledWithLocal()`'s `if (!localMatch) return pulledMapped;` branch (`4426`) returns the pulled record with `linkedInvId` entirely absent as an own key, while `linkedInvNum` is correctly populated. Confirmed live, not theoretical, by four existing sites that already fall back to (or exclusively use) `linkedInvNum` when resolving a CN's linked invoice: `iCalc()`'s applied-CN filter (`4796`, `cn.linkedInvId===inv.id || cn.linkedInvNum===inv.num` case-normalized), `renderStatement()` (`8102`), `prevStmtPdf()` (`8168`), and `saveCN()`'s own balance-due recalc (`10037-10044`). `saveCN()`'s (`10018`) and `saveInv()`'s CN sub-mode's (`7024`) own live derivation of `linkedInvId` — a fresh `DB.inv.find(i => i.num === l)` lookup by `num` — only runs at save time through those functions; it never runs for a CN merged in by `pullAll()`, which bypasses both entirely. `'inv'`/`'cn'` remain in `pullAll()`'s live pull set throughout this migration's own precondition period, so this shape is squarely in scope, not a corner case outside the migration's window.
- **Goodwill-credit `DB.payments[]` entries are self-referencing, not cross-referencing** (found, requirements-gate round 4 — blocking). `saveInv()` (`7075-7078`) and `saveCN()` (`10024-10028`) each push a `DB.payments[]` entry with `invId` set to the *very record being saved* (`inv.id`/`cn.id`), unlike an ordinary buyer payment's `invId`, which references a different, already-existing invoice. §2 REQ-CLOUD-006g's local sweep of `DB.payments[].invId` must not special-case away or accidentally exclude this shape — it needs the identical `invIdMap` rewrite as any other payment, since the migrating record on the *other end* of a self-reference is still, definitionally, present in the same `invIdMap` this migration builds.
- **A Credit Note's `linkedInvId` is not type-constrained to a true Invoice** (found, requirements-gate round 4 — advisory). `saveCN()` (`10018`) and `saveInv()`'s CN sub-mode (`7024`) both resolve `linkedInvId` via `DB.inv.find(i => i.num === l)` with no `type` filter — so a CN's linked-invoice reference can, in the live data, resolve to *another CN* rather than a true Invoice. The two-pass remap (§2 REQ-CLOUD-006b) handles this correctly by construction, since `invIdMap` is built from every `DB.inv` record regardless of `type` — but the REQ's own prose and AC-4 describe/test only the "CN→Invoice" case, never a CN→CN reference, leaving this edge case mechanically covered but never explicitly acknowledged or tested.

---

## 2. Requirements

**REQ-CLOUD-006a — Combined migration precondition.** `migrateInvToSupabase()` refuses to run unless Buyer, Line Item, Quote, and Purchase Order have *all* completed their own migrations, each verified live against the currently-connected Supabase project (mirroring `isSupplierMigrationComplete()`'s pattern, not a local flag alone) — the first precondition in this series requiring four prerequisite entities rather than one or two.

**REQ-CLOUD-006b — Live pre-flight checks, plus the self-referential remap, resolved `linkedInvNum`-first (corrected, requirements-gate round 7 — blocking).** Before inserting anything: (1) every non-blank `buyerId` resolves against the live Buyer table; (2) every non-blank `lineItems[].lid` resolves against the live Line Item table; (3) every non-blank `linkedQuoteId` resolves against the live Quote table; (4) every element of `pos[]` resolves against the live Purchase Order table — any failure blocks the migration with a clear per-record error, mirroring `migrateLineItemsToSupabase()`'s/`migratePOToSupabase()`'s existing `knownXIdSet` pattern. The insert itself is two-pass: **pass 1** inserts every `DB.inv` record (Invoice and CN alike) with `linked_inv_id` deferred to `null`, building `invIdMap = {oldId: newId}` from each insert's returned row; **pass 2** updates every CN whose `linkedInvNum` **or** `linkedInvId` is non-blank (an earlier draft keyed only on `linkedInvId`, missing the identical divergence risk rounds 5/6 already found and fixed for `PO.invId`/`payments.invId` — see §1.6), resolving the target invoice against the *pre-migration* `DB.inv` snapshot with `linkedInvNum`-first precedence (exact `num` match, mirroring `saveCN()`'s/`saveInv()`'s own live derivation, `index.html:10018`/`7024`), falling back to `linkedInvId` only when `linkedInvNum` is blank or doesn't resolve — then setting `linked_inv_id = invIdMap[resolvedOldId]` both in Supabase and locally. A CN where neither field resolves to any record in `invIdMap` (a pre-existing dangling reference, per `INV-GAP-002`) is left `null` and logged, not treated as a blocking error — the CN's own row still migrates.

**REQ-CLOUD-006c — Pre-flight duplicate-number scan.** Before inserting, scan the *entire* local `DB.inv` array (Invoice and CN together, matching how `vInv()`'s/`saveCN()`'s own duplicate checks already operate) for any duplicate `num` — block the migration with a clear listing if found, mirroring PO's/Quote's own precedent. `pullAll()`'s bulk merge (§1.3) is confirmed to bypass client-side duplicate checks entirely, so this pre-flight scan cannot assume local data is already clean.

**REQ-CLOUD-006d — `saveInv()`/`saveCN()` gain their own create-or-update Cloud Data branches**, mirroring `saveLI()`/`saveCon()`'s shape (a dedicated branch, not `persistInvChange()`, since only these two functions handle the create path).

**REQ-CLOUD-006e — New shared `persistInvChange(inv, skipRefresh)` helper**, mirroring `persistPOChange()`/`persistQteChange()`/`persistOrdChange()` exactly, wired into every site that mutates an *already-existing* invoice without itself creating one (corrected, requirements-gate round 1 — an earlier draft split this into two inconsistent lists, an "eight-site" §006e list and a separately-mechanism'd "five-site retrofit" §006h, with no stated reason the five would behave differently; both are the identical "push this touched invoice back to Supabase if Invoice has migrated" operation and are consolidated into one exhaustive list here). **Thirteen call sites in total, the most of any entity in this series** (PO needed three):
1. `saveInvApprove()` (`7220-7239`)
2. `saveInvProgress()` (`7262-7276`)
3. `autoPos()` (`7278-7320`)
4. `savePayment()` (`13043-13061`)
5. `deletePayment()` (`13095-13114`ish)
6. `advMergeBuyers()` (`11769-11795`, looped — `skipRefresh=true` per iteration plus one trailing refresh)
7. `delPO()`'s splice of a deleted PO id out of `Invoice.pos[]` (`8380-8387`)
8. `backfillInvoicePOs()`'s per-changed-invoice save (`2944-2963`)
9. `saveCN()`'s side-mutation of a *different* invoice's `calc_balanceDue` (`10036-10048`) — pushes that other touched invoice, distinct from `saveCN()`'s own create-or-update branch (§006d) for the CN record itself
10. `migrateSuppliersBuyersToSupabase()`'s existing local sweep of `inv.buyerId` (`5919-5929`) — the sweep's own field-rewrite logic is unchanged; this adds the push on top of it
11. `migrateLineItemsToSupabase()`'s existing local sweep of `inv.lineItems[].lid` (`6005-6017`) — same treatment
12. `migrateQteToSupabase()`'s existing local sweep of `inv.linkedQuoteId` (`6229-6230`) — same treatment
13. `migratePOToSupabase()`'s existing local sweep of `inv.pos[]` (`6336-6339`) — same treatment

**REQ-CLOUD-006f — Outward retrofit into Purchase Order, matching algorithm fully specified (corrected, requirements-gate round 5 — blocking).** An earlier draft left ambiguous which field(s) determine a PO's "home" invoice and which field(s) get rewritten — a real gap, not a hypothetical one, since `PO.invId`/`invNum` are documented to genuinely diverge in this codebase (`PO-GAP-004`, open, `docs/known-gaps.md`: `savePO()` can silently clear `invId` while leaving `invNum` stale; the CSV/Sheets PO-import paths, `index.html:9319`/`9666`, set `invNum` independently of whatever `invId` resolves to). Left unspecified, a SPEC author implementing this by analogy to REQ-CLOUD-006g's plain `invId`-only key lookup would silently skip any PO whose `invId` is already blank/stale but whose `invNum` correctly identifies the migrating invoice — leaving that PO's `invId` permanently unset post-migration. `migrateInvToSupabase()` instead resolves each PO's home invoice using the exact precedence `backfillInvoicePOs()` already established for this identical divergence risk (`index.html:2947-2948`): **`invNum` match first, `invId` fallback only when `invNum` is blank or doesn't resolve.** For each PO resolved to a migrating invoice this way, rewrite `po.invId = invIdMap[resolvedInvoice.oldId]` (`po.invNum` is left unchanged — invoice numbers don't change under migration) and — since PO already has a live Supabase table — push each touched PO via `persistPOChange()` when PO has migrated (always true here, since REQ-CLOUD-006a requires it as a precondition), the mirror image of `REQ-CLOUD-005`'s own outward sweep into `Quote.linkedPOIds[]`.

**REQ-CLOUD-006g — Local-only inward sweep of `DB.payments[].invId`, resolved `invNum`-first (corrected, requirements-gate round 6 — blocking).** An earlier draft specified this as "a plain key-lookup against `invIdMap`" — safe for the goodwill-credit self-reference case (round 4), but this framing missed that `DB.payments` has the *identical* `invId`/`invNum` divergence risk round 5 found and fixed for `DB.po`, via a different but equally live mechanism: `FIELD_MAPS.payments` (`index.html:4389`) never maps `invId` to Sheets, so a payment row added directly in the Sheet (or otherwise genuinely new to the local app) enters `DB.payments` via `pullAll()`'s id-keyed merge (`index.html:4609-4635`) with a correct `invNum` but an **undefined `invId`** (`mergePulledWithLocal()`, `index.html:4425-4433`, returns the pulled record verbatim when no local match exists, and `FIELD_MAPS.payments` never included `invId` in what was pulled). This is confirmed live, not theoretical: three existing sites already defend against exactly this by falling back to `invNum` when matching payments to an invoice — `renderStatement()` (`index.html:8100`), `prevStmtPdf()` (`index.html:8167`), and `saveCN()`'s balance-due recalc (`index.html:10044`) — the identical historical-ad-hoc-filter shape this REQ's own §0/§1.4 already cites as the pre-`backfillInvoicePOs()` PO precedent. (Contrast `migratePOToSupabase()`'s own bare-key sweep of `DB.supPayments[].poId`, `index.html:6341` — safe there specifically because that function's own comment, `6324-6326`, confirms `.poNum` is "confirmed unused for lookup anywhere"; no such confirmation holds for `payments.invNum`, and the three sites above prove the opposite.) `migrateInvToSupabase()` therefore resolves each `DB.payments[]` record's home invoice with the same precedence as REQ-CLOUD-006f's now-fixed PO sweep — `invNum` match first (against the pre-migration `DB.inv` snapshot), `invId` fallback only when `invNum` is blank or doesn't resolve — then rewrites `p.invId = invIdMap[resolvedInvoice.oldId]`. This still automatically covers the self-referencing goodwill-credit entries `saveInv()`/`saveCN()` push (§1.3, §1.6, round 4), since those always carry both a correct `invId` and `invNum` at creation and resolve via either path identically.

**REQ-CLOUD-006h — (removed, requirements-gate round 1) — folded into REQ-CLOUD-006e's unified 13-site list above**, since sites #10-13 there are exactly what this requirement used to describe separately as a "retrofit."

**REQ-CLOUD-006i — `pullAll()` exclusion gate, both halves.** PO's own exclusion (the actual precedent to mirror, not just its oft-cited block guard) is two parts working together: the `_allPullKeys` request-list filter (`index.html:4510-4511`, `var _allPullKeys = ['inv','cn','po'].concat(_simpleEntsForBatch); if (_sb && localStorage.getItem('st_po_cloud_migration_ts')) _allPullKeys = _allPullKeys.filter(function(e){ return e !== 'po'; });`) *and* the per-block merge guard (`4574`, `if (!(_sb && localStorage.getItem('st_po_cloud_migration_ts'))) { ... }`) — re-confirmed correct a third time, directly against a worktree pinned to this REQ's own stated commit, after two prior review rounds each independently misread these same two numbers from a stale copy of the file (see §8 round 3). `docs/requirements-tracker.md`'s own `REQ-CLOUD-005` row records that spec-gate round 2 specifically caught an implementation that added only the block guard and never the `_allPullKeys` filter — the identical mistake this REQ must not repeat. `'inv'`/`'cn'` are today hardcoded unconditionally into `_allPullKeys`'s literal (`4510`) alongside `'po'`; this REQ adds the same conditional filter for both, plus both blocks' own per-block guards (`4524-4543`, `4545-4565`), gated on one shared `st_inv_cloud_migration_ts` marker since Invoice and Credit Note are one migration.

**REQ-CLOUD-006j — New `refreshInvFromSupabase()`**, wired into `initCloudDataLayer()` with the standard non-destructive guard (refuse to overwrite real local data unless this device has migrated or there is nothing local to lose), matching every prior entity's refresh function.

**REQ-CLOUD-006k — Settings → Cloud Data UI card** for Invoice/Credit Note, matching the existing per-entity card pattern (migrate button, restore-from-archive button, 30-day archive/rollback).

**REQ-CLOUD-006l — CSV import stays local-only, deferred (corrected — requirements-gate round 1 found the cited precedent doesn't exist; round 2 found the corrected version still overstated it).** An earlier draft of this REQ claimed `processImport()`/`processImportRecords()`'s `'inv'` branches would gain a Cloud Data path "mirroring how PO's own CSV import was handled in `REQ-CLOUD-005`" — but PO's own CSV import was never given Cloud Data handling at all: `docs/REQ-CLOUD-005-v1.md` §3 explicitly put it out of scope. Round 1's fix corrected this to "logged both of PO's CSV branches under `CLOUD-GAP-003`" — **but that is also inaccurate**: `docs/known-gaps.md`'s actual `CLOUD-GAP-003` entry is titled "`processImport()`'s CSV import branches bypass Cloud Data for Supplier, Line Item, and Contact" and its own **Area** line names only `'sup'`/`'li'`/`'co'`/`'ord'`-Contact — Purchase Order is not mentioned anywhere in `known-gaps.md` under any gap number. `REQ-CLOUD-005-v1.md` §3 only *committed to* logging PO's CSV bypass under `CLOUD-GAP-003` at ship time; its own §7 ship-time checklist never actually did so, and it was never entered. `processImport()`'s and `processImportRecords()`'s `'inv'` branches (`9250-9298`, `9571-9636`) stay local-only, unchanged, matching the *actual* (not the falsely-cited) precedent — every entity's CSV import path in this series is local-only until each gets its own dedicated fix. At ship time (§7), this REQ's own `known-gaps.md` update both adds the two new Invoice/CN instances **and** the still-missing Purchase Order instance to `CLOUD-GAP-003`, broadening its title/scope to cover all four entity families (Supplier/Line Item/Contact/Purchase-Order) rather than compounding the same "committed to log, never logged" gap a second time.

---

## 3. Out of scope

- **Sub-phase 2 (Shipment)** and **sub-phase 3 (Buyer Payment, Supplier Payment)** — separate, later REQs (§0).
- **`CN-GAP-001`** (new, logged from §1.2): `saveCN()`'s dedicated modal enforces no number-format regex, unlike `saveInv()`'s CN sub-mode — a pre-existing validation asymmetry between the two CN creation paths, unrelated to Cloud Data, not fixed here.
- **`INV-GAP-002`** (new, logged from §1.6): `delInv()` leaves `PO.invId/invNum`, other CNs' `linkedInvId`, and `DB.payments[].invId` dangling on delete, and CN create/edit never calls `logEv()` while `delInv()` mislabels a CN deletion as an "Invoice" in the log — four related, pre-existing gaps, none created by this REQ, large enough to warrant a dedicated future REQ rather than folding into a migration REQ.
- **`CLOUD-GAP-002`** (push-side sync has no Cloud-Data exclusion for any entity) — pre-existing, unaffected by this REQ, same as every prior sub-phase.
- **CSV import Cloud Data support for Invoice/Credit Note** (corrected, requirements-gate rounds 1-2 — §2 REQ-CLOUD-006l): both `processImport()`'s and `processImportRecords()`'s `'inv'` branches stay local-only, matching the same local-only status Purchase Order's own CSV import already has (never fixed, and — contrary to an earlier draft's claim — never actually logged either) — this REQ's own ship-time `known-gaps.md` update (§7) newly logs both the two Invoice/CN instances and the still-missing Purchase Order instance under `CLOUD-GAP-003`, not built here.
- **Correcting `docs/architecture-data-model-v1.md`'s stale "wrong Sheets tab" claim** (§1.1) — a documentation fix, tracked in §7, not a code change.
- **Any change to `REQ-INTEG-002-2c`'s `getInvPayments()` matching logic** — this REQ only rewrites `DB.payments[].invId` *values*; it does not touch the matching function itself (§0).
- **The `v3.0.0` multi-tenant rebuild** (`docs/v3-architect-handoff.md`) — a separate, unscoped future initiative; this REQ's schema is expected to need redesign for tenant isolation later, an accepted tradeoff already priced into every prior Cloud Data sub-phase (§0).

---

## 4. Acceptance criteria

- **AC-1.** `migrateInvToSupabase()` refuses to run unless all four prerequisite entities (Buyer, Line Item, Quote, Purchase Order) show complete, live-verified migrations — demonstrated with each of the four missing in turn.
- **AC-2.** The four live pre-flight checks (§2b) each block migration on an unresolvable `buyerId`/`lineItems[].lid`/`linkedQuoteId`/`pos[]` element — demonstrated one per field.
- **AC-3.** The pre-flight duplicate-`num` scan blocks migration on a local duplicate spanning an Invoice and a CN sharing one number, not just two Invoices.
- **AC-4.** The two-pass self-referential remap correctly resolves a CN's `linkedInvId`/`linkedInvNum` to its linked invoice's new Supabase id via `linkedInvNum`-first resolution (corrected, requirements-gate round 7), regardless of insertion order (CN before its linked invoice in the local array, and after) — both orders demonstrated — and leaves a genuinely dangling reference (neither field resolving to any current record) as `null` without blocking the migration. **AC-4a (new, requirements-gate round 4 — advisory).** The same remap correctly resolves a CN whose `linkedInvId` points at *another CN* rather than a true Invoice, demonstrating the type-agnostic `invIdMap` handles this unconstrained-reference case (§1.6) without special-casing. **AC-4b (new, requirements-gate round 7 — blocking).** Demonstrated specifically for a Sheets-sync-originated CN merged via `pullAll()`'s genuinely-new-record path (blank/undefined `linkedInvId`, correct `linkedInvNum`): the CN is still correctly resolved to its true linked invoice via the `linkedInvNum` match, and `linked_inv_id` is set to that invoice's new Supabase id — not silently left `null` for lacking a resolvable `linkedInvId`.
- **AC-5.** `saveInv()`/`saveCN()` each correctly route through their own create-or-update Cloud Data branch (§2d) when Invoice has migrated, and are demonstrated unaffected (today's exact local-only behavior) when it has not.
- **AC-6.** `migrateInvToSupabase()` sweeps `PO.invId` outward using `backfillInvoicePOs()`'s own `invNum`-first, `invId`-fallback resolution and pushes each touched PO via `persistPOChange()`. **AC-6a (new, requirements-gate round 5 — blocking).** Demonstrated specifically for a PO whose `invId`/`invNum` disagree (a stale `invId` alongside a correct `invNum`, per `PO-GAP-004`'s documented divergence shape): the PO is still correctly resolved to its true source invoice via the `invNum` match, and its `invId` is rewritten to that invoice's new Supabase id — not silently skipped for having a stale `invId`.
- **AC-7.** `migrateInvToSupabase()` sweeps `DB.payments[].invId` locally, resolving each payment's home invoice `invNum`-first with `invId` fallback (corrected, requirements-gate round 6 — blocking), for every affected Buyer Payment record, **including a self-referencing goodwill-credit entry** (corrected, requirements-gate round 4 — blocking, new sub-case): a goodwill-credit `DB.payments[]` record whose `invId` equals the migrating credit's own (pre-migration) `DB.inv` id is demonstrated to have that `invId` correctly rewritten to the credit's new Supabase id, not silently skipped for lacking a "real" cross-reference shape. **AC-7a (new, requirements-gate round 6 — blocking).** Demonstrated specifically for a Sheets-sync-originated payment with a blank/undefined `invId` but a correct `invNum` (simulating `pullAll()`'s genuinely-new-record merge path, `4425-4433`): the payment is still correctly resolved to its true invoice via the `invNum` match, and its `invId` is set to that invoice's new Supabase id — not silently skipped for lacking a resolvable `invId` to key on.
- **AC-8 (corrected, requirements-gate round 1 — consolidated to match §2e's unified list).** All 13 `persistInvChange()` call sites — `saveInvApprove()`, `saveInvProgress()`, `autoPos()`, `savePayment()`, `deletePayment()`, `advMergeBuyers()`, `delPO()`'s splice, `backfillInvoicePOs()`, `saveCN()`'s different-invoice side-push, and the four existing local sweep functions' retrofits — each push a touched Invoice back to Supabase when Invoice has migrated, individually demonstrated, and are demonstrated unaffected when it has not (matching each site's existing, already-tested local-only behavior).
- **AC-9.** `unlockInv()`/`canTransitionStatus()` are confirmed, by an explicit test against the real functions (not by inspection alone), to need no Cloud-Data-specific handling — closing the one open question §1.6 flagged as not definitively resolved during check-first.
- **AC-10.** `pullAll()` drops both the Invoice and Credit Note blocks once `st_inv_cloud_migration_ts` is set.
- **AC-11.** `refreshInvFromSupabase()` refuses to overwrite real local data absent a completed migration or existing local data, matching every prior entity's refresh function.
- **AC-12 (corrected, requirements-gate round 1).** CSV import (`processImport()`/`processImportRecords()`) is demonstrated **unaffected** by this REQ — both `'inv'` branches remain local-only pre- and post-migration, matching today's exact behavior (no Cloud Data path exists to test, per the corrected §2 REQ-CLOUD-006l/§3).
- **AC-13.** Full pre-existing-test-suite audit: every direct test-suite call site of a mutation function this REQ touches is traced and confirmed either unaffected or correctly updated.

---

## 5. Testing approach

Mirrors `REQ-CLOUD-005`'s own test coverage shape (the most structurally similar prior entity: multiple creation paths, a combined precondition, both an outward and several inward sweeps) rather than inventing a new pattern. The self-referential remap (AC-4) is the one genuinely novel test shape in this series and needs its own dedicated fixture pair (a CN and its linked invoice, tested both insertion orders).

---

## 6. Gate process

Standard pipeline: this REQ → independent requirements-gate review (Agent) → SPEC-CLOUD-006 (exact diffs, including the SQL migration `supabase/migrations/0006_invoices.sql`) → independent spec-gate review (Agent, applies diffs to a scratch copy, runs the real test suite) → implementation → self-directed mutation testing → independent build-gate review (Agent) → PR → CI green → merge → verify main consistent.

---

## 7. Tracker updates (at ship time)

- `docs/requirements-tracker.md` — new `REQ-CLOUD-006` row.
- `docs/known-gaps.md` — new `CN-GAP-001` and `INV-GAP-002` entries (§3); `CLOUD-GAP-003` updated to add the two new Invoice/CN CSV-import instances *and* the still-missing Purchase Order instance (corrected per §2 REQ-CLOUD-006l, requirements-gate round 2 — `REQ-CLOUD-005` committed to logging PO's instance but never actually did), broadening its title/Area line to cover all four entity families; correct §6.6's now-stale "wrong Sheets tab" claim in `docs/architecture-data-model-v1.md` to reflect §1.1's finding.
- `docs/version-history.md`/`STACKD_CONTEXT.md`/`CLAUDE.md` — version bump, test count, a note on Invoice/CN joining Cloud Data and the four-entity precondition.
- `docs/user-guide.md` — Settings → Cloud Data section updated for the new Invoice/Credit Note card.

---

## 8. Review-resolution log

**Round 1: CONDITIONAL PASS — 3 blocking, 4 advisory, all fixed in place.** The reviewer spot-checked 20+ line citations across §1.1-§1.6 (all confirmed byte-exact), independently grepped every `DB.inv` occurrence to verify §1.3's mutation-site table completeness, and checked the AI-assistant dispatch path, `known-gaps.md`, and `requirements-tracker.md` for the gap-number and precedent claims. Every outward-reference table entry, `Shipment.linkedInvs[]`'s free-text-only shape, `pullAll()`'s lack of an existing exclusion, the two new gap numbers, and the self-referential remap design were all independently confirmed accurate.

Blocking:
- **REQ-CLOUD-006l cited a precedent that doesn't exist.** An earlier draft claimed CSV import Cloud Data support would "mirror how PO's own CSV import was handled" — but PO's own CSV import was never given Cloud Data handling at all; `REQ-CLOUD-005` explicitly deferred both of PO's CSV branches under `CLOUD-GAP-003`, confirmed live (neither branch contains any `_sb` reference). Fixed by following the actual precedent: Invoice/CN's CSV import branches stay local-only, logged as two new `CLOUD-GAP-003` instances, not built here (§2 REQ-CLOUD-006l, §3, AC-12 all corrected).
- **Inconsistent `persistInvChange()` call-site count** — an earlier draft split call sites into an "eight-site" §006e list and a separately-mechanism'd "five-site retrofit" §006h, with `delPO()` appearing in both and no stated reason the five would behave differently from the eight; `saveCN()`'s side-push of a different invoice's `calc_balanceDue` was also excluded from both counts. Fixed by consolidating into one exhaustive 13-site list under REQ-CLOUD-006e, removing 006h as a separate requirement, and updating AC-8 to match.
- **REQ-CLOUD-006i cited only half of PO's real exclusion mechanism**, repeating a defect this codebase's own history already caught once: PO's actual exclusion is the `_allPullKeys` request-list filter (`4510-4511`) *and* the per-block merge guard (`4574`) working together — `docs/requirements-tracker.md`'s own `REQ-CLOUD-005` row records that spec-gate round 2 caught an implementation that added only the block guard and never the array filter. Fixed by requiring both mechanisms explicitly, with exact citations, for both the Invoice and Credit Note blocks.

Advisory (fixed in place, didn't block the verdict):
- §1.2's "`vCN()`/`saveCN()` each run their own independent duplicate-`num` scan" was imprecise — only `saveCN()` does; `vCN()` has no duplicate check at all. Wording left as-is since it doesn't affect any requirement (the reviewer flagged it as non-blocking); noted here for completeness.
- AC-4's insertion-order clause tests a property the two-pass design guarantees structurally regardless of implementation correctness — left in place since it's not wrong, just non-discriminating; the load-bearing clause (a genuinely dangling `linkedInvId`) was already present.
- REQ-CLOUD-006k (Settings UI card) has no dedicated AC — matches an identical, pre-existing gap in the already-shipped `REQ-CLOUD-005` (005k also has none), an inherited series pattern rather than new to this REQ; left as-is.
- The REQ never explicitly stated the AI-assistant dispatch path (`_aiExecTool`, `create_invoice`/`create_credit_note`) needs no Cloud Data handling, despite `REQ-CLOUD-003`'s own round-1 history being burned by exactly this class of gap. Fixed by adding an explicit statement to §1.6, confirming (not assuming) the underlying fact was already fine.

Ready for SPEC, pending independent re-verification in a future round if one is dispatched.

**Round 2: CONDITIONAL PASS — 1 new blocking finding, 1 advisory, fixed in place; 1 candidate finding investigated and rejected as a false positive.** Round 1's consolidated 13-site `persistInvChange()` list (finding b) was independently re-verified site-by-site against the live code and holds up completely — every site's line range, "mutates existing rather than creates" property, and non-duplication was confirmed. The `a` through `l` lettering (with `h` explicitly marked removed/folded into `e`) was confirmed coherent with no dangling references.

New blocking:
- **Round 1's own CSV-import fix (finding a) replaced one false precedent with another.** Round 1 corrected "mirrors how PO's own CSV import was handled" to "logged both of PO's CSV branches under `CLOUD-GAP-003`" — but `docs/known-gaps.md`'s actual `CLOUD-GAP-003` entry only covers Supplier/Line Item/Contact; Purchase Order is not mentioned in it or under any other gap number. `REQ-CLOUD-005-v1.md` §3 only *committed to* logging PO's instance at ship time; its own §7 checklist never carried that through. Fixed by correcting the claim a second time (this REQ's own local-only decision for Invoice/CN was never in question, only the false "already logged" premise) and by having this REQ's own §7 ship-time update add both the two new Invoice/CN instances *and* the still-missing PO instance to `CLOUD-GAP-003`, closing the gap this REQ's own drafting process surfaced rather than repeating it a third time.

Advisory (fixed in place, didn't block the verdict):
- §1.3's header claimed "24 found" against a table of 23 rows — corrected to 23.

Investigated and rejected as a false positive **(re-confirmed correct a third time in round 3, after round 3 initially appeared to contradict this — see below):**
- The round-2 review candidate-flagged REQ-CLOUD-006i's line citations (`4510-4511`, `4574`) as off-by-one, proposing `4509-4510`/`4573` instead. Independently re-checked via `grep -n` against `index.html` and confirmed the existing citations were already correct.

Ready for SPEC, pending independent re-verification in a future round if one is dispatched.

**Round 3: raised the identical `pullAll()` candidate finding round 2 had already raised — investigated a third time, this time definitively, and confirmed the original citations were correct all along; root cause of the whole back-and-forth found and fixed document-wide.** Round 3 independently re-derived the `pullAll()` line numbers and reached `4509-4510`/`4573`, matching round 2's original (already-rejected) claim. Re-investigating with maximum rigor — multiple independent tools (`grep -n`, `awk`, `sed -n` with explicit line counting), run directly against `/tmp/cloud-006-wt/index.html` (a dedicated git worktree checked out on this REQ's own branch at its own commit, `93369ec`, itself built on the REQ's stated baseline `main`@`c4242e2`), repeated twice after an initial mistaken edit — conclusively confirmed the citations were **`4510-4511`/`4574` all along, exactly as originally written**. Both round 2's original flag and round 3's own re-derivation had been checking `/home/user/stackd-ops/index.html` instead — a shared working directory that, for both rounds, happened to be checked out on an unrelated in-flight branch (`claude/integ-002-2c-buyer-payments`) one merge behind `main`, missing `REQ-LI-001`'s handful of small insertions to `index.html`. `REQ-LI-001` adds exactly 4 net lines at three insertion points (after old-line 3896, old-line 8736, and old-line 8816); code at old-line ≥3897 shifts by +1 to +4 depending on position, which is exactly the 1-line discrepancy both rounds independently (and wrongly) "confirmed."

This also explained a second, real, separate problem worth fixing in its own right: this REQ's own original check-first research pass had unknowingly been run against that same stale, pre-`REQ-LI-001` file — not the `main`@`c4242e2` baseline the REQ claims — so every citation for code after old-line 3896 potentially needed re-verification, not just the two `pullAll()` ones already in dispute.

Blocking (of the underlying process, not this REQ's own content):
- **A dedicated citation-accuracy pass re-checked all ~42 distinct line citations in the document directly against the correct worktree.** 44 individual citation instances (some cited twice) were genuinely wrong, all explained by the same systematic offset, and have been corrected in place throughout §1.1-§1.6 and §2 — every correction is reflected directly in the citations as they now stand. Two citations were investigated, found already correct, and deliberately left untouched: `backfillInvoicePOs()` (`2944-2963`) and `LOCKED_STATUSES`/`canTransitionStatus()` (`2850-2858`), both located before `REQ-LI-001`'s first insertion point and therefore unaffected by the offset. The `pullAll()` exclusion-gate citations (`4510-4511`/`4574`) needed no correction — confirmed correct, for real this time, per above.

Advisory (fixed in place):
- §1's own header understated what "against `main`@`c7a80ed`" actually meant — corrected to disclose that this was an unintentional stale-file read, not a deliberate historical baseline note, per the correction now at the top of §1.

**Process lesson for future review dispatches on this REQ (and any other):** never instruct a reviewer to treat `/home/user/stackd-ops/index.html` as if it reflects a specific target commit — that directory is shared, mutable state actively repointed across branches throughout this session. Always pin review agents to a dedicated worktree or `git show <branch>:<path>` for the exact commit under review.

Ready for SPEC, pending independent re-verification in a future round if one is dispatched.

**Round 4: CONDITIONAL PASS — 1 blocking, 1 advisory, both fixed in place.** Working in a dedicated, correctly-pinned worktree (confirmed against the exact commit under review, 13702-line `index.html`), the reviewer independently spot-checked 35 distinct citations across §1.1-§1.6 and §2, including every citation named across all three prior rounds' findings and several never previously singled out — zero further citation errors found, closing the citation-accuracy saga for good. The reviewer then did a substantive design/content pass, since rounds 1-3 had spent most of their effort on citation accuracy.

Blocking:
- **Goodwill-credit `DB.payments[]` entries are self-referencing, not cross-referencing, and REQ-CLOUD-006g's original wording didn't make clear this shape is covered.** `saveInv()` (`7075-7078`) and `saveCN()` (`10024-10028`) each push a payment-ledger entry whose `invId` is the *very record being saved*, not a different invoice — a materially different shape from an ordinary buyer payment. Left ambiguous, a spec-time implementation reading "Buyer Payment record referencing a migrating invoice" narrowly could plausibly skip these, leaving them pointing at stale local ids after migration and silently breaking `getInvPayments()`'s lookup for every goodwill credit. Fixed by making REQ-CLOUD-006g's type-agnostic, plain-key-lookup nature explicit, adding the goodwill push to §1.3's `saveInv()`/`saveCN()` rows and a new §1.6 finding, and adding an explicit AC-7 sub-case testing this exact scenario.

Advisory (fixed in place, didn't block the verdict):
- A Credit Note's `linkedInvId` is not type-constrained to a true Invoice at the code level (`saveCN()`/`saveInv()`'s CN sub-mode both resolve it via an unfiltered `DB.inv.find()`) — so a CN→CN reference is possible in live data. The two-pass remap already handles this correctly by construction (its `invIdMap` covers every `DB.inv` record regardless of `type`), but the REQ's own prose and AC-4 only described the CN→Invoice case. Fixed by adding an explicit acknowledgment to §1.6 and a new AC-4a sub-case.

Ready for SPEC, pending independent re-verification in a future round if one is dispatched.

**Round 5: CONDITIONAL PASS — 1 blocking finding.** Re-verified round 4's two fixes are correctly integrated with no contradictions across §1.3/§1.5/§1.6/§2g/AC-4a/AC-7, and spot-checked the two new citations round 4 introduced (`7075-7078`, `10024-10028`) plus 8 others — all exact, closing out any remaining doubt about citation accuracy. Traced, rather than assumed, that only CN records carry a self-referential field (confirmed via `openInv()`/`editInv()`'s actual DOM lifecycle — a true Invoice's `linkedInvId` field is never populated by any live code path). Traced the duplicate-number-scan/self-referential-remap interaction for a historical-corruption edge case and found it already covered by existing handling, no new gap.

Blocking:
- **REQ-CLOUD-006f (the outward sweep into Purchase Order) left its `invId`/`invNum` matching algorithm unspecified — the identical ambiguity class this REQ's own §0 flags as a live risk in this codebase (`REQ-INTEG-002-2c`'s own multi-round struggle with this exact problem for payments).** Not hypothetical: `PO-GAP-004` (open, `docs/known-gaps.md`) documents that `savePO()` can silently clear `invId` while leaving `invNum` stale, and the CSV/Sheets PO-import paths set `invNum` independently of `invId`. Left unspecified, a SPEC author implementing this by analogy to REQ-CLOUD-006g's plain `invId`-key lookup would silently skip a PO with a stale `invId` but a correct `invNum`. Fixed by fully specifying the resolution as `backfillInvoicePOs()`'s own already-established precedence (`index.html:2947-2948`) — `invNum` match first, `invId` fallback only when `invNum` doesn't resolve — and clarifying only `po.invId` is rewritten (`po.invNum` is unaffected by migration). New AC-6a tests the disagreement case directly.

Ready for SPEC, pending independent re-verification in a future round if one is dispatched.

**Round 6: CONDITIONAL PASS — 1 blocking finding.** Independently re-verified round 5's fix as correct and complete — `backfillInvoicePOs()`'s cited precedence (`2947-2948`) matches the live code exactly, word for word. Confirmed no contradictions across the full accumulated document (§1.3/§1.5/§1.6/§2/§4/§8) after 5 rounds of patches.

Blocking:
- **REQ-CLOUD-006g's `DB.payments[].invId` sweep had the identical unaddressed matching-ambiguity round 5 had just fixed for PO — one array over, via a different but equally live mechanism.** Round 4 had framed the sweep as "a plain key-lookup against `invIdMap`," which is safe for the self-referencing goodwill-credit case that round found, but never re-examined whether `DB.payments` shares `DB.po`'s `invId`/`invNum` divergence risk — it does: `FIELD_MAPS.payments` (`4389`) never maps `invId` to Sheets, so a payment row genuinely new to the local app enters `DB.payments` via `pullAll()`'s id-keyed merge (`4609-4635`/`4425-4433`) with a correct `invNum` but an undefined `invId`. Confirmed live, not theoretical, by three existing sites that already fall back to `invNum` when matching payments to an invoice (`renderStatement()` `8100`, `prevStmtPdf()` `8167`, `saveCN()`'s balance-due recalc `10044`) — the same historical ad-hoc-filter shape this REQ's own §1.4 already cites as the pre-`backfillInvoicePOs()` PO precedent. (By contrast, `migratePOToSupabase()`'s own bare-key sweep of `DB.supPayments[].poId` is genuinely safe, per that function's own comment explicitly confirming `.poNum` is "unused for lookup anywhere" — no such confirmation exists for `payments.invNum`, and the three sites above prove the opposite.) Fixed by resolving each payment's home invoice with the same `invNum`-first, `invId`-fallback precedence as the now-fixed REQ-CLOUD-006f, updating §1.5's table row and REQ-CLOUD-006g's text accordingly. New AC-7a tests a Sheets-sync-originated payment with a blank `invId` but correct `invNum` directly.

Ready for SPEC, pending independent re-verification in a future round if one is dispatched.

**Round 7: CONDITIONAL PASS — 1 blocking finding (a third instance of the same bug class).** Re-verified round 6's fix is correctly and consistently integrated across §1.5/§2g/AC-7/AC-7a, and re-confirmed round 5's `backfillInvoicePOs()` citation matches the live code word-for-word. Given the pattern of the same bug found independently at two different sweep sites in consecutive rounds, this round specifically hunted for a third instance before doing a normal pass.

Blocking:
- **The CN self-referential remap (REQ-CLOUD-006b) had the identical unaddressed `linkedInvId`/`linkedInvNum` divergence rounds 5 and 6 already found and fixed for `PO.invId` and `payments.invId` — the same root mechanism, a third time.** `FIELD_MAPS.cn` (`4387`) maps `linkedInvNum` to Sheets but never `linkedInvId` — the identical omission that caused both prior bugs. `pullAll()`'s Credit Note block (`4545-4565`) matches by business key (`num`); for a CN genuinely new to the local app, `mergePulledWithLocal()`'s `if (!localMatch) return pulledMapped;` branch (`4426`) returns the pulled record with `linkedInvId` entirely absent while `linkedInvNum` is correctly populated. Confirmed live by four existing sites that already fall back to or exclusively use `linkedInvNum` when resolving a CN's linked invoice: `iCalc()`'s applied-CN filter (`4796`), `renderStatement()` (`8102`), `prevStmtPdf()` (`8168`), and `saveCN()`'s own balance-due recalc (`10037-10044`). `saveCN()`'s/`saveInv()`'s own live derivation of `linkedInvId` from `linkedInvNum` (`10018`/`7024`) only runs through those save functions, never for a CN merged in by `pullAll()`, which bypasses both. Fixed by resolving pass 2's target with the same `linkedInvNum`-first, `linkedInvId`-fallback precedence as REQ-CLOUD-006f/g, updating §1.4/§1.5's self-reference rows and adding a new §1.6 finding. New AC-4b tests a Sheets-sync-originated CN with blank `linkedInvId` but correct `linkedInvNum` directly.

Ready for SPEC, pending independent re-verification in a future round if one is dispatched.

**Round 8: PASS — clean, no blocking or advisory findings.** The first non-conditional pass in this REQ's series. Two parts.

Part A hunted for a possible 4th instance of the recurring bug class, this time in the *reverse* direction: does `FIELD_MAPS.inv` (`4386`) omitting Invoice's own outward references — `buyerId`, `lineItems[].lid`, `linkedQuoteId`/`linkedQuoteNum`, `pos[]` — create the same exploitable divergence the prior three findings did? Confirmed no, for three distinct reasons, each verified directly against live code rather than assumed:
- `pos[]` is safe because `backfillInvoicePOs()` (`2944-2963`) unconditionally rebuilds it from `DB.po` on every `initApp()`, using its own `invNum`-first/`invId`-fallback precedence (`2947-2948`) — it never trusts a pulled record's `pos[]`, so any gap self-heals on the next load regardless of what `pullAll()`/`migratePOToSupabase()`'s bare-key sweep produced.
- `buyerId` and `lineItems[].lid` are safe because no exact-match business-key fallback exists to build a fix on: `buyer` (the one field Sheets does carry) is free-text display, not a unique key, and per-line-item detail was never mapped to Sheets at all — this is data that never round-trips, not data that round-trips correctly on one form and silently vanishes on the other. Matches this REQ's own pre-flight design (§2b/AC-2 already treat blank `buyerId`/`lineItems[].lid` as non-blocking).
- `linkedQuoteId`/`linkedQuoteNum` are safe because *neither* form is mapped to Sheets — there is no mapped-Num/unmapped-Id asymmetry for the bug to exploit; both are consistently blank on a genuinely-new pulled record, so there's no scenario where one silently diverges from the other.

Part B re-verified round 7's fix is fully and consistently integrated everywhere referenced (status line, §1.4, §1.5, §1.6, REQ-CLOUD-006b, AC-4/4a/4b, this log), then spot-checked 30+ citations directly against a worktree pinned to this REQ's exact commit, finding zero errors — including confirming several deliberately narrow sub-range citations (e.g. `processImport()`'s `'inv'` branch, `savePayment()`'s invoice-mutation sub-block) as intentional scoping rather than off-by-one mistakes. No internal inconsistencies found across the document's accumulated 7 rounds of edits.

Requirements-gate is complete. Proceeding to SPEC-CLOUD-006.
