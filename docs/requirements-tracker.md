# Requirements Tracker — Stackd Ops

Last updated: 2026-09-03 (REQ-LI-001 — Line Item unit cost/price 3-decimal precision)

---

## Active requirements

| ID | Title | Req file | Spec file | Version | Req gate | Spec gate | Build | PR |
|---|---|---|---|---|---|---|---|---|
| REQ-V3-GAP-007 | Global Event Log (`DB.events`) | REQ-V3-GAP-007-v3.md | SPEC-V3-GAP-007-v1.md | v2.9.28 | PASS | PASS | ✓ shipped | #46 |
| REQ-V3-GAP-006 | Supplier → Contact Sub-panel | REQ-V3-GAP-006-v3.md | SPEC-V3-GAP-006-v2.md | v2.9.29 | PASS | PASS | ✓ shipped | #46 |
| REQ-AI-GAP-001 | AI Order Flow Actions (narrow) | REQ-AI-GAP-001-v1.md | SPEC-AI-GAP-001-v1.md | v2.9.30 | PASS | PASS | ✓ shipped | #46 |
| REQ-DEMO-001 | End-to-End Demo Mode | REQ-DEMO-001-v2.md | SPEC-DEMO-001-v1.md | v2.9.31 | PASS | PASS | ✓ shipped | — |
| REQ-MTD-001 | MTD-Compatible VAT Export | REQ-MTD-001-v2.md | SPEC-MTD-001-v1.md | v2.9.32 | PASS | PASS | ✓ shipped | — |
| REQ-BUY-001 | Buyers / Customers Entity | REQ-BUY-001-v1.md | SPEC-BUY-001-v4.md | v2.9.37 | PASS | PASS | ✓ shipped | — |
| REQ-QTE-001 (Part A) | Per-line quote margin override | REQ-QTE-001-v3.md | SPEC-QTE-001-v2.md | v2.9.52 | CONDITIONAL PASS (3 rounds — v1: 2 findings, independent v2: 5 findings incl. 2 real bugs, v3 resolved all) | CONDITIONAL PASS (1 independent round — no blocking calc-logic bugs, citation/test-plan cleanup only) | ✓ shipped | — |
| REQ-SUP-001 | Supplier price intelligence retention & reference view | REQ-SUP-001-v2.md | SPEC-SUP-001-v2.md | v2.9.53 | CONDITIONAL PASS (v1: 4 findings, v2 resolved all) | CONDITIONAL PASS (1 independent round — no blocking defects, citation/test-plan cleanup only) | ✓ shipped | #76 |
| REQ-CLOUD-001 | Supabase-backed shared data layer (Suppliers + Buyers only) | REQ-CLOUD-001-v3.md | SPEC-CLOUD-001-v4.md | v2.9.54 | CONDITIONAL PASS (v1→v2, council decision APPROVED recorded in v3, conditioned on verified backup/rollback) | 4 independent rounds (v1 FAIL 5 blocking → v2 FAIL 5 new blocking → v3 FAIL 1 blocking → v4 PASS, 2 advisory only) | ✓ shipped — build-gate round 1 found a real `pullAll()` Sheets-pull-back race + 2 re-entrancy bugs + a skipped test-conversion deliverable, fixed in v2.9.54 follow-up (PR #80, since PR #79 was merged before the fixes landed); round-2 build-gate review pending | #79, #80 |
| REQ-AI-GAP-002 | Invoice/Line Item/Credit Note AI creation + Supplier/Buyer read tools | REQ-AI-GAP-002-v2.md | SPEC-AI-GAP-002-v2.md | v2.9.55 | CONDITIONAL PASS (v1: 1 blocking on `create_line_item` min-fields, v2 resolved) | PASS (v1: 1 advisory citation error, v2 resolved) | ✓ shipped — build-gate independent review **PASS**: every field mapping/validation guard traced correctly to `vLI()`/real invoice line-item shape/`openNewCN()`; PII-minimization on `get_suppliers`/`get_buyers` confirmed structurally true, not just conventional. 1 advisory finding (the "Review in [Form]" `actionLabels` map wasn't extended for the 3 new action types, rendering raw action keys) fixed same-day. Closes AI-GAP-006/AI-GAP-008/AI-GAP-009 (known-gaps.md updated to reflect closure, corrects a doc-staleness gap the reviewer also caught). | #81 |
| REQ-CON-004 | AI-assisted enquiry intake check | REQ-CON-004-v2.md | SPEC-CON-004-v2.md | v2.9.55 | CONDITIONAL PASS (v1: 1 advisory-but-real PII-adjacency finding, v2 resolved) | CONDITIONAL PASS (v1: 1 blocking missing `AI_SYSTEM_PROMPT` update + 1 advisory citation error, v2 resolved) | ✓ shipped — build-gate independent review **PASS**, no issues found: `conCheckEnquirySemantic()` confirmed byte-for-byte contract-identical to `ordCheckLineGapsSemantic()`; PII-scoped payload confirmed via a real test parsing the actual outbound fetch body. | #81 |
| REQ-QTE-001 (Part B) | RFQ supplier comparison & commit (landed-value ranking) | REQ-QTE-001-v3.md | SPEC-QTE-001-partB-v2.md | v2.9.56 | CONDITIONAL PASS (3 rounds, carried over from Part A gating — v1: 5 findings, independent v2: staleness-warning bug/Contact-orphaning gap/wrong hand-off citation/Part A×B interaction undefined, v3 resolves all) | CONDITIONAL PASS (independent v1: 1 blocking — `ordConvertToQuote()` hand-off dropped REQ-QTE-001l's required currency conversion, silently corrupting downstream Quote math for non-USD responses; 1 citation error; 2 advisory — CBM-optionality undermining the "apples-to-apples" claim, hardcoded `qty:1` ignoring an already-in-scope `baseQty` — v2 resolves all, confirmed by an independent confirmatory re-review, PASS) | ✓ shipped — build-gate independent review **PASS**, no issues found: currency-conversion math traced with a worked example (RMB 700 → correctly non-identity-converted, not the raw number) and confirmed against a real independently-computed test assertion; `delSup()`'s warn-count confirmed to apply to both the Supabase and local branches from a single pre-split computation; all field guards/defensive-init, modal HTML, and doc consistency verified byte-for-byte. | #82 |
| REQ-DATA-002 | Data Integrity Cleanup (phantom record removal & safe renumbering) | REQ-DATA-002-v2.md | SPEC-DATA-002-v2.md | v2.9.57 | CONDITIONAL PASS → PASS (v1: 2 blocking — Contacts self-healed by `backfillConIds()` into id-bearing-but-blank zombies, defeating a plain `!id` check; wrong/incomplete FK-field list (`buyId`→`buyerId`, missing `lid`/`sourceContactId`) — v2 resolves both, confirmed by an independent confirmatory re-review, PASS) | CONDITIONAL PASS → PASS (independent v1: 3 blocking — incomplete Quote-line `lid` FK check; missing REQ-mandated sort-stability comment; `logEv()` audit-trail used a raw DB-key abbreviation + `'bulk'` placeholder id instead of the app's real per-record Activity-tab convention, meaning a renumbered Supplier/Contact would never show it in its own Activity tab — v2 resolves all three, confirmed by an independent confirmatory re-review, PASS) | ✓ shipped — `isPhantomRecord()`/`scanForPhantomRecords()`/`renumberEntitySequentially()`/`verifyFkIntegrityAfterCleanup()`/`executeDataCleanup()` + parallel backup-gate modal + `pullAll()` id-keyed-entity hardening. One pre-existing test's expectations updated to match the corrected `pullAll()` behavior. 15 new tests. 521/521 pass. Build-gate independent review **CONDITIONAL PASS** — no blocking findings (build confirmed a faithful, correct implementation of every spec'd function/modal/gate/FK-check across all 10 verification points); 1 advisory finding (this row's "17 new tests" was miscounted, corrected to 15) plus 2 cosmetic wording deviations from the spec's literal modal-copy suggestions, both fixed same-day, prior to merge. | #84 (docs), #85 (build) |
| REQ-INTEG-001 (Phase 1) | Order Request/RFQ → Quote → Invoice referential integrity — traceability & staleness detection (fixes `ORD-GAP-003`, `PO-GAP-003`) | REQ-INTEG-001-v2.md | SPEC-INTEG-001-v2.md | v2.9.58 | CONDITIONAL PASS → PASS (v1: 1 blocking — the PO staleness mechanism as originally scoped could only ever detect 2 of the 3 drift types `PO-GAP-003` itself names, never a newly-added invoice line item; 3 advisory — wrong `FIELD_MAPS` citation, missing PO-modal copy from the open-questions list, no mixed-scenario ACs — v2 resolves all four, confirmed by an independent confirmatory re-review, PASS) | CONDITIONAL PASS → PASS (v1: 1 blocking — the price-comparison mechanism defaulted a missing price-snapshot field to `0`, which would have permanently false-flagged every PO already in existence before this phase shipped since nothing backfills the new field; 2 advisory — a documented-but-unfixed multi-line-per-`lid` assumption, an imprecise REQ cross-reference — v2 resolves all three (treats an absent snapshot as "predates tracking," not "price is zero"), confirmed by an independent confirmatory re-review, PASS) | ✓ shipped — `ordConvertToQuote()`/`saveQte()` gain source back-reference fields (the explicit-allowlist gotcha caught in requirements-gate specifically prevented from recurring); new `renderQteSourceDriftWarn()` wired into `editQte()`; `autoPos()` captures a price snapshot; new `renderPoSourceDriftWarn()` wired into `editPO()`, covering both the existing-line comparison and the new-line-added reverse check; Invoice gains a schema-only `sourceQuoteId`. 18 new tests. 539/539 pass. Build-gate independent review **PASS**, no findings — every function/wiring/HTML change verified line-by-line against spec, including the two previously-corrected regression-guard mechanisms (the new-line reverse check, the absent-`sourceInvUp` no-false-positive guard), both confirmed present and correctly implemented. **Post-merge update (v2.9.59):** the schema-only `sourceQuoteId` field on Invoice (REQ-INTEG-001g) was removed — confirmed with the business that direct Quote→Invoice traceability is unnecessary, since the real workflow is always Quote → PO → Invoice and that chain is already fully covered by two pre-existing mechanisms, `qteToPoConvert()` (`quoteId`/`quoteNum` on PO) and `autoPos()` (`invId`/`invNum` on PO). Everything else from Phase 1 is unaffected. 1 test removed. 538/538 pass. | #89 (docs), #90 (build), #92 (removal) |
| REQ-INTEG-001 (Phase 2) | Buyer-approval capture on Invoice — "Mark Buyer Approved" + "Progress to Invoicing", automatic clearing on post-approval line-item edits | REQ-INTEG-001-phase2-v2.md | SPEC-INTEG-001-phase2-v2.md | v2.9.60 | CONDITIONAL PASS → PASS (v1: 3 blocking — a false "reuses G-06" claim for the line-item comparison (G-06 only diffs header fields + aggregate count/total, never per-line); a missing AC for the re-approval timestamp-overwrite behavior; a missing AC for the Progress-to-Invoicing `Pro-forma`-status precondition, independent of the approval-set precondition — 4 advisory — v2 resolves all seven, confirmed by an independent confirmatory re-review, PASS) | CONDITIONAL PASS → PASS (v1: 2 blocking — the `invLinesChanged()` insertion diff was drawn in the wrong position relative to the `cIL.length===0` preserve block, correctable only via a follow-up paragraph a verbatim build could miss; the six new fields were never extended to the two CSV-import wholesale-replace code paths, a real, demonstrated silent-approval-loss path on re-import — 5 advisory — v2 resolves all seven, confirmed by an independent confirmatory re-review, PASS) | ✓ shipped — `saveInv()` gains six new fields (`buyerApprovedAt`/`buyerApprovedBy`/`approvalMethod`/`approvalNote`/`linkedQuoteId`/`linkedQuoteNum`) with default+preserve wiring; new `invLinesChanged()` (standalone per-line comparison, not a G-06 reuse) clears approval on a genuine line-item change, correctly placed after the `cIL.length===0` block resolves; new `saveInvApprove()`/`saveInvProgress()` direct-mutation actions (bypass `saveInv()`'s form-rebuild) with pure, directly-testable `invApprovalActionVisible()`/`invProgressActionVisible()` gating functions (resolving a requirements-gate confirmatory-review finding that locked-status invoices never reach the modal these buttons live in); both CSV-import paths extended to preserve the six fields. A build-time bug in `invLinesChanged()`'s rid-fallback (found via test-writing, not by either gate review — the fallback key never actually matched because `saveInv()` always mints a fresh `uid()` for an empty `rid`) was fixed by detecting an entirely rid-less legacy line array and falling back to positional comparison for the whole array, rather than a per-line key fallback. 19 new tests. 557/557 pass. | #94 |

**Watch item for Phase 3 (payment-request entity) briefing — not a gap, a pre-check to run:** Phase 2 added `approvalMethod`/`approvalNote` as captured-but-not-yet-surfaced-anywhere fields (no per-Invoice Activity-tab UI exists to display the `buyer_approved`/`progressed_to_invoicing` `logEv()` entries either). This is the exact shape of risk that produced the dead `sourceQuoteId` field in Phase 1 (added, never populated by anything, removed post-merge once confirmed unnecessary). When Phase 3 is briefed, check-first should explicitly confirm whether Phase 3's design actually reads/surfaces `approvalMethod`/`approvalNote`/the two new event types, or whether they remain captured-but-invisible — and flag it the same way if so, rather than letting it repeat silently a second time.

| REQ-INTEG-002 (Sub-phase 2a) | Supplier Payment ledger — first standalone piece of a larger Payment Allocation build (2a Supplier ledger → 2b Invoice→PO enumeration fix → 2c Buyer payment tranches → 2d full allocation link) | REQ-INTEG-002-v2.md | SPEC-INTEG-002-v2.md | v2.9.62 | CONDITIONAL PASS → PASS (v1: 2 blocking — a false claim that the buyer-side Payments ledger is a "mapped but never synced" FIELD_MAPS category, used to justify giving the new entity an equivalent inert field-map entry (buyer Payments is actually actively bulk-synced via `syncAll()`/`pullAll()`/`pushAll()`, just missing a real-time per-save sync call — the genuine no-sync precedent is Order Requests/Buyers, which the new entity now follows instead, with zero FIELD_MAPS/sync-array footprint); a missing AC for the payment-history table display, the phase's headline UI requirement — 4 advisory — v2 resolves all six, confirmed by an independent confirmatory re-review, PASS) | CONDITIONAL PASS → PASS (v1: 3 blocking — `expAll()`/`doImport()`, the local JSON backup/restore path, were never extended for the new entity, and since it has no Sheets-sync path by design, that JSON file was its *only* portable backup — a real, silent data-loss risk on restore/device-migration that the `buy`/`ord` precedent had already correctly handled; a wrong `saveAll()` line citation; an underspecified "regex check" for the sync-array-absence test that would have false-failed once built, since the entity name legitimately appears elsewhere in the file (`saveAll()` itself) — 3 advisory — v2 resolves all six, confirmed by an independent confirmatory re-review, PASS) | ✓ shipped — new `DB.supPayments`/`K.spm` entity with default+persistence wiring in `saveAll()` and the local backup/restore path (`expAll()`/`doImport()`); `lockFxRate()` wraps the existing `toGBP()` mechanism to capture a point-in-time GBP-equivalent snapshot (never recomputed on read); `getPOPayments()`/`getPOTotalPaid()`/`saveSupPayment()`/`deleteSupPayment()`/`vSupPay()` mirror the existing buyer-side Payments trio, with `getPOTotalPaid()` deliberately summing locked GBP-equivalents rather than raw amounts, since a PO's payments can span multiple native currencies where a buyer Invoice's payments cannot; new per-PO "$" action opens a dedicated payment-history modal (`renderPOPaymentsTab()`), mirroring the existing buyer Payments-tab UI with an added `purpose` field; confirmed zero `FIELD_MAPS`/bulk-sync footprint, matching the genuine local-only precedent (Order Requests/Buyers). Deliberately does not touch `PO.dep`/`PO.fpmFunded`/`PO.fpmRecovered` or the duplicated `fpmRecovered` auto-set logic — this ledger coexists with, not replaces, those fields. 12 new tests. 575/575 pass. **Fix-forward (v2.9.63, REQ-INTEG-002-2a-fix):** real-world testing found the ledger built here had zero visible effect on any of the 10 places `PO.dep` is displayed (Edit PO modal, PO PDF, Dashboard, Accounts view, AI assistant) — recording a payment changed nothing an operator could see. Fixed via a new `getPOEffectiveDep(po)`, the single point of truth for all 10 sites: reconciles from the ledger once records exist (same-currency payments summed exactly, cross-currency payments converted via each record's own historically-locked rate — `lockFxRate()` widened from a 1-key to a 3-key `ratesUsed` snapshot to make this possible), with a safe legacy fallback for POs with no records or an unreconcilable currency (EUR, logged as `PROC-GAP-002`). `docs/REQ-INTEG-002-2a-fix-v2.md`/`docs/SPEC-INTEG-002-2a-fix-v2.md` (both v1 FAIL → v2 PASS at requirements-gate and spec-gate respectively). 19 new tests. 594/594 pass. Build-gate independent review **PASS**, no findings (blocking or advisory) — all 10 call sites confirmed wired, `openPO()`'s read-only/note leak fix confirmed present and correct, `savePO()` confirmed unmodified, no regressions to 2a's core ledger mechanism (`renderPOPaymentsTab()`/`addSupPaymentFromForm()`/`saveSupPayment()`/`deleteSupPayment()`/`vSupPay()` all absent from the diff). **Fix-forward 2 (v2.9.64, REQ-INTEG-002-2a-fix-2):** manual (Cowork-driven) testing of v2.9.63 found 4 real issues, verified as new/separate findings, not regressions of the core `getPOEffectiveDepInfo()` reconciliation logic. Priority 1 (critical, full gate review): `renderAccts()`'s totals bar summed raw amounts across POs/Invoices of different currencies with zero conversion, mislabeling the garbage total as `$` — fixed via `toDisp()`-before-summing, mirroring `rDash()`'s already-correct KPI-tile pattern, for `tBD`/`tSD`/`tFPM`. `docs/REQ-INTEG-002-2a-fix-2-v1.md` (requirements-gate PASS, no blocking findings) / `docs/SPEC-INTEG-002-2a-fix-2-v2.md` (spec-gate v1 CONDITIONAL PASS → v2 PASS: v1's test plan had 2 blocking gaps — an assertion mechanism that would never match `fmt()`'s rounded/comma-formatted output, and a "same-currency regression guard" test whose premise was false since `QR_DEFAULTS.displayCurrency` is `'GBP'`, not `'USD'` — both resolved in v2, confirmed by an independent confirmatory re-review, PASS). Priority 2/3 and the demo-data fix went through standard build-gate only, per instruction (no design decision, no financial-computation change): `rPO()`/`rDash()` added to `addSupPaymentFromForm()`/`deleteSupPayment()` (PO list wasn't re-rendering after a ledger mutation, matching the buyer-side `savePayment()` precedent); `editPO()`'s `pf-dep` value now rounds to 2dp for display only; `renderPoDepNote()` now toggles a strong readOnly visual signal (cursor/background), which surfaced and fixed a self-inflicted ordering bug in `openPO()` (`vClrAll()` was running after the new styling code and silently clearing it); `loadDemoData()`'s `PO-DINV-0005-1` now seeds a real $13,225 legacy deposit instead of $0, making its intended backward-compat-fallback scenario actually testable. Logged, not fixed: `ACCT-GAP-001` (same currency-mixing flaw, pre-existing, in `renderAccts()`'s per-invoice/per-supplier columns) and `AI-GAP-010` (the same flaw, pre-existing, in `_aiExecTool('get_kpis')`, which also affects invoice-side figures). 10 new tests. 604/604 pass. Build-gate independent review **PASS**, no findings (blocking or advisory) — Priority 1's diff confirmed byte-for-byte against spec and mirrors `rDash()`'s pattern exactly; 4 of 5 Priority-1 tests manually traced against real `toDisp()`/`fmt()` behavior and confirmed genuinely correct; `openPO()`'s `vClrAll()`-before-`renderPoDepNote()` ordering fix confirmed present and correct; demo-data fix confirmed (zero linked `supPayments` for `demo-po-002`); no unrelated code touched. **Sub-phase 2b (v2.9.66, REQ-INTEG-002-2b, Invoice→PO enumeration fix):** check-first found `inv.pos[]`, an Invoice field written on every save but never read anywhere in the app — the real, live "which POs belong to this invoice" answer was an ad-hoc filter (`po.invId===inv.id||po.invNum===inv.num`) independently retyped at 3 call sites. User-chosen design direction: make `inv.pos[]` authoritative (new `getInvoicePOs(inv)` helper, new `backfillInvoicePOs()` migration, new `delPO()` cleanup step), not the narrower alternative of just extracting the existing filter into a shared read-only helper. Went through an unusually long gate cycle — 4 REQ versions, 4 SPEC versions — because each of 3 independent review rounds found a genuine PO-linking path the design had missed beyond the originally-assumed sole path (`autoPos()`): `processImport()`/`processImportRecords()` (CSV/Sheets per-entity import, found at spec-gate v1), then `pullAll()`'s bulk Sheets-merge block (found at a confirmatory requirements-gate round dispatched specifically to hunt for a 4th path after the first two misses — none found, but one accuracy-only correction to the REQ's own claimed exhaustiveness was made), then a genuine ALGORITHM bug in `backfillInvoicePOs()` itself (found at a second spec-gate pass on the newest content only): its original per-invoice OR-filter could assign the same PO to two invoices at once when `invId`/`invNum` disagreed (exactly what a `pullAll()` re-link produces, since `FIELD_MAPS.po` never maps `invId`) — fixed by inverting the algorithm to resolve each PO to one "home" invoice, preferring an `invNum` match (the field every real re-link path actually changes) over `invId`. `docs/REQ-INTEG-002-2b-v4.md`/`docs/SPEC-INTEG-002-2b-v4.md` carry the full four-round-plus-one review-resolution log. `renderAccts()`'s per-invoice `linkedPOs`, and both independently-duplicated FPM-deposit auto-recovery blocks (`saveInv()`/`savePayment()`, left as separate copies per `REQ-INTEG-002h`'s 2a non-goal), all switched to `getInvoicePOs(inv)` with their own business logic otherwise untouched. Logged, not fixed: `PO-GAP-004` (a narrow, pre-existing `savePO()` edge case — renaming a linked invoice can silently clear a PO's `invId` on its next save). 11 new tests, plus 3 pre-existing tests fixed (they seeded PO/invoice fixtures directly, bypassing `autoPos()`, so needed an explicit `backfillInvoicePOs()` call to populate `inv.pos[]` the way a real app boot would). 620/620 pass. Build-gate independent review **PASS**, no findings — algorithm confirmed byte-for-byte against spec via direct code read; all 5 `backfillInvoicePOs()` wiring call sites confirmed present and correctly placed; `delPO()`'s conditional `sv(K.i,DB.inv)` confirmed gated correctly; a mutation test (reverting the fix to the old, buggy per-invoice filter) confirmed exactly the 2 tests targeting the fixed bug fail and nothing else regresses, then the revert was undone. **Sub-phase 2c (v2.9.77, REQ-INTEG-002-2c, Buyer payment tranches):** check-first found buyer payments already support multiple partial payments per invoice (predates 2a) — this sub-phase instead closes the parity gap against the now-more-mature Supplier Payment ledger (`purpose`/`currency`/`rateLock` fields, new `getInvTotalPaidNative(inv)`/`getInvEffectiveDepInfo(inv)` mirroring the PO-side pair) and fixes a confirmed-live bug (`deletePayment()` never reversed `inv.status`). Went through an unusually long requirements-gate cycle — 11 rounds, the longest in this series — the last four rounds (7-10) each finding a genuine defect in the shared `getInvPayments(inv)` matching-rule's field-preference order before round 11 passed clean; round 10's final rule inverts `REQ-INTEG-002-2b`'s own "prefer invNum" precedent, since payments (unlike POs) never have their `invId`/`invNum` deliberately re-edited after creation, making the direct, immutable `invId` reference more trustworthy than the rename-vulnerable denormalized `invNum` copy. Spec-gate went 2 rounds (round 1 CONDITIONAL PASS — 1 blocking, 2 advisory, both fixed in place; round 2 an independent re-verification from scratch, clean PASS). `docs/REQ-INTEG-002-2c-v1.md`/`docs/SPEC-INTEG-002-2c-v1.md`. Six currency-blind/matching-inconsistent read sites (`cInv()`, `editInv()`, `renderStatement()`, `prevStmtPdf()`, `saveCN()`'s balance-due recompute, `renderPaymentsTab()`'s own summary box) now share one correct reconciliation path; `renderPaymentsTab()` gains a Purpose column (always) and a Currency/GBP-equivalent column pair (only when a row's resolved currency differs from the invoice's own); `acctPmtCSV()`/`acctFACSV()`/`_aiExecTool('get_payments')` gain `purpose`/currency-repointed fields. Logs `PROC-GAP-003` (NGN/GHS share EUR's pre-existing, unlogged `toGBP()`/`fromGBP()` gap) — guarded against, not fixed. 31 new tests, 806/806 pass (baseline 775/775 → 806/806, zero regressions). Build-gate self-review: a mutation test (reverting `getInvPayments(inv)`'s `invId`-authoritative rule to the old bare-`invId`-equality behavior, dropping the `invNum` fallback entirely) reproduced exactly its predicted single test failure — the "dangling/blank invId falls back to invNum" AC-3c-scenario-2 test, the only one of the five AC-3c tests that actually exercises that fallback path — then the revert was undone and 806/806 reconfirmed. | #100 (2a); #102 (fix); #105 (fix-2); #110 (2b); PR pending (2c) |
| REQ-SDLC-003 | PR preview environment — closes `SDLC-GAP-003` (no staging/preview environment), revisited per its own 2026-08-25 trigger now that Payment Allocation moves into sub-phase 2c. First SDLC-track REQ/SPEC pair in this tracker — infrastructure, not app code; no `index.html`/version change. | REQ-SDLC-003-v1.md | SPEC-SDLC-003-v2.md | n/a (infra) | PASS (both load-bearing design claims — GitHub Pages' custom-domain redirect behavior, `localStorage`'s origin-only partitioning — independently verified against GitHub's own documentation and a direct code check, not merely asserted; 1 advisory on the sticky-comment lookup mechanism, folded into the spec) | v1 FAIL → v2 fixed in place (1 blocking: creating the preview repo with no initial commit leaves no default-branch ref, so `actions/checkout@v4` would fail on every workflow run, forever, since checkout runs before the push step that was supposed to create that first commit — fixed by creating the repo with `autoInit: true`; 1 advisory — sticky-comment lookup fetches only the first 100 PR comments, low risk, not fixed). Not yet independently re-confirmed by a second spec-gate round — verified instead at build-gate against the real merged workflow files. | Two new workflows in `stackd-ops` (`preview-deploy.yml`, `preview-cleanup.yml`), triggered by the fork-safe `pull_request` event (not `pull_request_target`, confirmed at requirements-gate to prevent secret exfiltration from a malicious fork PR). Corrects the original 2026-06-06 LLM Council recommendation, which assumed a same-repository preview could be origin-isolated from `app.getstackdops.com` — verified false: GitHub Pages redirects a repo's default `github.io` URL to its own configured custom domain, so any same-repo preview would still resolve onto production and share its `localStorage`. Design instead uses a second, dedicated public repository (`stkdcfpm/stackd-ops-preview`, no custom domain of its own) for genuine origin isolation. **Discovered during implementation, not anticipated at either gate:** Claude Code's GitHub integration cannot create new repositories (`create_repository` returned `403 Resource not accessible by integration`) — repo creation added as a manual step (REQ §3/SPEC §5) alongside the already-known PAT/secret/Pages-toggle steps; the two workflow files were still built and merged, since they fail harmlessly (no effect on `main`/`qa.yml`/production) until those steps are completed. Build-gate independent review **CONDITIONAL PASS**, 2 doc-accuracy advisories (a "three" vs "four" manual-steps miscount in `STACKD_CONTEXT.md`, and two ACs left with a stale nested path from before an earlier fix) — both fixed same-PR; workflow YAML fidelity, trigger safety (independently re-read from the committed files, not just the spec doc), diff scope, and test suite all confirmed clean. | #113 |
| REQ-SYNC-002 | Batch Sheets sync into one request per direction — closes `SYNC-GAP-002` (sync performance), user-reported ("sync function takes so long") | REQ-SYNC-002-v2.md | SPEC-SYNC-002-v1.md | v2.9.67 | CONDITIONAL PASS → v2 fixed in place (v1: 2 real design gaps — no per-entity error isolation inside the new batched handlers, no consideration of Apps Script's execution-time quota — plus 1 overstated claim about existing status-checking on the push side; v2 adds REQ-SYNC-002e/f and corrects the claim, all in §8's review-resolution log). Not yet independently re-confirmed by a second requirements-gate round — verified instead at spec-gate/build-gate against the real diffs. | PASS (diffs verified accurate against the real current `Code.gs`/`index.html`; REQ-SYNC-002e's isolation confirmed under a concrete failure walkthrough; all three client functions' rollout fallback traced and confirmed correct; mock-harness extension confirmed to leave every existing test unaffected). 4 advisory findings (a catch-block guard gap, a missing `entity` field on one error shape, an incomplete caller list, a miscounted call-site total), all fixed in place, no second spec-gate round needed. | Round 1: **FAIL** — a pre-existing `var pulled` inside `pullAll()`'s simple-entities loop shadowed the new `pulled()` helper function added for batched pulls (`var` is function-scoped); the first simple entity with non-empty records silently broke every entity processed after it in that loop, on both the batched and fallback paths, in a way the initial 629/629 test run didn't catch (no test exercised two populated simple entities in one pull). Fixed by renaming the shadowing local (`pulledRecs`); added a regression test covering two populated simple entities in the same pull, plus corrected a second test that had coincidentally passed for the wrong reason (via the B-1 crash, not the isolation logic it claimed to test). Round 2 (confirmatory) **PASS**: independently re-verified the rename via direct code read (only the intended `pulled()` helper remains in scope), confirmed zero "pulled is not a function" occurrences in a fresh 630/630 test run, and — the key evidence — reintroduced the exact B-1 bug by hand, reran the suite (629/630, the new regression test failing with precisely the expected symptom), then reverted and reconfirmed 630/630 clean, proving the new test is a genuine regression guard, not a re-labeled pass-for-the-wrong-reason test. Also confirmed the two fix commits touched nothing outside their stated scope (`syncAll()`/`pushAll()` untouched) and that all version/test-count references across `index.html`/`STACKD_CONTEXT.md`/`CLAUDE.md` agree. | #115 |
| REQ-ORD-006 | Edit and delete RFQ responses — user asked how to update a recorded RFQ response after a supplier sends revised pricing; found no edit/delete path anywhere, only "+ Add Response" (push-only save, blank-only open). Also the direct prerequisite unblocking `REQ-AI-GAP-011` (AI-assisted RFQ update from a pasted supplier email), scoped alongside this REQ but held back pending this one shipping. | REQ-ORD-006-v1.md | SPEC-ORD-006-v1.md | v2.9.69 | CONDITIONAL PASS, no blocking findings (2 advisories fixed in place — a missing AC for the delete-confirm() message content, now AC-8, and a fixture citation misattributed to REQ-QTE-001 Part B instead of the correct REQ/SPEC-INTEG-001 Phase 1 fixtures; §8 has the full review-resolution log). The single highest-risk item — whether an id-rotating edit/delete would correctly re-trigger the existing, unmodified `renderQteSourceDriftWarn()` staleness banner — was independently hand-traced against the real code with a concrete worked example and confirmed sound. | PASS, no blocking findings (3 advisories fixed in place, including a real test-isolation trap: `cRfqEditId` is module-level UI state `resetDB()` doesn't reset, so a leaked truthy value from one test could silently reroute an unrelated later test's `saveRfqResponse()` into a no-op edit branch — flagged for the test-writing pass, not fixed in the spec itself since it required no code change, only test discipline). The staleness-mechanism reasoning was verified a second, independent way at this gate: the reviewer extracted and actually ran the proposed code end-to-end against the real `renderQteSourceDriftWarn()`, not just reasoned about it. | ✓ shipped — build-gate independent review **CONDITIONAL PASS**: every AC (1-8) verified against the real committed code, including a hand-trace of the id-rotation/repoint mechanism against the actual (unmodified) `renderQteSourceDriftWarn()` comparison, confirmed load-bearing by a mutation test (temporarily reverting the fix to reuse the edited response's old id reproduced exactly the missing-staleness-banner regression the design exists to prevent, in both the edit and delete tests, then reverted and reconfirmed 654/654 clean). 1 real finding: `AI_SYSTEM_PROMPT`'s pre-existing RFQ-comparison description still claimed "no response is ever deleted," now false — fixed in this same delivery, along with the required `docs/user-guide.md` update. 13 new tests. 654/654 pass. | #119 |
| REQ-AI-GAP-011 | AI-assisted RFQ response update from a pasted supplier email — scoped alongside REQ-ORD-006 when the user asked "is there an AI capability to submit the email content to be updated in the quote?"; explicitly blocked pending REQ-ORD-006's merge, which landed as PR #119 | REQ-AI-GAP-011-v1.md | SPEC-AI-GAP-011-v1.md | v2.9.70 | CONDITIONAL PASS, no blocking findings (5 advisories fixed in place — a missing response-shape validation note, a citation drift, an under-argued PII input-scoping claim now explicitly named as an accepted residual risk with a stated mitigation, a durability difference from `update_order_line`'s pending-log pattern now explicitly disclosed as a conscious trade-off, and an overstated rejected-alternative NLU-risk argument corrected to the real reason — unnecessary complexity/friction, given `create_invoice`'s get_buyers-then-clarify precedent shows the alternative would likely work fine). | CONDITIONAL PASS — **1 real blocking finding**: the pending-proposal tracking state (`cRfqEmailParseRespId`/`cRfqEmailParseProposed`) was flat module vars with no per-line scoping, but `rOrdLines()` renders every Order Request line's own comparison panel simultaneously — a stale Apply button left over from an abandoned line's review could silently act on a different line's AI-proposed values, potentially corrupting a third, unrelated line via whatever `cRfqOrdId`/`cRfqLineId` last pointed at. Fixed by adding `cRfqEmailParseLineId` plus two independent guards in `rfqApplyEmailParse()` — an early check that the clicked button's own line matches the tracked line, and a post-`editRfqResponse()` check that the edit actually took effect for the given response before writing any field values. 4 advisories also fixed: two citation-range drifts, a `notes`-payload-asymmetry risk now named as an accepted limitation, and a failed-`saveRfqResponse()`-validation edge case now documented as accepted (§13 has the full review-resolution log). New required test locks in the fix: two lines' proposals held open simultaneously, a stale Apply on the abandoned one confirmed a genuine no-op on both lines, then the legitimate line's own Apply confirmed to still work correctly. | ✓ shipped — build-gate independent review **CONDITIONAL PASS**: every function verified byte-for-byte against the approved spec; the cross-line fix **independently mutation-tested by the reviewer** (removing the primary guard left exactly one test failing — the cross-line regression test — with the reviewer additionally discovering *why* both guards are independently necessary: without the first guard, the stale click doesn't corrupt data (the second guard catches it) but does silently wipe the *other*, still-legitimate line's pending proposal as collateral damage, breaking its own subsequent real Apply). 2 real findings, both fixed in this same delivery: a stray one-character formatting change unrelated to the diff, and incomplete version-ship housekeeping (this row is that update). 17 new tests. 671/671 pass. | #120 |
| REQ-DATA-003 | Fix CSV import corruption on multi-line quoted fields — user reported a live Suppliers-tab corruption after a CSV re-import; diagnosed to `parseImportCSV()` tearing a multi-line Notes/Address cell into multiple phantom records, invisible to the existing phantom-record scanner since each fragment gets a real, non-falsy `id` | REQ-DATA-003-v1.md | SPEC-DATA-003-v1.md | v2.9.71 | PASS, no blocking findings (2 advisories fixed in place — a missing AC for malformed/unterminated-quote safety, now AC-9, and two citation-range drifts; §8 has the full review-resolution log). Independently confirmed, by reading the entire `apps-script/Code.gs`, that the Google Sheets sync path is not implicated — this REQ's direct answer to the user's second question ("is sync corrupting my data") survives scrutiny rather than merely sounding plausible. | PASS, no blocking findings. Every AC (1-9) — including the two highest-risk, escaped-quote off-by-one and unterminated-quote safety — independently reproduced and passed by actually extracting and running the proposed replacement function against adversarial inputs, not by reading the diff and trusting it. AC-8 (zero regressions) confirmed empirically: the reviewer applied the exact proposed function to a full copy of `index.html` and ran the real 671-test suite, 671/671 pass. 2 advisories fixed: a documented, deliberate minor divergence (a bare standalone `\r` outside quotes is now dropped rather than preserved — accepted, no realistic occurrence in any real CSV export), and a tightened AC-4 test assertion to match what the code actually guarantees. | ✓ shipped — build-gate independent review **PASS**, no findings. The committed function confirmed byte-for-byte identical to the approved spec; every AC re-verified by extracting and running the real committed code (not the spec's text) against adversarial inputs, including a ~1.2MB unterminated-quote stress test (linear time, no hang). The commit message's precise mutation-test claim ("exactly 5 tests fail on revert") verified exactly: reverted to the old parser, got **676/681** with exactly the 5 targeted failures (the multi-line field, escaped-quote, embedded-CRLF, Supplier end-to-end, and cross-entity tests), all 3 regression-guard tests stayed green, then restored to clean 681/681. Confirmed `processImport()` and the Sheets sync path both byte-identical to the pre-fix commit — zero unrelated changes. 10 new tests, closing a pre-existing zero-coverage gap for Supplier CSV import. 681/681 pass. | #121 |
| REQ-PO-002 | Fix `qteToPoConvert()` field-shape mismatch corrupting Quote-converted Purchase Orders — surfaced during architecture research for the planned cross-platform backend migration (`docs/architecture-data-model-v1.md` §6.1), not an operator report; assessed there as the review's single highest-priority finding | REQ-PO-002-v1.md | SPEC-PO-002-v1.md | v2.9.72 | 2 rounds, both CONDITIONAL PASS. Round 1: 2 blocking findings — the original draft addressed only the parent PO document's fields, missing that the mismatch went one level deeper into every line item (`liId`/`up`/a stray per-line `cur` instead of `lid`/`cost`/no per-line currency, plus a missing `sku`); and it cited the wrong migration-wiring precedent (`migrateLinkedPOIds()`, 2 call sites, wrong entity) instead of the correct one (`backfillInvoicePOs()`, 5 call sites, same `DB.po` array). Both fixed in place. Round 2: CONDITIONAL PASS, 2 non-blocking wording advisories only (an unclear self-reference and an ambiguous doc-paragraph pointer in the tracker-update section), fixed in place; confirmed ready for SPEC. | CONDITIONAL PASS, advisories only (no blocking findings) — the reviewer went beyond reading citations and independently applied the full diff to a scratch copy of the codebase, ran the real 681-test suite (687/687 with the new tests), and executed both required mutation tests themselves, reproducing the exact predicted failure signatures. 3 advisories fixed in place: weaker-than-necessary "stray key" assertions at the document level tightened to match the already-strict line-item-level checks; the mutation-test instructions for the migration function now explicitly note the expected collateral failure count (~2 dozen unrelated tests) so a future build-gate reviewer isn't confused by it; and a narrow, pre-existing, out-of-scope CSV-reimport edge case the reviewer found along the way was disclosed as a new, separate, open/accepted known-gaps entry (`PO-GAP-006`) rather than silently dropped. | ✓ shipped — build-gate PASS | #124 |
| REQ-CLOUD-002 | Extend Cloud Data (Supabase) to Line Item and Contact — Phase 1 of the cross-platform backend migration's second step (`docs/architecture-data-model-v1.md` §8), following REQ-CLOUD-001's already-proven mechanism | REQ-CLOUD-002-v1.md | SPEC-CLOUD-002-v1.md | v2.9.73 | 2 rounds, both CONDITIONAL PASS. Round 1: 1 blocking (AC-2 omitted `dg`/`dims`/`priceHistory` and left `invoiceRefs[]`'s fate ambiguous) + 2 advisories, all fixed. Round 2: 2 advisories only (imprecise `SEC-GAP-011` section citation; AC-3 not held to the same field-enumeration standard as AC-2), fixed. | 4 rounds, all CONDITIONAL PASS. Round 1: 4 blocking (an unconditional `initCloudDataLayer()` wiring that would have silently wiped every existing Cloud Data adopter's real local Line Item/Contact data on first reload; `pullAll()`'s Sheets exclusion never extended to the two new entities; a zero-Supplier lockout in the Supplier-migration-completion check; the new restore buttons' visibility never surviving a reload) + 5 advisories, all fixed. Round 2: 1 new blocking (the completion-check's local marker could outlive the Supabase project it was true for) + 2 advisories, fixed. Round 3: 1 new blocking (five existing UI code paths — Supplier-Contact linking/unlinking, a Supplier deletion's Contact cascade, a Quote's Contact-status conversion/reversion — plus Line Item price-history auto-recording, all bypassing every Cloud Data branch entirely) + 1 advisory, fixed. Round 4: 1 trivial blocking (a wrong claim that `saveQte()` was already `async`, which would have been a SyntaxError as specified) + 2 advisories, fixed. | ✓ shipped — implemented per the fully-resolved SPEC, mutation-tested across every safety-critical check (each reverted mutation reproduced exactly its predicted single test failure), 25 new tests, 712/712 | #125 |
| REQ-CLOUD-003 | Extend Cloud Data (Supabase) to Order Request — Phase 2 sub-phase 1 of 3 of the cross-platform backend migration's deal-pipeline step (`docs/architecture-data-model-v1.md` §8), following REQ-CLOUD-001/002's already-proven mechanism. Scoped to Order Request only, with Quote (`REQ-CLOUD-004`) and Purchase Order (`REQ-CLOUD-005`) deferred as separate future sub-phases (REQ §0) — a scoping decision made and disclosed at REQ time, not asked of the user in advance. | REQ-CLOUD-003-v1.md | SPEC-CLOUD-003-v1.md | v2.9.74 | 3 rounds. Round 1: 3 blocking (the mutation-site inventory was wrong/incomplete — wrongly included `ordAddAction()`, missed `saveQte()`'s `convOrd` mutation and both of `delCon()`'s cascade branches; the `migrateSuppliersBuyersToSupabase()` retrofit wrongly targeted a `.contactId` gap that doesn't exist, since `migrateContactsToSupabase()` already handles it correctly; `order_requests.contact_id`'s FK-vs-plain-nullable description was inconsistent between two REQ sections) + 3 advisories, all fixed. Round 2: 1 new blocking (`executeDataCleanup()`'s renumbering step, missed by round 1's own "corrected, now-exhaustive" mutation-site list) + 1 advisory, fixed. Round 3: PASS, no new findings — confirmed via a third, independent search covering the AI action dispatch table, the RFQ email-parse flow, and all 14 `sv(K.ord, DB.ord)` call sites individually. | 3 rounds. Round 1: FAIL, 6 blocking (`contact_id`/`active_quote_id` typed `uuid` instead of `text`, which would reject every non-RFC-4122 local Contact/Quote id reference; `refreshOrdFromSupabase()` written but never wired into `initCloudDataLayer()`, so it would never run in practice; `saveOrd()`'s cloud-branch `row` silently discarded caller-supplied `lines`/`activeQuoteId`/`outcome` in favor of stale `existing.*` values; six new tests missing `selectData`, letting an internal refresh wipe `DB.ord` mid-test; the migration test's archive assertion not seeded from `localStorage`; four multi-record `persistOrdChange()` loops refreshing once per record instead of once per batch) + 4 advisories, all fixed. Round 2: FAIL, 3 new blocking (the async conversion of 8 functions was verified only against production callers, not the pre-existing 712-test suite itself — 12 tests broken by `async`'s universal return-value-wrapping and microtask-deferral rules; `processImport('ord')`'s `ordObj` literal missing an `activeQuoteId`/`outcome` fallback, which combined with round 1's B3 fix would silently wipe both on a Cloud-migrated update to a Quoted/Won/Lost Order Request; a combined test only actually exercised 3 of the 7 functions named in its own title) + 2 advisories, all fixed. Round 3: PASS, 0 blocking, 1 advisory (a missing test-hygiene cleanup test for the new block), fixed. | ✓ shipped — implemented per the fully-resolved SPEC. Two implementation-time defects caught by self-directed mutation testing that neither gate had tested for (both are genuine gaps in the SPEC's own test list, not gate failures — the SPEC only specified the code diff, not every regression test the diff needed): converting `processImport('ord')`'s per-submission `forEach` to a `for` loop required converting every internal `return` (a "skip to next submission") to `continue` (a `return` inside a `for` loop exits the whole function) — the SPEC's diff showed this correctly but the existing test suite had no case where an early submission's skip preceded a later valid one, so a reintroduced `return` went undetected until a new 4-submission regression test was added; and `saveOrd()`'s B3 fix (round 1) and the `processImport('ord')` `ordObj` fix (round 2) each had zero real test coverage despite passing both spec-gate rounds — added one regression test per fix. Mutation-tested every safety-critical check found this way plus the SPEC's own list (the B1 overwrite guard, all three `continue` sites, the `skipRefresh` batching, `saveOrd()`'s row-sourcing, the `ordObj` fallback) — each reverted mutation reproduced exactly its predicted test failure(s), with one set of legitimate multi-test collateral (disabling the overwrite guard correctly broke every OTHER call site that relies on it as a no-op when Order Request hasn't migrated, confirming the guard's role is broader than just the second-device case). 22 new tests, 734/734 pass. | #126 |
| REQ-CLOUD-004 | Extend Cloud Data (Supabase) to Quote — Phase 2 sub-phase 2 of 3 of the cross-platform backend migration's deal-pipeline step (`docs/architecture-data-model-v1.md` §8), following REQ-CLOUD-001/002/003's already-proven mechanism. Scoped to Quote only, with Purchase Order (`REQ-CLOUD-005`) remaining as the final deferred sub-phase (REQ §0, reaffirming REQ-CLOUD-003's own scoping decision). Quote is the first entity in this series whose own id is referenced *outward* by other entities (`Order Request.activeQuoteId`, `PurchaseOrder.quoteId`, `Invoice.linkedQuoteId`), requiring a genuinely novel outward sweep in addition to the usual inward retrofit. | REQ-CLOUD-004-v1.md | SPEC-CLOUD-004-v1.md | v2.9.75 | 3 rounds. Round 1: 3 blocking (the outward-sweep field list was incomplete, missing `Invoice.linkedQuoteId`; the required cross-phase retrofit for the outward sweep was claimed unnecessary when it is not; a self-contradictory SQL type for `dt`) + 3 advisories (a mutation site missing from the inventory, the outward sweep's robustness against `PO-GAP-007` left unstated, `Quote._demo`'s schema disposition left silent), all fixed. Round 2: 0 blocking, 1 advisory (the outward-retrofit rationale understated `refreshOrdFromSupabase()`'s real trigger surface — cited only "app boot," actually 8+ call sites), fixed. Round 3: 0 blocking, 1 cosmetic advisory (four inward-retrofit citations pointed at the wrong line; the `qteToPoConvert()` test-range citation slightly overextended), fixed — reviewer noted this did not warrant a fourth round. One additional self-caught, pre-SPEC clarification (no new round): the REQ's "Inward" framing implied `migrateQteToSupabase()` itself would rewrite other entities' fields "using already-established id-maps" that don't actually exist outside each sweep function's own single execution — corrected as a pure framing fix with zero AC/behavior impact. | 2 rounds. Round 1: CONDITIONAL PASS, 7 blocking (wiring the new refresh function unconditionally into `initCloudDataLayer()` broke a pre-existing Order Request test and, through it, a second one — empirically confirmed by reverting the fix and observing both predicted failures reappear; an optional-field mapping assigned `undefined` inside an object literal without actually omitting the key; a test for the primary migration function let its own trailing refresh call wipe test data before assertions ran; two `saveQte()` tests and two `delQte()` tests never satisfied the validation/confirmation gate needed to reach the code under test; a retrofit named in an acceptance criterion had a code diff but no test; the document gave two directly contradictory test-placement instructions) + 7 advisories (citation precision, test-idiom consistency, a dangling mock reset, a confusing test title, a missing exact doc diff, an acknowledged-not-fixed cosmetic double-render), all fixed and independently re-verified by applying every diff to a scratch copy of the real codebase and re-running the suite (752/752), not just re-reading the SPEC. Round 2 (independent re-verification, including independently re-reverting the two riskiest round-1 fixes to re-confirm load-bearing-ness): PASS, 0 blocking, 2 advisories (one a citation slip surviving round 1's own "fixed" claim, corrected; one a claimed test-declaration inconsistency that did not hold up against the actual document text, left unchanged with the disposition documented). | ✓ shipped — implemented per the fully-resolved, twice-verified SPEC, every diff applied cleanly to the real `index.html`/`tests/run.js`. New `supabase/migrations/0004_quotes.sql`. 18 new tests, 752/752 pass. | #127 |
| REQ-CLOUD-005 | Extend Cloud Data (Supabase) to Purchase Order — Phase 2 sub-phase 3 of 3, the final sub-phase of the cross-platform backend migration's deal-pipeline step (`docs/architecture-data-model-v1.md` §8), following REQ-CLOUD-001/002/003/004's already-proven mechanism. Completes Phase 2. Purchase Order is the first entity in this series built from three different creation paths (`savePO()`, `autoPos()`, `qteToPoConvert()`) rather than one save function, and the first whose own id is referenced outward by an array field (`Quote.linkedPOIds[]`) rather than a scalar. | REQ-CLOUD-005-v1.md | SPEC-CLOUD-005-v1.md | v2.9.76 | 3 rounds. Round 1: 5 blocking + 5 advisory — most notably that `autoPos()` was completely missing from the mutation-site inventory despite being confirmed as the entity's actual primary/majority creation path (only `qteToPoConvert()` had been assumed); all fixed. Round 2: 1 blocking (an earlier fix had over-generalized the empty-`supId` finding to both `autoPos()`/`qteToPoConvert()` when only `qteToPoConvert()` can actually produce it, since `autoPos()`'s own guard structurally excludes Supplier-less lines instead) + 2 advisories (two citation off-by-ones), fixed. Round 3: clean PASS, 0 findings. | 3 rounds, the most severe of this series. Round 1: **FAIL**, 10 blocking + 5 advisory — most critically a literal syntax error (`await` inside a non-async `savePayment()`) that would have broken the entire single-`<script>`-block application, reproduced by the reviewer as a collapse to 17/771 passing; also a `migratePOToSupabase()` test whose trailing refresh would wipe its own assertions' data, a `saveInv()` FPM test that never satisfied `vInv()`'s validator, both pre-existing `initCloudDataLayer()` tests needing retrofitting (not just one), and a vacuous `pullAll()` exclusion test mocking a function shadowed by its own enclosing closure — all fixed and re-verified against a scratch copy (775/775). Round 2: **FAIL**, 3 blocking — an independent reviewer applying every diff literally, not trusting round 1's own "all fixed" commit message, found the `pullAll()` exclusion fix only added a try-block guard and never the `_allPullKeys` array filter the request payload itself needed; the `savePayment()` diff showed `await` calls added to the body but never the required `async` signature-line change itself; and a stale pre-existing test's title/assertion ("PO creation itself always stays local regardless") was claimed fixed in the round-1 commit message but never actually touched in the document — all three fixed with explicit diffs and re-verified. Round 3: clean PASS, two trivial citation drifts (a modal-insertion line off by 4, a wrong function citation off by ~330 lines) fixed in place. | ✓ shipped — implemented per the fully-resolved, three-times-independently-verified SPEC, every diff applied cleanly to the real `index.html`/`tests/run.js`. New `supabase/migrations/0005_purchase_orders.sql`. Closes `PO-GAP-007`. Mutation-tested across every safety-critical piece identified (the outward `Quote.linkedPOIds[]` array sweep, the `PO-GAP-007` cleanup, both multi-PO-creation paths, both FPM-recovery pushes, the `pullAll()` exclusion fix) — each reverted mutation reproduced exactly its predicted test failure. 23 new tests, 775/775 pass. | (pending) |
| REQ-CLOUD-006 | Extend Cloud Data (Supabase) to Invoice and Credit Note — Phase 3 sub-phase 1 of 3 of the cross-platform backend migration (`docs/architecture-data-model-v1.md` §8, point 5), following REQ-CLOUD-001-005's already-proven mechanism. Invoice and Credit Note go first within Phase 3 because they share one local array (`DB.inv`) and are the least entangled with `REQ-INTEG-002`'s in-flight Buyer Payment work; Shipment (sub-phase 2) and Buyer/Supplier Payment (sub-phase 3) remain deferred. The most demanding precondition in the series (four prerequisite entities — Buyer, Line Item, Quote, Purchase Order) and the first self-referential FK within a migrating table (a Credit Note's `linkedInvId` pointing at another row in the same batch). | REQ-CLOUD-006-v1.md | SPEC-CLOUD-006-v1.md | v2.9.79 | 8 rounds. Round 1: 3 blocking (a false CSV-import precedent citation; an inconsistent 13-vs-8-site `persistInvChange()` call-site count; half of PO's real `pullAll()` exclusion mechanism cited) + 4 advisory, all fixed. Round 2: 1 blocking (round 1's own CSV-import fix replaced one false precedent with another) + 1 advisory, fixed. Round 3: re-raised and definitively resolved a `pullAll()` citation dispute, triggering a full citation-accuracy pass. Round 4: 1 blocking (goodwill-credit `DB.payments[]` self-reference needed explicit sweep coverage) + 1 advisory. Round 5: 1 blocking (Purchase Order outward sweep's `invId`/`invNum` matching left unspecified). Round 6: 1 blocking (identical matching ambiguity, one array over, for `DB.payments[].invId`). Round 7: 1 blocking (a third instance of the same bug class, in the CN self-referential remap itself). Round 8: **PASS**, clean. | Round 1: CONDITIONAL PASS/FAIL, 3 blocking, fixed in place — `saveCN()`'s cloud branch left the `DB.inv` commit inside the local-only branch only (a real implementation bug once applied), plus two test-authoring bugs (empty `cIL` blocking `vInv()`; `autoPos()` test missing PO's own migration marker). Round 2: independent re-verification from scratch, PASS — 835/835 against the SPEC's own 803-baseline. | ✓ shipped — implemented against this worktree's actual code, which already carried `REQ-INTEG-002-2c` (merged from `main` after the SPEC was drafted against an older commit). Applied every diff in §2/§3/§4, adapting three functions whose "Current" text had already been superseded by INTEG-002-2c's own shipped improvements rather than reverting them: `saveCN()`'s balance-due recompute (kept `getInvEffectiveDepInfo(linkedInv).value` in place of the SPEC's stale raw-filter/reduce text while still relocating the cloud-aware commit ahead of the goodwill-credit push, per the SPEC's round-1 fix); `savePayment()` (kept `getInvTotalPaidNative(inv)`); `deletePayment()` (converted to `async` and added call site #5 while preserving INTEG-002-2c's own added status-re-derivation block, absent from the SPEC's "Current" text). Found and fixed one genuine test-authoring bug during implementation, not present in either spec-gate round's own verification: the new `processImportRecords()` CSV/AC-12 test pushed a record numbered `INV75001` but asserted for `INV74001` — the mismatch threw before the test's own cleanup lines ran, leaking `_sb`/`st_inv_cloud_migration_ts` forward into the entire remaining async suite; fixed by aligning the pushed and asserted numbers. Self-directed mutation testing: (a) reverted `saveCN()`'s `DB.inv` commit back to the local-only branch only (reproducing round 1's original bug) — confirmed exactly one test fails, the dedicated AC-8/call-site-#9 regression test (the AC-5 saveCN test does not exercise this path and correctly stayed green), then undone. (b) swapped the two-pass remap's `linkedInvNum`-first precedence to `linkedInvId`-first — the SPEC's own AC-4b test (a CN with a genuinely *blank* `linkedInvId`) turned out NOT to discriminate order, since a falsy `linkedInvId` falls through to the `linkedInvNum` fallback under either precedence; this is a real, narrow test-coverage gap in the SPEC's own AC-4b, found only by actually running the mutation rather than reading the test. Added one new regression test exercising the true order-discriminating shape (`linkedInvNum`/`linkedInvId` both present and pointing at *different* invoices) — confirmed it fails exactly as expected under the mutation (resolves to the wrong, stale invoice), then the mutation was undone and the new test reconfirmed green. Suite reconfirmed fully green after each revert. Build-gate independent review went 2 rounds: round 1 (CONDITIONAL PASS) found the identical non-discriminating-fixture gap in AC-6a's (Purchase Order sweep) and AC-7a's (payments sweep) own tests — closed with two more genuinely order-discriminating regression tests, each independently mutation-tested by the reviewer, reproducing exactly one isolated failure per sweep; round 2 independent re-verification from scratch, clean PASS. New `supabase/migrations/0006_invoices.sql`. 35 new tests (834/834 baseline, itself already including `REQ-INTEG-002-2c`'s 31 tests on top of this REQ's own 803-baseline; 32 from the SPEC's own §3.1 plus 1 build-time addition, plus 2 more from build-gate round 1's fix) → **869/869 pass**. | #131 |
| REQ-CLOUD-007 | Extend Cloud Data (Supabase) to Shipment — Phase 3 sub-phase 2 of 3 of the cross-platform backend migration (`docs/architecture-data-model-v1.md` §8, point 5), following REQ-CLOUD-001-006's already-proven mechanism. Structurally the simplest migration in the series so far — one creation path, zero outward/inward references, zero migration precondition, zero cross-phase retrofit, zero CSV-import branch. | REQ-CLOUD-007-v1.md | SPEC-CLOUD-007-v1.md | v2.9.80 | 3 rounds. Round 1: 2 blocking (a `mapRec()`/`unmapRec()` asymmetry lets ordinary Sheets sync silently corrupt `linkedInvs` from an array into a string, threatening the new `linked_invs jsonb` column's shape — fixed via new REQ-CLOUD-007i-1/AC-11, logged as `SH-GAP-002`; a doc-update requirement that would have compounded pre-existing staleness rather than fixing it) + 3 advisory, all fixed. Round 2: 1 blocking (a third stale-docs spot, `index.html:781`, round 1's fix didn't cover) + 2 advisory, fixed. Round 3: independently re-reproduced the `linkedInvs` bug/fix a third time, confirmed clean; found the doc-staleness pattern extends beyond this REQ's own scope — resolved by fixing that whole class directly outside this REQ (`#133`), narrowing this REQ's own remaining doc obligation to a small "in progress" → "done" flip at ship time. | CONDITIONAL PASS/FAIL, 1 blocking, fixed in place — confined entirely to two of the SPEC's own newly-authored tests (an unstubbed trailing `refreshShFromSupabase()` call wiping `DB.sh` out from under its own assertion, with one leaking a stub past its own cleanup into two subsequent tests); the implementation diffs were independently confirmed correct, complete, and precisely matched to the REQ with no gaps. Fixed and re-verified at 887/887; no second spec-gate round dispatched, per the project's tightened-process convention for narrow, mechanical, self-verified fixes. | ✓ shipped — build-gate independently mutation-tested the `linkedInvs` coercion (reverting it to a bare passthrough reproduced exactly the two AC-11 test failures, nothing else) and the `initCloudDataLayer()` retrofit (removing one of the four required stubs reproduced the exact self-marking-marker corruption this series has hit repeatedly) — both confirmed load-bearing on the actual committed code, not just the SPEC's own scratch-copy verification. Found one real process gap: this REQ's own doc-staleness resolution (`#133`) had been drafted and reviewed but not actually merged to `main` yet — closed by merging `#133` and performing the small, mechanical "in progress" → "done" flip this REQ's own narrowed doc obligation (REQ-CLOUD-007k) called for, across `index.html:781`/`10702`, `CLAUDE.md`, and `docs/architecture-data-model-v1.md` §2/§8. New `supabase/migrations/0007_shipments.sql`. 18 new tests → **887/887 pass**. | (pending) |
| REQ-LI-001 | Line Item unit cost/price: 3-decimal-place precision — user-requested, for high-volume/low-unit-price items losing real precision under the 2-decimal `RX.currency` cap | REQ-LI-001-v1.md | SPEC-LI-001-v1.md | v2.9.77 | 4 rounds. Round 1: 3 blocking (an earlier draft mislabeled `index.html:8498` as a Quote render site — it's Invoice — and missed the real Quote PDF site (`prevQteDoc()`, `12758`) and a second independent render of price-history data in `renderSupPriceHistory()` (`5468`) entirely; also an internal field-count error, "8 other fields" vs. the real 11, that caused AC-2's test list to omit `pf-oth`) + 2 advisory, fixed. Round 2: 2 blocking (a 7th display-truncation site independently found, `renderQteLineHistory()`'s `fn(v.cost,0)` at `12123`; `renderSupPriceHistory()`'s data-lineage description missing a third source), fixed. Round 3: 1 blocking (§1.2's "seven render paths" summary undercounted relative to AC-3+AC-4's own total of 8, caused by bundling two distinct PDF functions into one bullet), fixed. Round 4: clean PASS, 0 findings. | Clean PASS — every diff verified byte-for-byte against the live file, all 5 `fmt()`→`fmtN()` and 5 `fn(...,3)` sites applied, 775/775 baseline confirmed unbroken by the diffs alone (before any new tests), `RX.currency3`/`fmtN()` manually verified against their exact intended outputs, no coverage gaps found against REQ's AC-1 through AC-7. | ✓ shipped — build-gate independent review CONDITIONAL PASS (2 non-blocking hygiene findings: the AC-5 margin test left `lf-c`/`lf-p` dirty in the shared, never-reset mock-DOM object — the identical test-isolation hazard the AC-2 loop had already been fixed for, just missed in one more spot; and a `fmt()` call-count sanity test the SPEC had promised for AC-7 was never actually written — both fixed in place, no re-review needed). Mutation-tested: `RX.currency3`'s regex, `vLI()`'s two guard checks, one `fmtN()` site, one `fn(...,3)` site, the `step` attribute, and (separately) `RX.currency` itself each reproduced exactly their predicted single/eleven-test failure(s) when reverted; the `lf-c`/`lf-p` on-blur wiring lines could not be mutation-tested at the wiring level specifically since this test harness's mock DOM no-ops `addEventListener` — a pre-existing limitation affecting every blur handler in the suite, not specific to this change. 28 new tests, 803/803 pass. | (pending) |
| REQ-QTE-002 | Per-quote overhead charge overrides (Origin Charges, Dest Charges, FPM Admin) — user asked why a printed quote's overhead figures couldn't be adjusted or removed; found to be global Rate Engine defaults with no per-quote override | REQ-QTE-002-v1.md | SPEC-QTE-002-v1.md | v2.9.68 | CONDITIONAL PASS, no blocking findings (3 advisories fixed in place — an inverted claim about Sheets-sync survival via `mergePulledWithLocal()`, two citation drifts, one missing AC for the overhead-override + per-line-markup-override interaction, now AC-7; §8 has the full review-resolution log) | CONDITIONAL PASS (1 real blocking finding — the `qteSellTotals()` signature change would have broken two existing tests that call it directly with the old 4-arg form; the reviewer applied every other diff to a scratch copy and reproduced the failure before the fix was made — resolved by adding the required test-call-site updates to the spec itself, §9.0. 2 advisories also fixed: an imprecise citation, an overclaimed test-precedent note) | ✓ shipped — build-gate independent review **PASS**, no findings. Every AC (1-7) verified against the real committed code; the "explicit 0 override" guard's `!== undefined/null/''` check (not a truthy check) independently mutation-tested by the reviewer — reproduced the exact 3-test failure the commit message claimed, then reverted and reconfirmed clean; the `prevQteDoc()` PDF-breakdown fix (reading `c.overheadBreakdown` instead of raw `QR`) also independently mutation-tested and reproduced. `saveQte()`'s fresh-object-per-save construction confirmed to correctly drop a cleared override on re-save. No regression to `qteEffectiveMargin()`, per-line `ql-mkp` overrides, `rQte()`, or the `create_quote` AI action (confirmed untouched by diff). 11 new tests. 641/641 pass. | #117 |
| REQ-CUR-002 | Close `ACCT-GAP-001`/`AI-GAP-010` — the two remaining currency-mixing gaps logged (not fixed) alongside REQ-INTEG-002-2a-fix-2, same defect class, user-prioritized as the immediate next fix after that shipped | REQ-CUR-002-v1.md | SPEC-CUR-002-v1.md | v2.9.65 | CONDITIONAL PASS (3 advisory only — a wrong test-precedent line citation, a `fmt()` signature citation off by 9 lines, `avgMargin` not explicitly flagged as the same self-correcting-ratio case as `pct` — all 3 resolved in place, no second round needed) | — (no independent spec-gate FAIL round; the one blocking spec-gate finding, below, was resolved directly in the spec before implementation) | ✓ shipped — `renderAccts()`'s per-invoice (`supDepPaid`/`fpmFunded`/`supBalDue`/`totalToChase`) and per-supplier (`totalCOGS`/`totalDep`/`totalBal`) sums now convert each linked PO's figure via `toDisp(amount, po.cur||'USD')` before summing, rendered in `QR.displayCurrency`; invoice-native columns (Invoice Total, Buyer Dep. Rec., Buyer Bal. Due) unchanged. Check-first found the original `ACCT-GAP-001` entry under-scoped — `fpmFunded`/`totalToChase`/`totalCOGS` weren't named in it, fixed together here regardless. `_aiExecTool('get_kpis')` gets the identical fix for `invoiceRevenue`/`netProfit`/`outstanding`/`poBalanceDue`, plus a new `currency` field in its JSON response and an updated `AI_TOOLS` description. `pct` (Dep. Coverage %) and `avgMargin` needed no code change — proven algebraically invariant to the display-currency choice, since both are ratios of two sums over the same record set, both converted identically (the common per-currency factor cancels). Spec-gate independent review found one blocking issue before implementation: a pre-existing test (`poBalanceDue`/`depositPaid`/`balanceDue` ledger-total test) would have broken once `get_kpis` started converting currencies, since it never pinned `QR.displayCurrency` — fixed by pinning it to the test PO's own currency (`'USD'`), consistent with the established regression-guard pattern, before implementation began. 5 new tests. 609/609 pass. Build-gate independent review **PASS**, 1 advisory (docs/tracker updates not yet applied at review time — this row is that update) — every diff confirmed against spec with no deviations; `totalToChase`'s `toDisp(balFromBuyer,cur) + unrecoveredFPM` traced end-to-end for double-conversion risk, none found; mutation test (temporarily reverting one `toDisp()` wrap in `get_kpis`) confirmed the new tests are not tautological — the corresponding test failed as expected, then the revert was undone. | #108 |

**Note on Sheet ID reference:** per this REQ's brief, the Requirements Tracker Sheet ID `1q05sSoCMmiqaNNixDWVk2_aJPwEqx37vDbOPNh2gqGw` is the canonical tracker referenced — all actual tracker record-keeping for this initiative continues to be performed in this local markdown file, consistent with every other REQ/SPEC entry in this tracker; no separate Google Sheets update was made.

---

## Backlog / unscoped

| ID | Title | Area | Logged | Decision |
|---|---|---|---|---|
| AI-GAP-001 (broad) | Agentic multi-step order flow | AI Assistant | v2.9.27 | Deferred v3.0.x — requires server-side proxy |
| REQ-V3-GAP-006 (ev) | Event log emissions on delCon / link / unlink | Event log + Contacts | v2.9.28 | **✓ closed v2.9.56.** `delCon()`'s emission (the only part `REQ-V3-GAP-006-v3.md` actually deferred) was found already implemented when checked against the live code — `link`/`unlink` emissions were never a formally-gated requirement (only present in this backlog row's own description), added as a small, proportionate fix mirroring the already-established `logEv()` pattern rather than run through a full REQ/SPEC cycle. |
| S3-1 | Demo shipment mode | Trial conversion | pre-sprint | ✓ shipped v2.9.31 |
| S3-2 | MTD-compatible VAT export | Compliance | pre-sprint | ✓ shipped v2.9.32 |
| REQ-RPT-001 G-01 | AI date filter (get_invoices, get_payments) | AI / Reporting | v2.9.33 | **✓ shipped v2.9.34** (SPEC-RPT-001-G01-G04-v1.md) — tracker row corrected 2026-08-22, previously left showing "target v2.9.34" after shipping; see `docs/version-history.md` v2.9.34. |
| REQ-RPT-001 G-02 | Aging Report (0–30/31–60/61–90/90+ days, DSO) | Financial Control | v2.9.33 | **✓ shipped v2.9.35** (SPEC-RPT-001-G02-G03-v1.md) — tracker row corrected 2026-08-22; see `docs/version-history.md` v2.9.35. |
| REQ-RPT-001 G-03 | P&L by dimension (buyer, period) | Financial Control | v2.9.33 | **✓ shipped v2.9.35** (SPEC-RPT-001-G02-G03-v1.md) — tracker row corrected 2026-08-22; see `docs/version-history.md` v2.9.35. |
| REQ-RPT-001 G-04 | Quick-add COGS warning in invoice modal | Data Quality | v2.9.33 | **✓ shipped v2.9.34** (SPEC-RPT-001-G01-G04-v1.md) — tracker row corrected 2026-08-22; see `docs/version-history.md` v2.9.34. |
| REQ-RPT-001 G-05 | Full entity event log (Invoice, PO, Payment, Supplier) | Audit | v2.9.33 | **✓ shipped v2.9.36** (SPEC-RPT-001-G05-G06-v1.md) — tracker row corrected 2026-08-22; see `docs/version-history.md` v2.9.36. |
| REQ-RPT-001 G-06 | Invoice edit delta logging (old → new values) | Audit | v2.9.33 | **✓ shipped v2.9.36** (SPEC-RPT-001-G05-G06-v1.md) — tracker row corrected 2026-08-22; see `docs/version-history.md` v2.9.36. |
| REQ-RPT-001 G-07 | Input VAT on POs + MTD Boxes 4 & 7 | Compliance | v2.9.33 | Req gate CONDITIONAL PASS — council gate required before spec (`REQ-RPT-001-v1.md` §G-07). **Decision 2026-08-22: deferred to v3.0.x, council gate not run.** Boxes 4 & 7 remain operator responsibility (MTD-GAP-001, per `STACKD_CONTEXT.md`'s existing framing) — a live, real recurring VAT-reclaim under-claim, but building input-VAT tracking now against a single-file, no-server, no-audit-trail-beyond-`DB.events` stack was judged higher-risk than the value of closing it ahead of the v3.0 Supabase migration, where server-side validation and a proper audit trail make this materially safer to get right. Revisit as part of v3.0.x planning rather than late in the v2.9.x line. |
| REQ-RPT-001 G-08 | Intrastat report (UK → EU, 8-box CSV) | Compliance | v2.9.33 | Deferred v3.0.x — new schema required |
| REQ-RPT-001 G-09 | Supplier performance tracking (on-time %, cost variance) | Operational | v2.9.33 | Deferred v3.0.x |
| REQ-RPT-001 G-10 | HS code duty recalculation on existing invoices | Data Integrity | v2.9.33 | Deferred v3.0.x |
| REQ-SUP-001 | Supplier price intelligence retention & reference view | Supplier Intelligence / R-007 | v2.9.53 | **Built and shipped v2.9.53** — see Active requirements above. `getSupplierPriceHistory()`/`supPriceStaleness()`/`getProductPriceHistory()`/`renderSupPriceHistory()` added byte-for-byte per `SPEC-SUP-001-v2.md`; Price History panel wired into `openSup()`/`editSup()`; new Settings → Supplier Intelligence threshold control. 10 new tests, 454/454 passing. **Build-gate: independent review PASS** — every function/HTML/wiring change verified byte-for-byte against spec, tests confirmed real and passing; 2 advisory-only notes (a "9 vs 10 new tests" documentation undercount, since fixed, and a pre-existing spec-approved input-validation gap on the threshold setting — negative values aren't guarded, cosmetic-only impact in this trusted single-operator app). |

---

## Gate pipeline

```
requirements-gate → spec-gate → build-gate → security-gate
```

- **requirements-gate** — verifies requirement is complete, unambiguous, testable; flags GDPR/FPM risks
- **spec-gate** — verifies spec covers all ACs, is implementable without design decisions
- **build-gate** — post-build review of index.html changes against spec
- **security-gate** — run before any release to main

---

## FM-1 exception register

All active requirements have FM-1 clearance. See STACKD_CONTEXT.md for full exception text.

| Item | Scope | Approved |
|---|---|---|
| 1 | UI/AI layer features with no new localStorage entities | 2026-06-21 |
| 2 | New fields on existing entities (e.g. supplierId, role on DB.con) | 2026-06-21 |
| 3 | New internal-only K key / DB entity with no Sheets sync (K.ev / DB.events) | 2026-06-21 |
| 4 | Live external database dependency for Suppliers + Buyers only (`REQ-CLOUD-001`, Supabase) — scope-specific, not a general category; condition: mandatory backed-up/rollback-verified migration (see `REQ-CLOUD-001-v3.md`) | 2026-08-21 |
